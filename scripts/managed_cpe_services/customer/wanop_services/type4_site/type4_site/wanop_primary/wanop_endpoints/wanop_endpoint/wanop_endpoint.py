#
# This computer program is the confidential information and proprietary trade
# secret of Anuta Networks, Inc. Possessions and use of this program must
# conform strictly to the license agreement between the user and
# Anuta Networks, Inc., and receipt or possession does not convey any rights
# to divulge, reproduce, or allow others to use this program without specific
# written authorization of Anuta Networks, Inc.
#
# Copyright (c) 2016-2017 Anuta Networks, Inc. All Rights Reserved.
#

#
#DO NOT EDIT THIS FILE ITS AUTOGENERATED ONE
#ALL THE CUSTOMIZATIONS REGARDING DATAPROCESSING SHOULD BE WRITTEN INTO service_customization.py FILE
#
"""
Tree Structure of Handled XPATH:

services
        |
        managed-cpe-services
                            |
                            customer
                                    |
                                    wanop-services
                                                  |
                                                  type4-site
                                                            |
                                                            type4-site
                                                                      |
                                                                      wanop-primary
                                                                                   |
                                                                                   wanop-endpoints
                                                                                                  |
                                                                                                  wanop-endpoint
                                                                                                                
Schema Representation:

/services/managed-cpe-services/customer/wanop-services/type4-site/type4-site/wanop-primary/wanop-endpoints/wanop-endpoint
"""

from servicemodel import util
from servicemodel import yang
from servicemodel import devicemgr

from cpedeployment.cpedeployment_lib import getLocalObject
from cpedeployment.cpedeployment_lib import getDeviceObject
from cpedeployment.cpedeployment_lib import getCurrentObjectConfig
from cpedeployment.cpedeployment_lib import getPreviousObjectConfig
from cpedeployment.cpedeployment_lib import ServiceModelContext
from cpedeployment.cpedeployment_lib import getParentObject
from cpedeployment.cpedeployment_lib import log


import service_customization


class WanopEndpoint(yang.AbstractYangServiceHandler):
    _instance = None

    def __init__(self):
        self.delete_pre_processor = service_customization.DeletePreProcessor()
        self.create_pre_processor = service_customization.CreatePreProcessor()
        self.opaque_args = {}

    def create(self, id, sdata):
        sdata.getSession().addYangSessionPreReserveProcessor(self.create_pre_processor)

        #Fetch Local Config Object
        config = getCurrentObjectConfig(id, sdata, 'wanop_endpoint')

        #Fetch Service Model Context Object
        smodelctx = None

        #Fetch Parent Object
        parentobj = getParentObject(sdata)

        dev = []
        inputkeydict = {}
        devbindobjs={}
        inputdict = {}
        opaque_args = self.opaque_args

        # START OF FETCHING THE LEAF PARAMETERS
        inputdict['interface_name'] = config.get_field_value('interface_name')
        inputdict['profile_name'] = config.get_field_value('profile_name')
        inputdict['cidr'] = config.get_field_value('cidr')
        inputdict['interface_ip'] = config.get_field_value('interface_ip')
        inputdict['interface_description'] = config.get_field_value('interface_description')
        inputdict['gateway_ip'] = config.get_field_value('gateway_ip')
        inputdict['speed'] = config.get_field_value('speed')
        inputdict['duplex'] = config.get_field_value('duplex')
        inputdict['mtu'] = config.get_field_value('mtu')
        inputdict['vrf'] = config.get_field_value('vrf')
        inputdict['vlan_id'] = config.get_field_value('vlan_id')
        inputdict['gateway_ip'] = config.get_field_value('gateway_ip')#leaf gateway-ip type False
        inputdict['cidr'] = config.get_field_value('cidr')#leaf cidr type False
        inputdict['enable_optimization'] = config.get_field_value('enable_optimization')#leaf enable-optimization type False

        # END OF FETCHING THE LEAF PARAMETERS

        _Gen_obj = getLocalObject(sdata, 'wanop-primary')
        device_mgmt_ip_address = _Gen_obj.wanop_primary.device_ip

        #Fetch Device Object
        dev = getDeviceObject(device_mgmt_ip_address, sdata)

        # START OF FETCHING THE PARENT KEY LEAF PARAMETERS
        inputkeydict['managed_cpe_services_customer_wanop_services_type4_site_type4_site_site_name'] = sdata.getRcPath().split('/')[-4].split('=')[1]
        inputkeydict['managed_cpe_services_customer_name'] = sdata.getRcPath().split('/')[-7].split('=')[1]
        # END OF FETCHING THE PARENT KEY LEAF PARAMETERS

        #Use the custom methods to process the data
        service_customization.ServiceDataCustomization.process_service_create_data(smodelctx, sdata, dev, id=id, device=dev, parentobj=parentobj, inputdict=inputdict, inputkeydict=inputkeydict, config=config, hopaque=opaque_args)
        #Use the custom method to process/create payload
        service_customization.ServiceDataCustomization.process_service_device_bindings(smodelctx, sdata, dev, id=id, device=dev, inputdict=inputdict, inputkeydict=inputkeydict, parentobj=parentobj, config=config, devbindobjs=devbindobjs, hopaque=opaque_args)

    def update(self, id, sdata):
        #Fetch Local Config Object
        config = getCurrentObjectConfig(id, sdata, 'wanop_endpoint')
        pconfig = getPreviousObjectConfig(id, sdata, 'wanop_endpoint')
        opaque_args = self.opaque_args

        #Fetch Service Model Context Object
        smodelctx = None

        #Fetch Parent Object
        parentobj = getParentObject(sdata)

        dev = []
        inputkeydict = {}
        devbindobjs={}
        inputdict = {}
        opaque_args = self.opaque_args

        # START OF FETCHING THE LEAF PARAMETERS
        inputdict['interface_name'] = config.get_field_value('interface_name')
        inputdict['profile_name'] = config.get_field_value('profile_name')
        inputdict['cidr'] = config.get_field_value('cidr')
        inputdict['interface_ip'] = config.get_field_value('interface_ip')
        inputdict['interface_description'] = config.get_field_value('interface_description')
        inputdict['gateway_ip'] = config.get_field_value('gateway_ip')
        inputdict['speed'] = config.get_field_value('speed')
        inputdict['duplex'] = config.get_field_value('duplex')
        inputdict['mtu'] = config.get_field_value('mtu')
        inputdict['vlan_id'] = config.get_field_value('vlan_id')
        inputdict['gateway_ip'] = config.get_field_value('gateway_ip')#leaf gateway-ip type False
        inputdict['cidr'] = config.get_field_value('cidr')#leaf cidr type False
        inputdict['enable_optimization'] = config.get_field_value('enable_optimization')#leaf enable-optimization type False

        # END OF FETCHING THE LEAF PARAMETERS

        pinputdict = {}

        # START OF FETCHING THE PREVIOUS LEAF PARAMETERS
        pinputdict['interface_name'] = pconfig.get_field_value('interface_name')
        pinputdict['profile_name'] = pconfig.get_field_value('profile_name')
        pinputdict['cidr'] = pconfig.get_field_value('cidr')
        pinputdict['interface_ip'] = pconfig.get_field_value('interface_ip')
        pinputdict['interface_description'] = pconfig.get_field_value('interface_description')
        pinputdict['gateway_ip'] = pconfig.get_field_value('gateway_ip')
        pinputdict['speed'] = pconfig.get_field_value('speed')
        pinputdict['duplex'] = pconfig.get_field_value('duplex')
        pinputdict['mtu'] = pconfig.get_field_value('mtu')
        pinputdict['vlan_id'] = pconfig.get_field_value('vlan_id')
        pinputdict['gateway_ip'] = pconfig.get_field_value('gateway_ip')#leaf gateway-ip type False
        pinputdict['cidr'] = pconfig.get_field_value('cidr')#leaf cidr type False
        pinputdict['enable_optimization'] = pconfig.get_field_value('enable_optimization')#leaf enable-optimization type False

        _Gen_obj = getLocalObject(sdata, 'wanop-primary')
        device_mgmt_ip_address = _Gen_obj.wanop_primary.device_ip

        #Fetch Device Object
        dev = getDeviceObject(device_mgmt_ip_address, sdata)

        #Use the custom method to process the data
        service_customization.ServiceDataCustomization.process_service_update_data(smodelctx, sdata, id=id, dev=dev, parentobj=parentobj, config=config, pconfig=pconfig, hopaque=opaque_args, inputdict=inputdict, pinputdict=pinputdict)

    def delete(self, id, sdata):
        sdata.getSession().addYangSessionPreReserveProcessor(self.delete_pre_processor)

        #Fetch Local Config Object
        config = getCurrentObjectConfig(id, sdata, 'wanop_endpoint')
        opaque_args = self.opaque_args

        #Fetch Service Model Context Object
        smodelctx = None

        #Fetch Parent Object
        parentobj = getParentObject(sdata)

        dev = []
        inputkeydict = {}
        devbindobjs={}
        inputdict = {}
        opaque_args = self.opaque_args

        # START OF FETCHING THE LEAF PARAMETERS
        inputdict['interface_name'] = config.get_field_value('interface_name')#leaf interface-name type True
        inputdict['profile_name'] = config.get_field_value('profile_name')#leaf profile-name type True
        inputdict['interface_ip'] = config.get_field_value('interface_ip')#leaf interface-ip type False
        inputdict['mask_length'] = config.get_field_value('mask_length')#leaf mask-length type False
        inputdict['interface_description'] = config.get_field_value('interface_description')#leaf interface-description type False
        inputdict['speed'] = config.get_field_value('speed')#leaf speed type False
        inputdict['duplex'] = config.get_field_value('duplex')#leaf duplex type False
        inputdict['mtu'] = config.get_field_value('mtu')#leaf mtu type False
        inputdict['vlan_id'] = config.get_field_value('vlan_id')#leaf vlan-id type False
        inputdict['enable_optimization'] = config.get_field_value('enable_optimization')#leaf enable-optimization type False

        # END OF FETCHING THE LEAF PARAMETERS

        _Gen_obj = getLocalObject(sdata, 'wanop-primary')
        device_mgmt_ip_address = _Gen_obj.wanop_primary.device_ip

        #Fetch Device Object
        dev = getDeviceObject(device_mgmt_ip_address, sdata)

        #Use the custom method to process the data
        service_customization.ServiceDataCustomization.process_service_delete_data(smodelctx, sdata, id=id, dev=dev, parentobj=parentobj, config=config, hopaque=opaque_args, inputdict=inputdict)

    @staticmethod
    def getInstance():
        if(WanopEndpoint._instance == None):
            WanopEndpoint._instance = WanopEndpoint()
        return WanopEndpoint._instance
