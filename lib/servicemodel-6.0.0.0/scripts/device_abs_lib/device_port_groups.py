#
# This computer program is the confidential information and proprietary trade
# secret of Anuta Networks, Inc. Possessions and use of this program must
# conform strictly to the license agreement between the user and
# Anuta Networks, Inc., and receipt or possession does not convey any rights
# to divulge, reproduce, or allow others to use this program without specific
# written authorization of Anuta Networks, Inc.
#
# Copyright (c) 2015-2016 Anuta Networks, Inc. All Rights Reserved.
#

#
#DO NOT EDIT THIS FILE ITS AUTOGENERATED ONE
#

from servicemodel import util
from servicemodel import yang
from servicemodel import devicemgr
from servicemodel.abstract_dev_mgr import AbstractDeviceMgr

def get_valid_devices(devs):
  vdevs = []
  if isinstance(devs, list):
    for dev in devs:
      drivername = dev.device.get_field_value('driver_name')
      if util.isEmpty(drivername):
        vdevs.append(dev)
  else:
    drivername = devs.device.get_field_value('driver_name')
    if util.isEmpty(drivername):
      vdevs.append(devs)

  return vdevs

class port_groups(object):
  #XPATH devices/device/port-groups/port-group
  class port_group(AbstractDeviceMgr):
    key_hints = [[]]
    def getRcpathPayload(self, sdata, dev, mapping_dict):
      dev = get_valid_devices(dev)
      if len(dev) == 0:
        return
      #convert parent keys to list
      rcpath_list = []
      payload_list = []
      ##prepare rcpath
      rcpath = "l2features:port-groups"
      rcpath_list.append(rcpath)
      mapping_dict = kwargs.get('mapping_dict')

      #validating inputs and get payload object
      port_group_object_list = self.validate_inputs_form_payload(mapping_dict)

      for port_group_object in port_group_object_list:
        #fetch payload
        port_group_payload = port_group_object.getxml(filter=True)
        util.log_debug('port_group_payload %s'%port_group_payload)
        payload_list.append(port_group_payload)

      return rcpath_list, payload_list

    def create(self, sdata, dev, mapping_dict, addref=True, autocommit=True):
      dev = get_valid_devices(dev)
      if len(dev) == 0:
        return
      #convert parent keys to list
      ##prepare rcpath
      rcpath = "l2features:port-groups"
      self.create_(sdata, dev, mapping_dict=mapping_dict, addref=addref, autocommit=autocommit, rcpath=rcpath)

    def create_(self, sdata, dev, **kwargs):
      mapping_dict = kwargs.get('mapping_dict')

      #validating inputs and get payload object
      port_group_object_list = self.validate_inputs_form_payload(mapping_dict)

      for port_group_object in port_group_object_list:
        #fetch payload
        port_group_payload = port_group_object.getxml(filter=True)

        util.log_debug('port_group_payload %s'%port_group_payload)

        #call the base abstract class for createData
        super(self.__class__, self).create_(sdata, dev, rcpath=kwargs.get('rcpath'), payload=port_group_payload, key_hints=self.key_hints, addref=kwargs.get('addref', True), autocommit=kwargs.get('autocommit', True), mapping_dict=mapping_dict)

    def update(self, sdata, dev, mapping_dict, addref=False, autocommit=True):
      dev = get_valid_devices(dev)
      if len(dev) == 0:
        return

      ##prepare rcpath
      rcpath = "l2features:port-groups"
      self.update_(sdata, dev, mapping_dict=mapping_dict, addref=addref, autocommit=autocommit, rcpath=rcpath)

    def update_(self, sdata, dev, **kwargs):
      mapping_dict = kwargs.get('mapping_dict')

      #validating inputs and get payload object
      port_group_object_list = self.validate_inputs_form_payload(mapping_dict, update=True)

      #convert keys to list
      name = mapping_dict.get('name')
      if not isinstance(name, list):
        name = [name]

      #prepare rcpath
      rcpath = kwargs.get('rcpath')
      rcpath_list = []
      for name_iterator in name:
        rcpath_tmp =  rcpath+"/port-group=%s"%(util.make_interfacename(name_iterator))
        rcpath_list.append(rcpath_tmp)
      for rc_counter, port_group_object in enumerate(port_group_object_list):
        #fetch payload
        port_group_payload = port_group_object.getxml(filter=True)

        util.log_debug('update port_group_payload %s'%port_group_payload)

        rcpath = rcpath_list[rc_counter]
        #call the base abstract class for createData
        super(self.__class__, self).update_(sdata, dev, rcpath=rcpath, payload=port_group_payload, key_hints=self.key_hints, addref=kwargs.get('addref', False), autocommit=kwargs.get('autocommit', True), mapping_dict=mapping_dict)

    def delete(self, sdata, dev, mapping_dict, fail_silently=False, remove_reference=False):
      dev = get_valid_devices(dev)
      if len(dev) == 0:
        return

      ##prepare rcpath
      rcpath = "l2features:port-groups"
      self.delete_(sdata, dev, mapping_dict=mapping_dict, fail_silently=fail_silently, remove_reference=remove_reference, rcpath=rcpath)

    def delete_(self, sdata, dev, **kwargs):
      mapping_dict = kwargs.get('mapping_dict')

      #validating inputs
      if util.isEmpty(mapping_dict.get('name')):
        raise Exception("'name' cannot be empty")

      #convert keys to list
      name = mapping_dict.get('name')
      if not isinstance(name, list):
        name = [name]

      #prepare rcpath
      rcpath = kwargs.get('rcpath')
      rcpath_list = []
      for name_iterator in name:
        rcpath_tmp =  rcpath+"/port-group=%s"%(util.make_interfacename(name_iterator))
        rcpath_list.append(rcpath_tmp)
      payload = ''

      for rcpath in rcpath_list:
        #call the base abstract class for deleteData
        super(self.__class__, self).delete_(sdata, dev, rcpath=rcpath, payload=payload, fail_silently=kwargs.get('fail_silently', False), remove_reference=kwargs.get('remove_reference', False))

    def validate_inputs_form_payload(self, mapping_dict, update=False):
      #validating inputs
      if util.isEmpty(mapping_dict.get('name')):
        raise Exception("'name' cannot be empty")
      if util.isEmpty(mapping_dict.get('vlan_type')):
        raise Exception("'vlan_type' cannot be empty")
      if util.isEmpty(mapping_dict.get('vlan_id')):
        raise Exception("'vlan_id' cannot be empty")
      if util.isEmpty(mapping_dict.get('vlan_name')):
        raise Exception("'vlan_name' cannot be empty")
      if util.isEmpty(mapping_dict.get('mode')):
        raise Exception("'mode' cannot be empty")

      #convert keys to list
      name = mapping_dict.get('name')
      if not isinstance(name, list):
        name = [name]

      #prepare payload
      port_group_object_list = []
      for name_iterator in name:
        from servicemodel.controller.devices.device import port_groups
        port_group_object = port_groups.port_group.port_group()
        port_group_object.name = name_iterator
        try:
          if (update == False) or (update == True and str(mapping_dict.get('vlan_type', None)) != ''):
            port_group_object.vlan_type = mapping_dict.get('vlan_type', None)
          else:
            port_group_object.vlan_type._empty_tag = True
        except TypeError:
          pass
        try:
          if (update == False) or (update == True and str(mapping_dict.get('vlan_id', None)) != ''):
            port_group_object.vlan_id = mapping_dict.get('vlan_id', None)
          else:
            port_group_object.vlan_id._empty_tag = True
        except TypeError:
          pass
        try:
          if (update == False) or (update == True and str(mapping_dict.get('vlan_name', None)) != ''):
            port_group_object.vlan_name = mapping_dict.get('vlan_name', None)
          else:
            port_group_object.vlan_name._empty_tag = True
        except TypeError:
          pass
        try:
          if (update == False) or (update == True and str(mapping_dict.get('port_count', None)) != ''):
            port_group_object.port_count = mapping_dict.get('port_count', None)
          else:
            port_group_object.port_count._empty_tag = True
        except TypeError:
          pass
        try:
          if (update == False) or (update == True and str(mapping_dict.get('mode', None)) != ''):
            port_group_object.mode = mapping_dict.get('mode', None)
          else:
            port_group_object.mode._empty_tag = True
        except TypeError:
          pass
        try:
          if (update == False) or (update == True and str(mapping_dict.get('promiscuous_mode', None)) != ''):
            port_group_object.promiscuous_mode = mapping_dict.get('promiscuous_mode', None)
          else:
            port_group_object.promiscuous_mode._empty_tag = True
        except TypeError:
          pass
        port_group_object_list.append(port_group_object)

      return port_group_object_list

    #XPATH devices/device/port-groups/port-group/assigned-vm
    class assigned_vm(AbstractDeviceMgr):
      key_hints = [['name']]
      def getRcpathPayload(self, sdata, dev, port_group_name, mapping_dict):
        dev = get_valid_devices(dev)
        if len(dev) == 0:
          return
        #Input Key Validations
        self.validate_parent_keys( port_group_name)

        #convert parent keys to list
        rcpath_list = []
        payload_list = []
        if not isinstance(port_group_name, list):
          port_group_name_list = [port_group_name]
        else:
          port_group_name_list = port_group_name

        for port_group_name in port_group_name_list:
          ##prepare rcpath
          rcpath = "l2features:port-groups/port-group=%s"%(util.make_interfacename(port_group_name))
          rcpath_list.append(rcpath)
        mapping_dict = kwargs.get('mapping_dict')

        #validating inputs and get payload object
        assigned_vm_object_list = self.validate_inputs_form_payload(mapping_dict)

        for assigned_vm_object in assigned_vm_object_list:
          #fetch payload
          assigned_vm_payload = assigned_vm_object.getxml(filter=True)
          util.log_debug('assigned_vm_payload %s'%assigned_vm_payload)
          payload_list.append(assigned_vm_payload)

        return rcpath_list, payload_list

      def create(self, sdata, dev, port_group_name, mapping_dict, addref=True, autocommit=True):
        dev = get_valid_devices(dev)
        if len(dev) == 0:
          return
        #Input Key Validations
        self.validate_parent_keys( port_group_name)

        #convert parent keys to list
        if not isinstance(port_group_name, list):
          port_group_name_list = [port_group_name]
        else:
          port_group_name_list = port_group_name

        for port_group_name in port_group_name_list:
          ##prepare rcpath
          rcpath = "l2features:port-groups/port-group=%s"%(util.make_interfacename(port_group_name))
          self.create_(sdata, dev, mapping_dict=mapping_dict, addref=addref, autocommit=autocommit, rcpath=rcpath)

      def create_(self, sdata, dev, **kwargs):
        mapping_dict = kwargs.get('mapping_dict')

        #validating inputs and get payload object
        assigned_vm_object_list = self.validate_inputs_form_payload(mapping_dict)

        for assigned_vm_object in assigned_vm_object_list:
          #fetch payload
          assigned_vm_payload = assigned_vm_object.getxml(filter=True)

          util.log_debug('assigned_vm_payload %s'%assigned_vm_payload)

          #call the base abstract class for createData
          super(self.__class__, self).create_(sdata, dev, rcpath=kwargs.get('rcpath'), payload=assigned_vm_payload, key_hints=self.key_hints, addref=kwargs.get('addref', True), autocommit=kwargs.get('autocommit', True), mapping_dict=mapping_dict)

      def update(self, sdata, dev, port_group_name, mapping_dict, addref=True, autocommit=True):
        dev = get_valid_devices(dev)
        if len(dev) == 0:
          return
        #Input Key Validations
        self.validate_parent_keys( port_group_name)


        ##prepare rcpath
        rcpath = "l2features:port-groups/port-group=%s"%(util.make_interfacename(port_group_name))
        self.update_(sdata, dev, mapping_dict=mapping_dict, addref=addref, autocommit=autocommit, rcpath=rcpath)

      def update_(self, sdata, dev, **kwargs):
        mapping_dict = kwargs.get('mapping_dict')

        #validating inputs and get payload object
        assigned_vm_object_list = self.validate_inputs_form_payload(mapping_dict, update=True)

        #convert keys to list
        vm_name = mapping_dict.get('vm_name')
        if not isinstance(vm_name, list):
          vm_name = [vm_name]
        interface_name = mapping_dict.get('interface_name')
        if not isinstance(interface_name, list):
          interface_name = [interface_name]

        #prepare rcpath
        rcpath = kwargs.get('rcpath')
        rcpath_list = []
        for vm_name_iterator in vm_name:
          for interface_name_iterator in interface_name:
            rcpath_tmp =  rcpath+"/assigned-vm=%s,%s"%(util.make_interfacename(vm_name_iterator),util.make_interfacename(interface_name_iterator))
            rcpath_list.append(rcpath_tmp)
        for rc_counter, assigned_vm_object in enumerate(assigned_vm_object_list):
          #fetch payload
          assigned_vm_payload = assigned_vm_object.getxml(filter=True)

          util.log_debug('update assigned_vm_payload %s'%assigned_vm_payload)

          rcpath = rcpath_list[rc_counter]
          #call the base abstract class for createData
          super(self.__class__, self).update_(sdata, dev, rcpath=rcpath, payload=assigned_vm_payload, key_hints=self.key_hints, addref=kwargs.get('addref', False), autocommit=kwargs.get('autocommit', True), mapping_dict=mapping_dict)

      def delete(self, sdata, dev, port_group_name, mapping_dict, fail_silently=False, remove_reference=False):
        dev = get_valid_devices(dev)
        if len(dev) == 0:
          return
        #Input Key Validations
        self.validate_parent_keys( port_group_name)


        if not isinstance(port_group_name, list):
          port_group_name_list = [port_group_name]
        else:
          port_group_name_list = port_group_name

        for port_group_name in port_group_name_list:
          ##prepare rcpath
          rcpath = "l2features:port-groups/port-group=%s"%(util.make_interfacename(port_group_name))
          self.delete_(sdata, dev, mapping_dict=mapping_dict, fail_silently=fail_silently, remove_reference=remove_reference, rcpath=rcpath)

      def delete_(self, sdata, dev, **kwargs):
        mapping_dict = kwargs.get('mapping_dict')

        #validating inputs
        if util.isEmpty(mapping_dict.get('vm_name')):
          raise Exception("'vm_name' cannot be empty")
        if util.isEmpty(mapping_dict.get('interface_name')):
          raise Exception("'interface_name' cannot be empty")

        #convert keys to list
        vm_name = mapping_dict.get('vm_name')
        if not isinstance(vm_name, list):
          vm_name = [vm_name]
        interface_name = mapping_dict.get('interface_name')
        if not isinstance(interface_name, list):
          interface_name = [interface_name]

        #prepare rcpath
        rcpath = kwargs.get('rcpath')
        rcpath_list = []
        for vm_name_iterator in vm_name:
          for interface_name_iterator in interface_name:
            rcpath_tmp =  rcpath+"/assigned-vm=%s,%s"%(util.make_interfacename(vm_name_iterator),util.make_interfacename(interface_name_iterator))
            rcpath_list.append(rcpath_tmp)
        payload = ''

        for rcpath in rcpath_list:
          #call the base abstract class for deleteData
          super(self.__class__, self).delete_(sdata, dev, rcpath=rcpath, payload=payload, fail_silently=kwargs.get('fail_silently', False), remove_reference=kwargs.get('remove_reference', False))

      def validate_parent_keys(self, port_group_name):
        #Parent Key Validations
        if util.isEmpty(port_group_name):
          raise Exception("'port_group_name' cannot be empty")

      def validate_inputs_form_payload(self, mapping_dict, update=False):
        #validating inputs
        if util.isEmpty(mapping_dict.get('vm_name')):
          raise Exception("'vm_name' cannot be empty")
        if util.isEmpty(mapping_dict.get('interface_name')):
          raise Exception("'interface_name' cannot be empty")

        #convert keys to list
        vm_name = mapping_dict.get('vm_name')
        if not isinstance(vm_name, list):
          vm_name = [vm_name]
        interface_name = mapping_dict.get('interface_name')
        if not isinstance(interface_name, list):
          interface_name = [interface_name]

        #prepare payload
        assigned_vm_object_list = []
        for vm_name_iterator in vm_name:
          for interface_name_iterator in interface_name:
            from servicemodel.controller.devices.device.port_groups import port_group
            assigned_vm_object = port_group.assigned_vm.assigned_vm()
            assigned_vm_object.vm_name = vm_name_iterator
            assigned_vm_object.interface_name = interface_name_iterator
            assigned_vm_object_list.append(assigned_vm_object)

        return assigned_vm_object_list

