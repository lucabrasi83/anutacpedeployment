#
# This computer program is the confidential information and proprietary trade
# secret of Anuta Networks, Inc. Possessions and use of this program must
# conform strictly to the license agreement between the user and
# Anuta Networks, Inc., and receipt or possession does not convey any rights
# to divulge, reproduce, or allow others to use this program without specific
# written authorization of Anuta Networks, Inc.
#
# Copyright (c) 2015-2016 Anuta Networks, Inc. All Rights Reserved.
#

#
#DO NOT EDIT THIS FILE ITS AUTOGENERATED ONE
#

from servicemodel import util
from servicemodel import yang
from servicemodel import devicemgr
from servicemodel.abstract_dev_mgr import AbstractDeviceMgr

def get_valid_devices(devs):
  vdevs = []
  if isinstance(devs, list):
    for dev in devs:
      drivername = dev.device.get_field_value('driver_name')
      if util.isEmpty(drivername):
        vdevs.append(dev)
  else:
    drivername = devs.device.get_field_value('driver_name')
    if util.isEmpty(drivername):
      vdevs.append(devs)

  return vdevs

class applications(object):
  #XPATH devices/device/applications/application
  class application(AbstractDeviceMgr):
    key_hints = [[]]
    def getRcpathPayload(self, sdata, dev, mapping_dict):
      dev = get_valid_devices(dev)
      if len(dev) == 0:
        return
      #convert parent keys to list
      rcpath_list = []
      payload_list = []
      ##prepare rcpath
      rcpath = "firewall:applications"
      rcpath_list.append(rcpath)
      mapping_dict = kwargs.get('mapping_dict')

      #validating inputs and get payload object
      application_object_list = self.validate_inputs_form_payload(mapping_dict)

      for application_object in application_object_list:
        #fetch payload
        application_payload = application_object.getxml(filter=True)
        util.log_debug('application_payload %s'%application_payload)
        payload_list.append(application_payload)

      return rcpath_list, payload_list

    def create(self, sdata, dev, mapping_dict, addref=True, autocommit=True):
      dev = get_valid_devices(dev)
      if len(dev) == 0:
        return
      #convert parent keys to list
      ##prepare rcpath
      rcpath = "firewall:applications"
      self.create_(sdata, dev, mapping_dict=mapping_dict, addref=addref, autocommit=autocommit, rcpath=rcpath)

    def create_(self, sdata, dev, **kwargs):
      mapping_dict = kwargs.get('mapping_dict')

      #validating inputs and get payload object
      application_object_list = self.validate_inputs_form_payload(mapping_dict)

      for application_object in application_object_list:
        #fetch payload
        application_payload = application_object.getxml(filter=True)

        util.log_debug('application_payload %s'%application_payload)

        #call the base abstract class for createData
        super(self.__class__, self).create_(sdata, dev, rcpath=kwargs.get('rcpath'), payload=application_payload, key_hints=self.key_hints, addref=kwargs.get('addref', True), autocommit=kwargs.get('autocommit', True), mapping_dict=mapping_dict)

    def update(self, sdata, dev, mapping_dict, addref=False, autocommit=True):
      dev = get_valid_devices(dev)
      if len(dev) == 0:
        return

      ##prepare rcpath
      rcpath = "firewall:applications"
      self.update_(sdata, dev, mapping_dict=mapping_dict, addref=addref, autocommit=autocommit, rcpath=rcpath)

    def update_(self, sdata, dev, **kwargs):
      mapping_dict = kwargs.get('mapping_dict')

      #validating inputs and get payload object
      application_object_list = self.validate_inputs_form_payload(mapping_dict, update=True)

      #convert keys to list
      name = mapping_dict.get('name')
      if not isinstance(name, list):
        name = [name]

      #prepare rcpath
      rcpath = kwargs.get('rcpath')
      rcpath_list = []
      for name_iterator in name:
        rcpath_tmp =  rcpath+"/application=%s"%(util.make_interfacename(name_iterator))
        rcpath_list.append(rcpath_tmp)
      for rc_counter, application_object in enumerate(application_object_list):
        #fetch payload
        application_payload = application_object.getxml(filter=True)

        util.log_debug('update application_payload %s'%application_payload)

        rcpath = rcpath_list[rc_counter]
        #call the base abstract class for createData
        super(self.__class__, self).update_(sdata, dev, rcpath=rcpath, payload=application_payload, key_hints=self.key_hints, addref=kwargs.get('addref', False), autocommit=kwargs.get('autocommit', True), mapping_dict=mapping_dict)

    def delete(self, sdata, dev, mapping_dict, fail_silently=False, remove_reference=False):
      dev = get_valid_devices(dev)
      if len(dev) == 0:
        return

      ##prepare rcpath
      rcpath = "firewall:applications"
      self.delete_(sdata, dev, mapping_dict=mapping_dict, fail_silently=fail_silently, remove_reference=remove_reference, rcpath=rcpath)

    def delete_(self, sdata, dev, **kwargs):
      mapping_dict = kwargs.get('mapping_dict')

      #validating inputs
      if util.isEmpty(mapping_dict.get('name')):
        raise Exception("'name' cannot be empty")

      #convert keys to list
      name = mapping_dict.get('name')
      if not isinstance(name, list):
        name = [name]

      #prepare rcpath
      rcpath = kwargs.get('rcpath')
      rcpath_list = []
      for name_iterator in name:
        rcpath_tmp =  rcpath+"/application=%s"%(util.make_interfacename(name_iterator))
        rcpath_list.append(rcpath_tmp)
      payload = ''

      for rcpath in rcpath_list:
        #call the base abstract class for deleteData
        super(self.__class__, self).delete_(sdata, dev, rcpath=rcpath, payload=payload, fail_silently=kwargs.get('fail_silently', False), remove_reference=kwargs.get('remove_reference', False))

    def validate_inputs_form_payload(self, mapping_dict, update=False):
      #validating inputs
      if util.isEmpty(mapping_dict.get('name')):
        raise Exception("'name' cannot be empty")

      #convert keys to list
      name = mapping_dict.get('name')
      if not isinstance(name, list):
        name = [name]

      #prepare payload
      application_object_list = []
      for name_iterator in name:
        from servicemodel.controller.devices.device import applications
        application_object = applications.application.application()
        application_object.name = name_iterator
        try:
          if (update == False) or (update == True and str(mapping_dict.get('description', None)) != ''):
            application_object.description = mapping_dict.get('description', None)
          else:
            application_object.description._empty_tag = True
        except TypeError:
          pass
        try:
          if (update == False) or (update == True and str(mapping_dict.get('category', None)) != ''):
            application_object.category = mapping_dict.get('category', None)
          else:
            application_object.category._empty_tag = True
        except TypeError:
          pass
        try:
          if (update == False) or (update == True and str(mapping_dict.get('app_protocol_name', None)) != ''):
            application_object.app_protocol_name = mapping_dict.get('app_protocol_name', None)
          else:
            application_object.app_protocol_name._empty_tag = True
        except TypeError:
          pass
        try:
          if (update == False) or (update == True and str(mapping_dict.get('protocol_name', None)) != ''):
            application_object.protocol_name = mapping_dict.get('protocol_name', None)
          else:
            application_object.protocol_name._empty_tag = True
        except TypeError:
          pass
        try:
          if (update == False) or (update == True and str(mapping_dict.get('protocol_number', None)) != ''):
            application_object.protocol_number = mapping_dict.get('protocol_number', None)
          else:
            application_object.protocol_number._empty_tag = True
        except TypeError:
          pass
        try:
          if (update == False) or (update == True and str(mapping_dict.get('source_port', None)) != ''):
            application_object.source_port = mapping_dict.get('source_port', None)
          else:
            application_object.source_port._empty_tag = True
        except TypeError:
          pass
        try:
          if (update == False) or (update == True and str(mapping_dict.get('dest_port', None)) != ''):
            application_object.dest_port = mapping_dict.get('dest_port', None)
          else:
            application_object.dest_port._empty_tag = True
        except TypeError:
          pass
        try:
          if (update == False) or (update == True and str(mapping_dict.get('icmp_type', None)) != ''):
            application_object.icmp_type = mapping_dict.get('icmp_type', None)
          else:
            application_object.icmp_type._empty_tag = True
        except TypeError:
          pass
        try:
          if (update == False) or (update == True and str(mapping_dict.get('icmp_code', None)) != ''):
            application_object.icmp_code = mapping_dict.get('icmp_code', None)
          else:
            application_object.icmp_code._empty_tag = True
        except TypeError:
          pass
        try:
          if (update == False) or (update == True and str(mapping_dict.get('icmp6_type', None)) != ''):
            application_object.icmp6_type = mapping_dict.get('icmp6_type', None)
          else:
            application_object.icmp6_type._empty_tag = True
        except TypeError:
          pass
        try:
          if (update == False) or (update == True and str(mapping_dict.get('icmp6_code', None)) != ''):
            application_object.icmp6_code = mapping_dict.get('icmp6_code', None)
          else:
            application_object.icmp6_code._empty_tag = True
        except TypeError:
          pass
        try:
          if (update == False) or (update == True and str(mapping_dict.get('device_group', None)) != ''):
            application_object.device_group = mapping_dict.get('device_group', None)
          else:
            application_object.device_group._empty_tag = True
        except TypeError:
          pass
        try:
          if (update == False) or (update == True and str(mapping_dict.get('virtual_device_name', None)) != ''):
            application_object.virtual_device_name = mapping_dict.get('virtual_device_name', None)
          else:
            application_object.virtual_device_name._empty_tag = True
        except TypeError:
          pass
        application_object_list.append(application_object)

      return application_object_list

    #XPATH devices/device/applications/application/application-port-ranges
    class application_port_ranges(AbstractDeviceMgr):
      key_hints = [['name']]
      def getRcpathPayload(self, sdata, dev, application_name, mapping_dict):
        dev = get_valid_devices(dev)
        if len(dev) == 0:
          return
        #Input Key Validations
        self.validate_parent_keys( application_name)

        #convert parent keys to list
        rcpath_list = []
        payload_list = []
        if not isinstance(application_name, list):
          application_name_list = [application_name]
        else:
          application_name_list = application_name

        for application_name in application_name_list:
          ##prepare rcpath
          rcpath = "firewall:applications/application=%s"%(util.make_interfacename(application_name))
          rcpath_list.append(rcpath)
        mapping_dict = kwargs.get('mapping_dict')

        #validating inputs and get payload object
        application_port_ranges_object_list = self.validate_inputs_form_payload(mapping_dict)

        for application_port_ranges_object in application_port_ranges_object_list:
          #fetch payload
          application_port_ranges_payload = application_port_ranges_object.getxml(filter=True)
          util.log_debug('application_port_ranges_payload %s'%application_port_ranges_payload)
          payload_list.append(application_port_ranges_payload)

        return rcpath_list, payload_list

      def create(self, sdata, dev, application_name, mapping_dict, addref=True, autocommit=True):
        dev = get_valid_devices(dev)
        if len(dev) == 0:
          return
        #Input Key Validations
        self.validate_parent_keys( application_name)

        #convert parent keys to list
        if not isinstance(application_name, list):
          application_name_list = [application_name]
        else:
          application_name_list = application_name

        for application_name in application_name_list:
          ##prepare rcpath
          rcpath = "firewall:applications/application=%s"%(util.make_interfacename(application_name))
          self.create_(sdata, dev, mapping_dict=mapping_dict, addref=addref, autocommit=autocommit, rcpath=rcpath)

      def create_(self, sdata, dev, **kwargs):
        mapping_dict = kwargs.get('mapping_dict')

        #validating inputs and get payload object
        application_port_ranges_object_list = self.validate_inputs_form_payload(mapping_dict)

        for application_port_ranges_object in application_port_ranges_object_list:
          #fetch payload
          application_port_ranges_payload = application_port_ranges_object.getxml(filter=True)

          util.log_debug('application_port_ranges_payload %s'%application_port_ranges_payload)

          #call the base abstract class for createData
          super(self.__class__, self).create_(sdata, dev, rcpath=kwargs.get('rcpath'), payload=application_port_ranges_payload, key_hints=self.key_hints, addref=kwargs.get('addref', True), autocommit=kwargs.get('autocommit', True), mapping_dict=mapping_dict)

      def update(self, sdata, dev, application_name, mapping_dict, addref=True, autocommit=True):
        dev = get_valid_devices(dev)
        if len(dev) == 0:
          return
        #Input Key Validations
        self.validate_parent_keys( application_name)


        ##prepare rcpath
        rcpath = "firewall:applications/application=%s"%(util.make_interfacename(application_name))
        self.update_(sdata, dev, mapping_dict=mapping_dict, addref=addref, autocommit=autocommit, rcpath=rcpath)

      def update_(self, sdata, dev, **kwargs):
        mapping_dict = kwargs.get('mapping_dict')

        #validating inputs and get payload object
        application_port_ranges_object_list = self.validate_inputs_form_payload(mapping_dict, update=True)

        #convert keys to list
        protocol_type = mapping_dict.get('protocol_type')
        if not isinstance(protocol_type, list):
          protocol_type = [protocol_type]
        source_port = mapping_dict.get('source_port')
        if not isinstance(source_port, list):
          source_port = [source_port]
        dest_port = mapping_dict.get('dest_port')
        if not isinstance(dest_port, list):
          dest_port = [dest_port]

        #prepare rcpath
        rcpath = kwargs.get('rcpath')
        rcpath_list = []
        for protocol_type_iterator in protocol_type:
          for source_port_iterator in source_port:
            for dest_port_iterator in dest_port:
              rcpath_tmp =  rcpath+"/application-port-ranges=%s,%s,%s"%(util.make_interfacename(protocol_type_iterator),util.make_interfacename(source_port_iterator),util.make_interfacename(dest_port_iterator))
              rcpath_list.append(rcpath_tmp)
        for rc_counter, application_port_ranges_object in enumerate(application_port_ranges_object_list):
          #fetch payload
          application_port_ranges_payload = application_port_ranges_object.getxml(filter=True)

          util.log_debug('update application_port_ranges_payload %s'%application_port_ranges_payload)

          rcpath = rcpath_list[rc_counter]
          #call the base abstract class for createData
          super(self.__class__, self).update_(sdata, dev, rcpath=rcpath, payload=application_port_ranges_payload, key_hints=self.key_hints, addref=kwargs.get('addref', False), autocommit=kwargs.get('autocommit', True), mapping_dict=mapping_dict)

      def delete(self, sdata, dev, application_name, mapping_dict, fail_silently=False, remove_reference=False):
        dev = get_valid_devices(dev)
        if len(dev) == 0:
          return
        #Input Key Validations
        self.validate_parent_keys( application_name)


        if not isinstance(application_name, list):
          application_name_list = [application_name]
        else:
          application_name_list = application_name

        for application_name in application_name_list:
          ##prepare rcpath
          rcpath = "firewall:applications/application=%s"%(util.make_interfacename(application_name))
          self.delete_(sdata, dev, mapping_dict=mapping_dict, fail_silently=fail_silently, remove_reference=remove_reference, rcpath=rcpath)

      def delete_(self, sdata, dev, **kwargs):
        mapping_dict = kwargs.get('mapping_dict')

        #validating inputs
        if util.isEmpty(mapping_dict.get('protocol_type')):
          raise Exception("'protocol_type' cannot be empty")
        if util.isEmpty(mapping_dict.get('source_port')):
          raise Exception("'source_port' cannot be empty")
        if util.isEmpty(mapping_dict.get('dest_port')):
          raise Exception("'dest_port' cannot be empty")

        #convert keys to list
        protocol_type = mapping_dict.get('protocol_type')
        if not isinstance(protocol_type, list):
          protocol_type = [protocol_type]
        source_port = mapping_dict.get('source_port')
        if not isinstance(source_port, list):
          source_port = [source_port]
        dest_port = mapping_dict.get('dest_port')
        if not isinstance(dest_port, list):
          dest_port = [dest_port]

        #prepare rcpath
        rcpath = kwargs.get('rcpath')
        rcpath_list = []
        for protocol_type_iterator in protocol_type:
          for source_port_iterator in source_port:
            for dest_port_iterator in dest_port:
              rcpath_tmp =  rcpath+"/application-port-ranges=%s,%s,%s"%(util.make_interfacename(protocol_type_iterator),util.make_interfacename(source_port_iterator),util.make_interfacename(dest_port_iterator))
              rcpath_list.append(rcpath_tmp)
        payload = ''

        for rcpath in rcpath_list:
          #call the base abstract class for deleteData
          super(self.__class__, self).delete_(sdata, dev, rcpath=rcpath, payload=payload, fail_silently=kwargs.get('fail_silently', False), remove_reference=kwargs.get('remove_reference', False))

      def validate_parent_keys(self, application_name):
        #Parent Key Validations
        if util.isEmpty(application_name):
          raise Exception("'application_name' cannot be empty")

      def validate_inputs_form_payload(self, mapping_dict, update=False):
        #validating inputs
        if util.isEmpty(mapping_dict.get('protocol_type')):
          raise Exception("'protocol_type' cannot be empty")
        if util.isEmpty(mapping_dict.get('source_port')):
          raise Exception("'source_port' cannot be empty")
        if util.isEmpty(mapping_dict.get('dest_port')):
          raise Exception("'dest_port' cannot be empty")

        #convert keys to list
        protocol_type = mapping_dict.get('protocol_type')
        if not isinstance(protocol_type, list):
          protocol_type = [protocol_type]
        source_port = mapping_dict.get('source_port')
        if not isinstance(source_port, list):
          source_port = [source_port]
        dest_port = mapping_dict.get('dest_port')
        if not isinstance(dest_port, list):
          dest_port = [dest_port]

        #prepare payload
        application_port_ranges_object_list = []
        for protocol_type_iterator in protocol_type:
          for source_port_iterator in source_port:
            for dest_port_iterator in dest_port:
              from servicemodel.controller.devices.device.applications import application
              application_port_ranges_object = application.application_port_ranges.application_port_ranges()
              try:
                if (update == False) or (update == True and str(mapping_dict.get('option', None)) != ''):
                  application_port_ranges_object.option = mapping_dict.get('option', None)
                else:
                  application_port_ranges_object.option._empty_tag = True
              except TypeError:
                pass
              application_port_ranges_object.protocol_type = protocol_type_iterator
              application_port_ranges_object.source_port = source_port_iterator
              application_port_ranges_object.dest_port = dest_port_iterator
              application_port_ranges_object_list.append(application_port_ranges_object)

        return application_port_ranges_object_list

