#
# This computer program is the confidential information and proprietary trade
# secret of Anuta Networks, Inc. Possessions and use of this program must
# conform strictly to the license agreement between the user and
# Anuta Networks, Inc., and receipt or possession does not convey any rights
# to divulge, reproduce, or allow others to use this program without specific
# written authorization of Anuta Networks, Inc.
#
# Copyright (c) 2015-2016 Anuta Networks, Inc. All Rights Reserved.
#

#
#DO NOT EDIT THIS FILE ITS AUTOGENERATED ONE
#

from servicemodel import util
from servicemodel import yang
from servicemodel import devicemgr
from servicemodel.abstract_dev_mgr import AbstractDeviceMgr

def get_valid_devices(devs):
  vdevs = []
  if isinstance(devs, list):
    for dev in devs:
      drivername = dev.device.get_field_value('driver_name')
      if util.isEmpty(drivername):
        vdevs.append(dev)
  else:
    drivername = devs.device.get_field_value('driver_name')
    if util.isEmpty(drivername):
      vdevs.append(devs)

  return vdevs

#XPATH devices/device/local-credentials
class local_credentials(AbstractDeviceMgr):
  key_hints = [[]]
  def getRcpathPayload(self, sdata, dev, mapping_dict):
    dev = get_valid_devices(dev)
    if len(dev) == 0:
      return
    #convert parent keys to list
    rcpath_list = []
    payload_list = []
    ##prepare rcpath
    rcpath = ""
    rcpath_list.append(rcpath)
    mapping_dict = kwargs.get('mapping_dict')

    #validating inputs and get payload object
    local_credentials_object_list = self.validate_inputs_form_payload(mapping_dict)

    for local_credentials_object in local_credentials_object_list:
      #fetch payload
      local_credentials_payload = local_credentials_object.getxml(filter=True)
      util.log_debug('local_credentials_payload %s'%local_credentials_payload)
      payload_list.append(local_credentials_payload)

    return rcpath_list, payload_list

  def create(self, sdata, dev, mapping_dict, addref=True, autocommit=True):
    dev = get_valid_devices(dev)
    if len(dev) == 0:
      return
    #convert parent keys to list
    ##prepare rcpath
    rcpath = ""
    self.create_(sdata, dev, mapping_dict=mapping_dict, addref=addref, autocommit=autocommit, rcpath=rcpath)

  def create_(self, sdata, dev, **kwargs):
    mapping_dict = kwargs.get('mapping_dict')

    #validating inputs and get payload object
    local_credentials_object_list = self.validate_inputs_form_payload(mapping_dict)

    for local_credentials_object in local_credentials_object_list:
      #fetch payload
      local_credentials_payload = local_credentials_object.getxml(filter=True)

      util.log_debug('local_credentials_payload %s'%local_credentials_payload)

      #call the base abstract class for createData
      super(self.__class__, self).create_(sdata, dev, rcpath=kwargs.get('rcpath'), payload=local_credentials_payload, key_hints=self.key_hints, addref=kwargs.get('addref', True), autocommit=kwargs.get('autocommit', True), mapping_dict=mapping_dict)

  def update(self, sdata, dev, mapping_dict, addref=False, autocommit=True):
    dev = get_valid_devices(dev)
    if len(dev) == 0:
      return

    ##prepare rcpath
    rcpath = ""
    self.update_(sdata, dev, mapping_dict=mapping_dict, addref=addref, autocommit=autocommit, rcpath=rcpath)

  def update_(self, sdata, dev, **kwargs):
    mapping_dict = kwargs.get('mapping_dict')

    #validating inputs and get payload object
    local_credentials_object_list = self.validate_inputs_form_payload(mapping_dict, update=True)

    #convert keys to list

    #prepare rcpath
    rcpath = kwargs.get('rcpath')
    rcpath_list = []
    rcpath_tmp =  rcpath+"/local-credentials"
    rcpath_list.append(rcpath_tmp)
    for rc_counter, local_credentials_object in enumerate(local_credentials_object_list):
      #fetch payload
      local_credentials_payload = local_credentials_object.getxml(filter=True)

      util.log_debug('update local_credentials_payload %s'%local_credentials_payload)

      rcpath = rcpath_list[rc_counter]
      #call the base abstract class for createData
      super(self.__class__, self).update_(sdata, dev, rcpath=rcpath, payload=local_credentials_payload, key_hints=self.key_hints, addref=kwargs.get('addref', False), autocommit=kwargs.get('autocommit', True), mapping_dict=mapping_dict)

  def delete(self, sdata, dev, mapping_dict, fail_silently=False, remove_reference=False):
    dev = get_valid_devices(dev)
    if len(dev) == 0:
      return

    ##prepare rcpath
    rcpath = ""
    self.delete_(sdata, dev, mapping_dict=mapping_dict, fail_silently=fail_silently, remove_reference=remove_reference, rcpath=rcpath)

  def delete_(self, sdata, dev, **kwargs):
    mapping_dict = kwargs.get('mapping_dict')

    #validating inputs

    #convert keys to list

    #prepare rcpath
    rcpath = kwargs.get('rcpath')
    rcpath_list = []
    rcpath_tmp =  rcpath+"/local-credentials"
    rcpath_list.append(rcpath_tmp)
    payload = ''

    for rcpath in rcpath_list:
      #call the base abstract class for deleteData
      super(self.__class__, self).delete_(sdata, dev, rcpath=rcpath, payload=payload, fail_silently=kwargs.get('fail_silently', False), remove_reference=kwargs.get('remove_reference', False))

  def validate_inputs_form_payload(self, mapping_dict, update=False):
    #validating inputs

    #convert keys to list

    #prepare payload
    local_credentials_object_list = []
    from servicemodel.controller.devices import device
    local_credentials_object = device.local_credentials.local_credentials()
    try:
      if (update == False) or (update == True and str(mapping_dict.get('enable_secret', None)) != ''):
        local_credentials_object.enable_secret = mapping_dict.get('enable_secret', None)
      else:
        local_credentials_object.enable_secret._empty_tag = True
    except TypeError:
      pass
    try:
      if (update == False) or (update == True and str(mapping_dict.get('enable_secret_password', None)) != ''):
        local_credentials_object.enable_secret_password = mapping_dict.get('enable_secret_password', None)
      else:
        local_credentials_object.enable_secret_password._empty_tag = True
    except TypeError:
      pass
    local_credentials_object_list.append(local_credentials_object)

    return local_credentials_object_list

  #XPATH devices/device/local-credentials/local-credential
  class local_credential(AbstractDeviceMgr):
    key_hints = [[]]
    def getRcpathPayload(self, sdata, dev, mapping_dict):
      dev = get_valid_devices(dev)
      if len(dev) == 0:
        return
      #convert parent keys to list
      rcpath_list = []
      payload_list = []
      ##prepare rcpath
      rcpath = "basicDeviceConfigs:local-credentials"
      rcpath_list.append(rcpath)
      mapping_dict = kwargs.get('mapping_dict')

      #validating inputs and get payload object
      local_credential_object_list = self.validate_inputs_form_payload(mapping_dict)

      for local_credential_object in local_credential_object_list:
        #fetch payload
        local_credential_payload = local_credential_object.getxml(filter=True)
        util.log_debug('local_credential_payload %s'%local_credential_payload)
        payload_list.append(local_credential_payload)

      return rcpath_list, payload_list

    def create(self, sdata, dev, mapping_dict, addref=True, autocommit=True):
      dev = get_valid_devices(dev)
      if len(dev) == 0:
        return
      #convert parent keys to list
      ##prepare rcpath
      rcpath = "basicDeviceConfigs:local-credentials"
      self.create_(sdata, dev, mapping_dict=mapping_dict, addref=addref, autocommit=autocommit, rcpath=rcpath)

    def create_(self, sdata, dev, **kwargs):
      mapping_dict = kwargs.get('mapping_dict')

      #validating inputs and get payload object
      local_credential_object_list = self.validate_inputs_form_payload(mapping_dict)

      for local_credential_object in local_credential_object_list:
        #fetch payload
        local_credential_payload = local_credential_object.getxml(filter=True)

        util.log_debug('local_credential_payload %s'%local_credential_payload)

        #call the base abstract class for createData
        super(self.__class__, self).create_(sdata, dev, rcpath=kwargs.get('rcpath'), payload=local_credential_payload, key_hints=self.key_hints, addref=kwargs.get('addref', True), autocommit=kwargs.get('autocommit', True), mapping_dict=mapping_dict)

    def update(self, sdata, dev, mapping_dict, addref=False, autocommit=True):
      dev = get_valid_devices(dev)
      if len(dev) == 0:
        return

      ##prepare rcpath
      rcpath = "basicDeviceConfigs:local-credentials"
      self.update_(sdata, dev, mapping_dict=mapping_dict, addref=addref, autocommit=autocommit, rcpath=rcpath)

    def update_(self, sdata, dev, **kwargs):
      mapping_dict = kwargs.get('mapping_dict')

      #validating inputs and get payload object
      local_credential_object_list = self.validate_inputs_form_payload(mapping_dict, update=True)

      #convert keys to list
      name = mapping_dict.get('name')
      if not isinstance(name, list):
        name = [name]

      #prepare rcpath
      rcpath = kwargs.get('rcpath')
      rcpath_list = []
      for name_iterator in name:
        rcpath_tmp =  rcpath+"/local-credential=%s"%(util.make_interfacename(name_iterator))
        rcpath_list.append(rcpath_tmp)
      for rc_counter, local_credential_object in enumerate(local_credential_object_list):
        #fetch payload
        local_credential_payload = local_credential_object.getxml(filter=True)

        util.log_debug('update local_credential_payload %s'%local_credential_payload)

        rcpath = rcpath_list[rc_counter]
        #call the base abstract class for createData
        super(self.__class__, self).update_(sdata, dev, rcpath=rcpath, payload=local_credential_payload, key_hints=self.key_hints, addref=kwargs.get('addref', False), autocommit=kwargs.get('autocommit', True), mapping_dict=mapping_dict)

    def delete(self, sdata, dev, mapping_dict, fail_silently=False, remove_reference=False):
      dev = get_valid_devices(dev)
      if len(dev) == 0:
        return

      ##prepare rcpath
      rcpath = "basicDeviceConfigs:local-credentials"
      self.delete_(sdata, dev, mapping_dict=mapping_dict, fail_silently=fail_silently, remove_reference=remove_reference, rcpath=rcpath)

    def delete_(self, sdata, dev, **kwargs):
      mapping_dict = kwargs.get('mapping_dict')

      #validating inputs
      if util.isEmpty(mapping_dict.get('name')):
        raise Exception("'name' cannot be empty")

      #convert keys to list
      name = mapping_dict.get('name')
      if not isinstance(name, list):
        name = [name]

      #prepare rcpath
      rcpath = kwargs.get('rcpath')
      rcpath_list = []
      for name_iterator in name:
        rcpath_tmp =  rcpath+"/local-credential=%s"%(util.make_interfacename(name_iterator))
        rcpath_list.append(rcpath_tmp)
      payload = ''

      for rcpath in rcpath_list:
        #call the base abstract class for deleteData
        super(self.__class__, self).delete_(sdata, dev, rcpath=rcpath, payload=payload, fail_silently=kwargs.get('fail_silently', False), remove_reference=kwargs.get('remove_reference', False))

    def validate_inputs_form_payload(self, mapping_dict, update=False):
      #validating inputs
      if util.isEmpty(mapping_dict.get('name')):
        raise Exception("'name' cannot be empty")

      #convert keys to list
      name = mapping_dict.get('name')
      if not isinstance(name, list):
        name = [name]

      #prepare payload
      local_credential_object_list = []
      for name_iterator in name:
        from servicemodel.controller.devices.device import local_credentials
        local_credential_object = local_credentials.local_credential.local_credential()
        local_credential_object.name = name_iterator
        try:
          if (update == False) or (update == True and str(mapping_dict.get('password', None)) != ''):
            local_credential_object.password = mapping_dict.get('password', None)
          else:
            local_credential_object.password._empty_tag = True
        except TypeError:
          pass
        try:
          if (update == False) or (update == True and str(mapping_dict.get('password_level', None)) != ''):
            local_credential_object.password_level = mapping_dict.get('password_level', None)
          else:
            local_credential_object.password_level._empty_tag = True
        except TypeError:
          pass
        try:
          if (update == False) or (update == True and str(mapping_dict.get('role', None)) != ''):
            local_credential_object.role = mapping_dict.get('role', None)
          else:
            local_credential_object.role._empty_tag = True
        except TypeError:
          pass
        try:
          if (update == False) or (update == True and str(mapping_dict.get('strength_check', None)) != ''):
            local_credential_object.strength_check = mapping_dict.get('strength_check', None)
          else:
            local_credential_object.strength_check._empty_tag = True
        except TypeError:
          pass
        try:
          if (update == False) or (update == True and str(mapping_dict.get('enable_password', None)) != ''):
            local_credential_object.enable_password = mapping_dict.get('enable_password', None)
          else:
            local_credential_object.enable_password._empty_tag = True
        except TypeError:
          pass
        try:
          if (update == False) or (update == True and str(mapping_dict.get('enable_pwd', None)) != ''):
            local_credential_object.enable_pwd = mapping_dict.get('enable_pwd', None)
          else:
            local_credential_object.enable_pwd._empty_tag = True
        except TypeError:
          pass
        try:
          if (update == False) or (update == True and str(mapping_dict.get('enable_password_level', None)) != ''):
            local_credential_object.enable_password_level = mapping_dict.get('enable_password_level', None)
          else:
            local_credential_object.enable_password_level._empty_tag = True
        except TypeError:
          pass
        try:
          if (update == False) or (update == True and str(mapping_dict.get('enable_privilege', None)) != ''):
            local_credential_object.enable_privilege = mapping_dict.get('enable_privilege', None)
          else:
            local_credential_object.enable_privilege._empty_tag = True
        except TypeError:
          pass
        try:
          if (update == False) or (update == True and str(mapping_dict.get('privilege_level', None)) != ''):
            local_credential_object.privilege_level = mapping_dict.get('privilege_level', None)
          else:
            local_credential_object.privilege_level._empty_tag = True
        except TypeError:
          pass
        try:
          if (update == False) or (update == True and str(mapping_dict.get('privilege_secret', None)) != ''):
            local_credential_object.privilege_secret = mapping_dict.get('privilege_secret', None)
          else:
            local_credential_object.privilege_secret._empty_tag = True
        except TypeError:
          pass
        try:
          if (update == False) or (update == True and str(mapping_dict.get('enable_secret', None)) != ''):
            local_credential_object.enable_secret = mapping_dict.get('enable_secret', None)
          else:
            local_credential_object.enable_secret._empty_tag = True
        except TypeError:
          pass
        try:
          if (update == False) or (update == True and str(mapping_dict.get('secret_level', None)) != ''):
            local_credential_object.secret_level = mapping_dict.get('secret_level', None)
          else:
            local_credential_object.secret_level._empty_tag = True
        except TypeError:
          pass
        try:
          if (update == False) or (update == True and str(mapping_dict.get('secret_password', None)) != ''):
            local_credential_object.secret_password = mapping_dict.get('secret_password', None)
          else:
            local_credential_object.secret_password._empty_tag = True
        except TypeError:
          pass
        local_credential_object_list.append(local_credential_object)

      return local_credential_object_list

