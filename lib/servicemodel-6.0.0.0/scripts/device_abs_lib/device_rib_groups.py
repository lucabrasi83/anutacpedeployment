#
# This computer program is the confidential information and proprietary trade
# secret of Anuta Networks, Inc. Possessions and use of this program must
# conform strictly to the license agreement between the user and
# Anuta Networks, Inc., and receipt or possession does not convey any rights
# to divulge, reproduce, or allow others to use this program without specific
# written authorization of Anuta Networks, Inc.
#
# Copyright (c) 2015-2016 Anuta Networks, Inc. All Rights Reserved.
#

#
#DO NOT EDIT THIS FILE ITS AUTOGENERATED ONE
#

from servicemodel import util
from servicemodel import yang
from servicemodel import devicemgr
from servicemodel.abstract_dev_mgr import AbstractDeviceMgr

def get_valid_devices(devs):
  vdevs = []
  if isinstance(devs, list):
    for dev in devs:
      drivername = dev.device.get_field_value('driver_name')
      if util.isEmpty(drivername):
        vdevs.append(dev)
  else:
    drivername = devs.device.get_field_value('driver_name')
    if util.isEmpty(drivername):
      vdevs.append(devs)

  return vdevs

class rib_groups(object):
  #XPATH devices/device/rib-groups/rib-group
  class rib_group(AbstractDeviceMgr):
    key_hints = [[]]
    def getRcpathPayload(self, sdata, dev, mapping_dict):
      dev = get_valid_devices(dev)
      if len(dev) == 0:
        return
      #convert parent keys to list
      rcpath_list = []
      payload_list = []
      ##prepare rcpath
      rcpath = "l3features:rib-groups"
      rcpath_list.append(rcpath)
      mapping_dict = kwargs.get('mapping_dict')

      #validating inputs and get payload object
      rib_group_object_list = self.validate_inputs_form_payload(mapping_dict)

      for rib_group_object in rib_group_object_list:
        #fetch payload
        rib_group_payload = rib_group_object.getxml(filter=True)
        util.log_debug('rib_group_payload %s'%rib_group_payload)
        payload_list.append(rib_group_payload)

      return rcpath_list, payload_list

    def create(self, sdata, dev, mapping_dict, addref=True, autocommit=True):
      dev = get_valid_devices(dev)
      if len(dev) == 0:
        return
      #convert parent keys to list
      ##prepare rcpath
      rcpath = "l3features:rib-groups"
      self.create_(sdata, dev, mapping_dict=mapping_dict, addref=addref, autocommit=autocommit, rcpath=rcpath)

    def create_(self, sdata, dev, **kwargs):
      mapping_dict = kwargs.get('mapping_dict')

      #validating inputs and get payload object
      rib_group_object_list = self.validate_inputs_form_payload(mapping_dict)

      for rib_group_object in rib_group_object_list:
        #fetch payload
        rib_group_payload = rib_group_object.getxml(filter=True)

        util.log_debug('rib_group_payload %s'%rib_group_payload)

        #call the base abstract class for createData
        super(self.__class__, self).create_(sdata, dev, rcpath=kwargs.get('rcpath'), payload=rib_group_payload, key_hints=self.key_hints, addref=kwargs.get('addref', True), autocommit=kwargs.get('autocommit', True), mapping_dict=mapping_dict)

    def update(self, sdata, dev, mapping_dict, addref=False, autocommit=True):
      dev = get_valid_devices(dev)
      if len(dev) == 0:
        return

      ##prepare rcpath
      rcpath = "l3features:rib-groups"
      self.update_(sdata, dev, mapping_dict=mapping_dict, addref=addref, autocommit=autocommit, rcpath=rcpath)

    def update_(self, sdata, dev, **kwargs):
      mapping_dict = kwargs.get('mapping_dict')

      #validating inputs and get payload object
      rib_group_object_list = self.validate_inputs_form_payload(mapping_dict, update=True)

      #convert keys to list
      name = mapping_dict.get('name')
      if not isinstance(name, list):
        name = [name]

      #prepare rcpath
      rcpath = kwargs.get('rcpath')
      rcpath_list = []
      for name_iterator in name:
        rcpath_tmp =  rcpath+"/rib-group=%s"%(util.make_interfacename(name_iterator))
        rcpath_list.append(rcpath_tmp)
      for rc_counter, rib_group_object in enumerate(rib_group_object_list):
        #fetch payload
        rib_group_payload = rib_group_object.getxml(filter=True)

        util.log_debug('update rib_group_payload %s'%rib_group_payload)

        rcpath = rcpath_list[rc_counter]
        #call the base abstract class for createData
        super(self.__class__, self).update_(sdata, dev, rcpath=rcpath, payload=rib_group_payload, key_hints=self.key_hints, addref=kwargs.get('addref', False), autocommit=kwargs.get('autocommit', True), mapping_dict=mapping_dict)

    def delete(self, sdata, dev, mapping_dict, fail_silently=False, remove_reference=False):
      dev = get_valid_devices(dev)
      if len(dev) == 0:
        return

      ##prepare rcpath
      rcpath = "l3features:rib-groups"
      self.delete_(sdata, dev, mapping_dict=mapping_dict, fail_silently=fail_silently, remove_reference=remove_reference, rcpath=rcpath)

    def delete_(self, sdata, dev, **kwargs):
      mapping_dict = kwargs.get('mapping_dict')

      #validating inputs
      if util.isEmpty(mapping_dict.get('name')):
        raise Exception("'name' cannot be empty")

      #convert keys to list
      name = mapping_dict.get('name')
      if not isinstance(name, list):
        name = [name]

      #prepare rcpath
      rcpath = kwargs.get('rcpath')
      rcpath_list = []
      for name_iterator in name:
        rcpath_tmp =  rcpath+"/rib-group=%s"%(util.make_interfacename(name_iterator))
        rcpath_list.append(rcpath_tmp)
      payload = ''

      for rcpath in rcpath_list:
        #call the base abstract class for deleteData
        super(self.__class__, self).delete_(sdata, dev, rcpath=rcpath, payload=payload, fail_silently=kwargs.get('fail_silently', False), remove_reference=kwargs.get('remove_reference', False))

    def validate_inputs_form_payload(self, mapping_dict, update=False):
      #validating inputs
      if util.isEmpty(mapping_dict.get('name')):
        raise Exception("'name' cannot be empty")

      #convert keys to list
      name = mapping_dict.get('name')
      if not isinstance(name, list):
        name = [name]

      #prepare payload
      rib_group_object_list = []
      for name_iterator in name:
        from servicemodel.controller.devices.device import rib_groups
        rib_group_object = rib_groups.rib_group.rib_group()
        rib_group_object.name = name_iterator
        rib_group_object_list.append(rib_group_object)

      return rib_group_object_list

    #XPATH devices/device/rib-groups/rib-group/import-rib
    class import_rib(AbstractDeviceMgr):
      key_hints = [['name']]
      def getRcpathPayload(self, sdata, dev, rib_group_name, mapping_dict):
        dev = get_valid_devices(dev)
        if len(dev) == 0:
          return
        #Input Key Validations
        self.validate_parent_keys( rib_group_name)

        #convert parent keys to list
        rcpath_list = []
        payload_list = []
        if not isinstance(rib_group_name, list):
          rib_group_name_list = [rib_group_name]
        else:
          rib_group_name_list = rib_group_name

        for rib_group_name in rib_group_name_list:
          ##prepare rcpath
          rcpath = "l3features:rib-groups/rib-group=%s"%(util.make_interfacename(rib_group_name))
          rcpath_list.append(rcpath)
        mapping_dict = kwargs.get('mapping_dict')

        #validating inputs and get payload object
        import_rib_object_list = self.validate_inputs_form_payload(mapping_dict)

        for import_rib_object in import_rib_object_list:
          #fetch payload
          import_rib_payload = import_rib_object.getxml(filter=True)
          util.log_debug('import_rib_payload %s'%import_rib_payload)
          payload_list.append(import_rib_payload)

        return rcpath_list, payload_list

      def create(self, sdata, dev, rib_group_name, mapping_dict, addref=True, autocommit=True):
        dev = get_valid_devices(dev)
        if len(dev) == 0:
          return
        #Input Key Validations
        self.validate_parent_keys( rib_group_name)

        #convert parent keys to list
        if not isinstance(rib_group_name, list):
          rib_group_name_list = [rib_group_name]
        else:
          rib_group_name_list = rib_group_name

        for rib_group_name in rib_group_name_list:
          ##prepare rcpath
          rcpath = "l3features:rib-groups/rib-group=%s"%(util.make_interfacename(rib_group_name))
          self.create_(sdata, dev, mapping_dict=mapping_dict, addref=addref, autocommit=autocommit, rcpath=rcpath)

      def create_(self, sdata, dev, **kwargs):
        mapping_dict = kwargs.get('mapping_dict')

        #validating inputs and get payload object
        import_rib_object_list = self.validate_inputs_form_payload(mapping_dict)

        for import_rib_object in import_rib_object_list:
          #fetch payload
          import_rib_payload = import_rib_object.getxml(filter=True)

          util.log_debug('import_rib_payload %s'%import_rib_payload)

          #call the base abstract class for createData
          super(self.__class__, self).create_(sdata, dev, rcpath=kwargs.get('rcpath'), payload=import_rib_payload, key_hints=self.key_hints, addref=kwargs.get('addref', True), autocommit=kwargs.get('autocommit', True), mapping_dict=mapping_dict)

      def update(self, sdata, dev, rib_group_name, mapping_dict, addref=True, autocommit=True):
        dev = get_valid_devices(dev)
        if len(dev) == 0:
          return
        #Input Key Validations
        self.validate_parent_keys( rib_group_name)


        ##prepare rcpath
        rcpath = "l3features:rib-groups/rib-group=%s"%(util.make_interfacename(rib_group_name))
        self.update_(sdata, dev, mapping_dict=mapping_dict, addref=addref, autocommit=autocommit, rcpath=rcpath)

      def update_(self, sdata, dev, **kwargs):
        mapping_dict = kwargs.get('mapping_dict')

        #validating inputs and get payload object
        import_rib_object_list = self.validate_inputs_form_payload(mapping_dict, update=True)

        #convert keys to list
        routing_table_name = mapping_dict.get('routing_table_name')
        if not isinstance(routing_table_name, list):
          routing_table_name = [routing_table_name]

        #prepare rcpath
        rcpath = kwargs.get('rcpath')
        rcpath_list = []
        for routing_table_name_iterator in routing_table_name:
          rcpath_tmp =  rcpath+"/import-rib=%s"%(util.make_interfacename(routing_table_name_iterator))
          rcpath_list.append(rcpath_tmp)
        for rc_counter, import_rib_object in enumerate(import_rib_object_list):
          #fetch payload
          import_rib_payload = import_rib_object.getxml(filter=True)

          util.log_debug('update import_rib_payload %s'%import_rib_payload)

          rcpath = rcpath_list[rc_counter]
          #call the base abstract class for createData
          super(self.__class__, self).update_(sdata, dev, rcpath=rcpath, payload=import_rib_payload, key_hints=self.key_hints, addref=kwargs.get('addref', False), autocommit=kwargs.get('autocommit', True), mapping_dict=mapping_dict)

      def delete(self, sdata, dev, rib_group_name, mapping_dict, fail_silently=False, remove_reference=False):
        dev = get_valid_devices(dev)
        if len(dev) == 0:
          return
        #Input Key Validations
        self.validate_parent_keys( rib_group_name)


        if not isinstance(rib_group_name, list):
          rib_group_name_list = [rib_group_name]
        else:
          rib_group_name_list = rib_group_name

        for rib_group_name in rib_group_name_list:
          ##prepare rcpath
          rcpath = "l3features:rib-groups/rib-group=%s"%(util.make_interfacename(rib_group_name))
          self.delete_(sdata, dev, mapping_dict=mapping_dict, fail_silently=fail_silently, remove_reference=remove_reference, rcpath=rcpath)

      def delete_(self, sdata, dev, **kwargs):
        mapping_dict = kwargs.get('mapping_dict')

        #validating inputs
        if util.isEmpty(mapping_dict.get('routing_table_name')):
          raise Exception("'routing_table_name' cannot be empty")

        #convert keys to list
        routing_table_name = mapping_dict.get('routing_table_name')
        if not isinstance(routing_table_name, list):
          routing_table_name = [routing_table_name]

        #prepare rcpath
        rcpath = kwargs.get('rcpath')
        rcpath_list = []
        for routing_table_name_iterator in routing_table_name:
          rcpath_tmp =  rcpath+"/import-rib=%s"%(util.make_interfacename(routing_table_name_iterator))
          rcpath_list.append(rcpath_tmp)
        payload = ''

        for rcpath in rcpath_list:
          #call the base abstract class for deleteData
          super(self.__class__, self).delete_(sdata, dev, rcpath=rcpath, payload=payload, fail_silently=kwargs.get('fail_silently', False), remove_reference=kwargs.get('remove_reference', False))

      def validate_parent_keys(self, rib_group_name):
        #Parent Key Validations
        if util.isEmpty(rib_group_name):
          raise Exception("'rib_group_name' cannot be empty")

      def validate_inputs_form_payload(self, mapping_dict, update=False):
        #validating inputs
        if util.isEmpty(mapping_dict.get('routing_table_name')):
          raise Exception("'routing_table_name' cannot be empty")

        #convert keys to list
        routing_table_name = mapping_dict.get('routing_table_name')
        if not isinstance(routing_table_name, list):
          routing_table_name = [routing_table_name]

        #prepare payload
        import_rib_object_list = []
        for routing_table_name_iterator in routing_table_name:
          from servicemodel.controller.devices.device.rib_groups import rib_group
          import_rib_object = rib_group.import_rib.import_rib()
          import_rib_object.routing_table_name = routing_table_name_iterator
          import_rib_object_list.append(import_rib_object)

        return import_rib_object_list

