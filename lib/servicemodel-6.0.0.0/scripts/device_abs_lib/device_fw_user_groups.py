#
# This computer program is the confidential information and proprietary trade
# secret of Anuta Networks, Inc. Possessions and use of this program must
# conform strictly to the license agreement between the user and
# Anuta Networks, Inc., and receipt or possession does not convey any rights
# to divulge, reproduce, or allow others to use this program without specific
# written authorization of Anuta Networks, Inc.
#
# Copyright (c) 2015-2016 Anuta Networks, Inc. All Rights Reserved.
#

#
#DO NOT EDIT THIS FILE ITS AUTOGENERATED ONE
#

from servicemodel import util
from servicemodel import yang
from servicemodel import devicemgr
from servicemodel.abstract_dev_mgr import AbstractDeviceMgr

def get_valid_devices(devs):
  vdevs = []
  if isinstance(devs, list):
    for dev in devs:
      drivername = dev.device.get_field_value('driver_name')
      if util.isEmpty(drivername):
        vdevs.append(dev)
  else:
    drivername = devs.device.get_field_value('driver_name')
    if util.isEmpty(drivername):
      vdevs.append(devs)

  return vdevs

class fw_user_groups(object):
  #XPATH devices/device/fw-user-groups/fw-user-group
  class fw_user_group(AbstractDeviceMgr):
    key_hints = [[]]
    def getRcpathPayload(self, sdata, dev, mapping_dict):
      dev = get_valid_devices(dev)
      if len(dev) == 0:
        return
      #convert parent keys to list
      rcpath_list = []
      payload_list = []
      ##prepare rcpath
      rcpath = "firewall:fw-user-groups"
      rcpath_list.append(rcpath)
      mapping_dict = kwargs.get('mapping_dict')

      #validating inputs and get payload object
      fw_user_group_object_list = self.validate_inputs_form_payload(mapping_dict)

      for fw_user_group_object in fw_user_group_object_list:
        #fetch payload
        fw_user_group_payload = fw_user_group_object.getxml(filter=True)
        util.log_debug('fw_user_group_payload %s'%fw_user_group_payload)
        payload_list.append(fw_user_group_payload)

      return rcpath_list, payload_list

    def create(self, sdata, dev, mapping_dict, addref=True, autocommit=True):
      dev = get_valid_devices(dev)
      if len(dev) == 0:
        return
      #convert parent keys to list
      ##prepare rcpath
      rcpath = "firewall:fw-user-groups"
      self.create_(sdata, dev, mapping_dict=mapping_dict, addref=addref, autocommit=autocommit, rcpath=rcpath)

    def create_(self, sdata, dev, **kwargs):
      mapping_dict = kwargs.get('mapping_dict')

      #validating inputs and get payload object
      fw_user_group_object_list = self.validate_inputs_form_payload(mapping_dict)

      for fw_user_group_object in fw_user_group_object_list:
        #fetch payload
        fw_user_group_payload = fw_user_group_object.getxml(filter=True)

        util.log_debug('fw_user_group_payload %s'%fw_user_group_payload)

        #call the base abstract class for createData
        super(self.__class__, self).create_(sdata, dev, rcpath=kwargs.get('rcpath'), payload=fw_user_group_payload, key_hints=self.key_hints, addref=kwargs.get('addref', True), autocommit=kwargs.get('autocommit', True), mapping_dict=mapping_dict)

    def update(self, sdata, dev, mapping_dict, addref=False, autocommit=True):
      dev = get_valid_devices(dev)
      if len(dev) == 0:
        return

      ##prepare rcpath
      rcpath = "firewall:fw-user-groups"
      self.update_(sdata, dev, mapping_dict=mapping_dict, addref=addref, autocommit=autocommit, rcpath=rcpath)

    def update_(self, sdata, dev, **kwargs):
      mapping_dict = kwargs.get('mapping_dict')

      #validating inputs and get payload object
      fw_user_group_object_list = self.validate_inputs_form_payload(mapping_dict, update=True)

      #convert keys to list
      name = mapping_dict.get('name')
      if not isinstance(name, list):
        name = [name]

      #prepare rcpath
      rcpath = kwargs.get('rcpath')
      rcpath_list = []
      for name_iterator in name:
        rcpath_tmp =  rcpath+"/fw-user-group=%s"%(util.make_interfacename(name_iterator))
        rcpath_list.append(rcpath_tmp)
      for rc_counter, fw_user_group_object in enumerate(fw_user_group_object_list):
        #fetch payload
        fw_user_group_payload = fw_user_group_object.getxml(filter=True)

        util.log_debug('update fw_user_group_payload %s'%fw_user_group_payload)

        rcpath = rcpath_list[rc_counter]
        #call the base abstract class for createData
        super(self.__class__, self).update_(sdata, dev, rcpath=rcpath, payload=fw_user_group_payload, key_hints=self.key_hints, addref=kwargs.get('addref', False), autocommit=kwargs.get('autocommit', True), mapping_dict=mapping_dict)

    def delete(self, sdata, dev, mapping_dict, fail_silently=False, remove_reference=False):
      dev = get_valid_devices(dev)
      if len(dev) == 0:
        return

      ##prepare rcpath
      rcpath = "firewall:fw-user-groups"
      self.delete_(sdata, dev, mapping_dict=mapping_dict, fail_silently=fail_silently, remove_reference=remove_reference, rcpath=rcpath)

    def delete_(self, sdata, dev, **kwargs):
      mapping_dict = kwargs.get('mapping_dict')

      #validating inputs
      if util.isEmpty(mapping_dict.get('name')):
        raise Exception("'name' cannot be empty")

      #convert keys to list
      name = mapping_dict.get('name')
      if not isinstance(name, list):
        name = [name]

      #prepare rcpath
      rcpath = kwargs.get('rcpath')
      rcpath_list = []
      for name_iterator in name:
        rcpath_tmp =  rcpath+"/fw-user-group=%s"%(util.make_interfacename(name_iterator))
        rcpath_list.append(rcpath_tmp)
      payload = ''

      for rcpath in rcpath_list:
        #call the base abstract class for deleteData
        super(self.__class__, self).delete_(sdata, dev, rcpath=rcpath, payload=payload, fail_silently=kwargs.get('fail_silently', False), remove_reference=kwargs.get('remove_reference', False))

    def validate_inputs_form_payload(self, mapping_dict, update=False):
      #validating inputs
      if util.isEmpty(mapping_dict.get('name')):
        raise Exception("'name' cannot be empty")

      #convert keys to list
      name = mapping_dict.get('name')
      if not isinstance(name, list):
        name = [name]

      #prepare payload
      fw_user_group_object_list = []
      for name_iterator in name:
        from servicemodel.controller.devices.device import fw_user_groups
        fw_user_group_object = fw_user_groups.fw_user_group.fw_user_group()
        fw_user_group_object.name = name_iterator
        try:
          if (update == False) or (update == True and str(mapping_dict.get('virtual_device_name', None)) != ''):
            fw_user_group_object.virtual_device_name = mapping_dict.get('virtual_device_name', None)
          else:
            fw_user_group_object.virtual_device_name._empty_tag = True
        except TypeError:
          pass
        fw_user_group_object_list.append(fw_user_group_object)

      return fw_user_group_object_list

    #XPATH devices/device/fw-user-groups/fw-user-group/fw-user
    class fw_user(AbstractDeviceMgr):
      key_hints = [['name']]
      def getRcpathPayload(self, sdata, dev, fw_user_group_name, mapping_dict):
        dev = get_valid_devices(dev)
        if len(dev) == 0:
          return
        #Input Key Validations
        self.validate_parent_keys( fw_user_group_name)

        #convert parent keys to list
        rcpath_list = []
        payload_list = []
        if not isinstance(fw_user_group_name, list):
          fw_user_group_name_list = [fw_user_group_name]
        else:
          fw_user_group_name_list = fw_user_group_name

        for fw_user_group_name in fw_user_group_name_list:
          ##prepare rcpath
          rcpath = "firewall:fw-user-groups/fw-user-group=%s"%(util.make_interfacename(fw_user_group_name))
          rcpath_list.append(rcpath)
        mapping_dict = kwargs.get('mapping_dict')

        #validating inputs and get payload object
        fw_user_object_list = self.validate_inputs_form_payload(mapping_dict)

        for fw_user_object in fw_user_object_list:
          #fetch payload
          fw_user_payload = fw_user_object.getxml(filter=True)
          util.log_debug('fw_user_payload %s'%fw_user_payload)
          payload_list.append(fw_user_payload)

        return rcpath_list, payload_list

      def create(self, sdata, dev, fw_user_group_name, mapping_dict, addref=True, autocommit=True):
        dev = get_valid_devices(dev)
        if len(dev) == 0:
          return
        #Input Key Validations
        self.validate_parent_keys( fw_user_group_name)

        #convert parent keys to list
        if not isinstance(fw_user_group_name, list):
          fw_user_group_name_list = [fw_user_group_name]
        else:
          fw_user_group_name_list = fw_user_group_name

        for fw_user_group_name in fw_user_group_name_list:
          ##prepare rcpath
          rcpath = "firewall:fw-user-groups/fw-user-group=%s"%(util.make_interfacename(fw_user_group_name))
          self.create_(sdata, dev, mapping_dict=mapping_dict, addref=addref, autocommit=autocommit, rcpath=rcpath)

      def create_(self, sdata, dev, **kwargs):
        mapping_dict = kwargs.get('mapping_dict')

        #validating inputs and get payload object
        fw_user_object_list = self.validate_inputs_form_payload(mapping_dict)

        for fw_user_object in fw_user_object_list:
          #fetch payload
          fw_user_payload = fw_user_object.getxml(filter=True)

          util.log_debug('fw_user_payload %s'%fw_user_payload)

          #call the base abstract class for createData
          super(self.__class__, self).create_(sdata, dev, rcpath=kwargs.get('rcpath'), payload=fw_user_payload, key_hints=self.key_hints, addref=kwargs.get('addref', True), autocommit=kwargs.get('autocommit', True), mapping_dict=mapping_dict)

      def update(self, sdata, dev, fw_user_group_name, mapping_dict, addref=True, autocommit=True):
        dev = get_valid_devices(dev)
        if len(dev) == 0:
          return
        #Input Key Validations
        self.validate_parent_keys( fw_user_group_name)


        ##prepare rcpath
        rcpath = "firewall:fw-user-groups/fw-user-group=%s"%(util.make_interfacename(fw_user_group_name))
        self.update_(sdata, dev, mapping_dict=mapping_dict, addref=addref, autocommit=autocommit, rcpath=rcpath)

      def update_(self, sdata, dev, **kwargs):
        mapping_dict = kwargs.get('mapping_dict')

        #validating inputs and get payload object
        fw_user_object_list = self.validate_inputs_form_payload(mapping_dict, update=True)

        #convert keys to list
        member_name = mapping_dict.get('member_name')
        if not isinstance(member_name, list):
          member_name = [member_name]

        #prepare rcpath
        rcpath = kwargs.get('rcpath')
        rcpath_list = []
        for member_name_iterator in member_name:
          rcpath_tmp =  rcpath+"/fw-user=%s"%(util.make_interfacename(member_name_iterator))
          rcpath_list.append(rcpath_tmp)
        for rc_counter, fw_user_object in enumerate(fw_user_object_list):
          #fetch payload
          fw_user_payload = fw_user_object.getxml(filter=True)

          util.log_debug('update fw_user_payload %s'%fw_user_payload)

          rcpath = rcpath_list[rc_counter]
          #call the base abstract class for createData
          super(self.__class__, self).update_(sdata, dev, rcpath=rcpath, payload=fw_user_payload, key_hints=self.key_hints, addref=kwargs.get('addref', False), autocommit=kwargs.get('autocommit', True), mapping_dict=mapping_dict)

      def delete(self, sdata, dev, fw_user_group_name, mapping_dict, fail_silently=False, remove_reference=False):
        dev = get_valid_devices(dev)
        if len(dev) == 0:
          return
        #Input Key Validations
        self.validate_parent_keys( fw_user_group_name)


        if not isinstance(fw_user_group_name, list):
          fw_user_group_name_list = [fw_user_group_name]
        else:
          fw_user_group_name_list = fw_user_group_name

        for fw_user_group_name in fw_user_group_name_list:
          ##prepare rcpath
          rcpath = "firewall:fw-user-groups/fw-user-group=%s"%(util.make_interfacename(fw_user_group_name))
          self.delete_(sdata, dev, mapping_dict=mapping_dict, fail_silently=fail_silently, remove_reference=remove_reference, rcpath=rcpath)

      def delete_(self, sdata, dev, **kwargs):
        mapping_dict = kwargs.get('mapping_dict')

        #validating inputs
        if util.isEmpty(mapping_dict.get('member_name')):
          raise Exception("'member_name' cannot be empty")

        #convert keys to list
        member_name = mapping_dict.get('member_name')
        if not isinstance(member_name, list):
          member_name = [member_name]

        #prepare rcpath
        rcpath = kwargs.get('rcpath')
        rcpath_list = []
        for member_name_iterator in member_name:
          rcpath_tmp =  rcpath+"/fw-user=%s"%(util.make_interfacename(member_name_iterator))
          rcpath_list.append(rcpath_tmp)
        payload = ''

        for rcpath in rcpath_list:
          #call the base abstract class for deleteData
          super(self.__class__, self).delete_(sdata, dev, rcpath=rcpath, payload=payload, fail_silently=kwargs.get('fail_silently', False), remove_reference=kwargs.get('remove_reference', False))

      def validate_parent_keys(self, fw_user_group_name):
        #Parent Key Validations
        if util.isEmpty(fw_user_group_name):
          raise Exception("'fw_user_group_name' cannot be empty")

      def validate_inputs_form_payload(self, mapping_dict, update=False):
        #validating inputs
        if util.isEmpty(mapping_dict.get('member_name')):
          raise Exception("'member_name' cannot be empty")

        #convert keys to list
        member_name = mapping_dict.get('member_name')
        if not isinstance(member_name, list):
          member_name = [member_name]

        #prepare payload
        fw_user_object_list = []
        for member_name_iterator in member_name:
          from servicemodel.controller.devices.device.fw_user_groups import fw_user_group
          fw_user_object = fw_user_group.fw_user.fw_user()
          fw_user_object.member_name = member_name_iterator
          fw_user_object_list.append(fw_user_object)

        return fw_user_object_list

