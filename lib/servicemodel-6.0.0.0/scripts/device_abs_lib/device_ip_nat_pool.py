#
# This computer program is the confidential information and proprietary trade
# secret of Anuta Networks, Inc. Possessions and use of this program must
# conform strictly to the license agreement between the user and
# Anuta Networks, Inc., and receipt or possession does not convey any rights
# to divulge, reproduce, or allow others to use this program without specific
# written authorization of Anuta Networks, Inc.
#
# Copyright (c) 2015-2016 Anuta Networks, Inc. All Rights Reserved.
#

#
#DO NOT EDIT THIS FILE ITS AUTOGENERATED ONE
#

from servicemodel import util
from servicemodel import yang
from servicemodel import devicemgr
from servicemodel.abstract_dev_mgr import AbstractDeviceMgr

def get_valid_devices(devs):
  vdevs = []
  if isinstance(devs, list):
    for dev in devs:
      drivername = dev.device.get_field_value('driver_name')
      if util.isEmpty(drivername):
        vdevs.append(dev)
  else:
    drivername = devs.device.get_field_value('driver_name')
    if util.isEmpty(drivername):
      vdevs.append(devs)

  return vdevs

class ip_nat_pool(object):
  #XPATH devices/device/ip-nat-pool/pool
  class pool(AbstractDeviceMgr):
    key_hints = [[]]
    def getRcpathPayload(self, sdata, dev, mapping_dict):
      dev = get_valid_devices(dev)
      if len(dev) == 0:
        return
      #convert parent keys to list
      rcpath_list = []
      payload_list = []
      ##prepare rcpath
      rcpath = "l3features:ip-nat-pool"
      rcpath_list.append(rcpath)
      mapping_dict = kwargs.get('mapping_dict')

      #validating inputs and get payload object
      pool_object_list = self.validate_inputs_form_payload(mapping_dict)

      for pool_object in pool_object_list:
        #fetch payload
        pool_payload = pool_object.getxml(filter=True)
        util.log_debug('pool_payload %s'%pool_payload)
        payload_list.append(pool_payload)

      return rcpath_list, payload_list

    def create(self, sdata, dev, mapping_dict, addref=True, autocommit=True):
      dev = get_valid_devices(dev)
      if len(dev) == 0:
        return
      #convert parent keys to list
      ##prepare rcpath
      rcpath = "l3features:ip-nat-pool"
      self.create_(sdata, dev, mapping_dict=mapping_dict, addref=addref, autocommit=autocommit, rcpath=rcpath)

    def create_(self, sdata, dev, **kwargs):
      mapping_dict = kwargs.get('mapping_dict')

      #validating inputs and get payload object
      pool_object_list = self.validate_inputs_form_payload(mapping_dict)

      for pool_object in pool_object_list:
        #fetch payload
        pool_payload = pool_object.getxml(filter=True)

        util.log_debug('pool_payload %s'%pool_payload)

        #call the base abstract class for createData
        super(self.__class__, self).create_(sdata, dev, rcpath=kwargs.get('rcpath'), payload=pool_payload, key_hints=self.key_hints, addref=kwargs.get('addref', True), autocommit=kwargs.get('autocommit', True), mapping_dict=mapping_dict)

    def update(self, sdata, dev, mapping_dict, addref=False, autocommit=True):
      dev = get_valid_devices(dev)
      if len(dev) == 0:
        return

      ##prepare rcpath
      rcpath = "l3features:ip-nat-pool"
      self.update_(sdata, dev, mapping_dict=mapping_dict, addref=addref, autocommit=autocommit, rcpath=rcpath)

    def update_(self, sdata, dev, **kwargs):
      mapping_dict = kwargs.get('mapping_dict')

      #validating inputs and get payload object
      pool_object_list = self.validate_inputs_form_payload(mapping_dict, update=True)

      #convert keys to list
      pool_name = mapping_dict.get('pool_name')
      if not isinstance(pool_name, list):
        pool_name = [pool_name]

      #prepare rcpath
      rcpath = kwargs.get('rcpath')
      rcpath_list = []
      for pool_name_iterator in pool_name:
        rcpath_tmp =  rcpath+"/pool=%s"%(util.make_interfacename(pool_name_iterator))
        rcpath_list.append(rcpath_tmp)
      for rc_counter, pool_object in enumerate(pool_object_list):
        #fetch payload
        pool_payload = pool_object.getxml(filter=True)

        util.log_debug('update pool_payload %s'%pool_payload)

        rcpath = rcpath_list[rc_counter]
        #call the base abstract class for createData
        super(self.__class__, self).update_(sdata, dev, rcpath=rcpath, payload=pool_payload, key_hints=self.key_hints, addref=kwargs.get('addref', False), autocommit=kwargs.get('autocommit', True), mapping_dict=mapping_dict)

    def delete(self, sdata, dev, mapping_dict, fail_silently=False, remove_reference=False):
      dev = get_valid_devices(dev)
      if len(dev) == 0:
        return

      ##prepare rcpath
      rcpath = "l3features:ip-nat-pool"
      self.delete_(sdata, dev, mapping_dict=mapping_dict, fail_silently=fail_silently, remove_reference=remove_reference, rcpath=rcpath)

    def delete_(self, sdata, dev, **kwargs):
      mapping_dict = kwargs.get('mapping_dict')

      #validating inputs
      if util.isEmpty(mapping_dict.get('pool_name')):
        raise Exception("'pool_name' cannot be empty")

      #convert keys to list
      pool_name = mapping_dict.get('pool_name')
      if not isinstance(pool_name, list):
        pool_name = [pool_name]

      #prepare rcpath
      rcpath = kwargs.get('rcpath')
      rcpath_list = []
      for pool_name_iterator in pool_name:
        rcpath_tmp =  rcpath+"/pool=%s"%(util.make_interfacename(pool_name_iterator))
        rcpath_list.append(rcpath_tmp)
      payload = ''

      for rcpath in rcpath_list:
        #call the base abstract class for deleteData
        super(self.__class__, self).delete_(sdata, dev, rcpath=rcpath, payload=payload, fail_silently=kwargs.get('fail_silently', False), remove_reference=kwargs.get('remove_reference', False))

    def validate_inputs_form_payload(self, mapping_dict, update=False):
      #validating inputs
      if util.isEmpty(mapping_dict.get('pool_name')):
        raise Exception("'pool_name' cannot be empty")

      #convert keys to list
      pool_name = mapping_dict.get('pool_name')
      if not isinstance(pool_name, list):
        pool_name = [pool_name]

      #prepare payload
      pool_object_list = []
      for pool_name_iterator in pool_name:
        from servicemodel.controller.devices.device import ip_nat_pool
        pool_object = ip_nat_pool.pool.pool()
        pool_object.pool_name = pool_name_iterator
        try:
          if (update == False) or (update == True and str(mapping_dict.get('start_ip', None)) != ''):
            pool_object.start_ip = mapping_dict.get('start_ip', None)
          else:
            pool_object.start_ip._empty_tag = True
        except TypeError:
          pass
        try:
          if (update == False) or (update == True and str(mapping_dict.get('end_ip', None)) != ''):
            pool_object.end_ip = mapping_dict.get('end_ip', None)
          else:
            pool_object.end_ip._empty_tag = True
        except TypeError:
          pass
        try:
          if (update == False) or (update == True and str(mapping_dict.get('mask', None)) != ''):
            pool_object.mask = mapping_dict.get('mask', None)
          else:
            pool_object.mask._empty_tag = True
        except TypeError:
          pass
        try:
          if (update == False) or (update == True and str(mapping_dict.get('netmask', None)) != ''):
            pool_object.netmask = mapping_dict.get('netmask', None)
          else:
            pool_object.netmask._empty_tag = True
        except TypeError:
          pass
        try:
          if (update == False) or (update == True and str(mapping_dict.get('prefix_length', None)) != ''):
            pool_object.prefix_length = mapping_dict.get('prefix_length', None)
          else:
            pool_object.prefix_length._empty_tag = True
        except TypeError:
          pass
        try:
          if (update == False) or (update == True and str(mapping_dict.get('pool_type', None)) != ''):
            pool_object.pool_type = mapping_dict.get('pool_type', None)
          else:
            pool_object.pool_type._empty_tag = True
        except TypeError:
          pass
        pool_object_list.append(pool_object)

      return pool_object_list

