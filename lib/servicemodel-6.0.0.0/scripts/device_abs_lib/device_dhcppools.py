#
# This computer program is the confidential information and proprietary trade
# secret of Anuta Networks, Inc. Possessions and use of this program must
# conform strictly to the license agreement between the user and
# Anuta Networks, Inc., and receipt or possession does not convey any rights
# to divulge, reproduce, or allow others to use this program without specific
# written authorization of Anuta Networks, Inc.
#
# Copyright (c) 2015-2016 Anuta Networks, Inc. All Rights Reserved.
#

#
#DO NOT EDIT THIS FILE ITS AUTOGENERATED ONE
#

from servicemodel import util
from servicemodel import yang
from servicemodel import devicemgr
from servicemodel.abstract_dev_mgr import AbstractDeviceMgr

def get_valid_devices(devs):
  vdevs = []
  if isinstance(devs, list):
    for dev in devs:
      drivername = dev.device.get_field_value('driver_name')
      if util.isEmpty(drivername):
        vdevs.append(dev)
  else:
    drivername = devs.device.get_field_value('driver_name')
    if util.isEmpty(drivername):
      vdevs.append(devs)

  return vdevs

class dhcppools(object):
  #XPATH devices/device/dhcppools/dhcp-pool
  class dhcp_pool(AbstractDeviceMgr):
    key_hints = [[]]
    def getRcpathPayload(self, sdata, dev, mapping_dict):
      dev = get_valid_devices(dev)
      if len(dev) == 0:
        return
      #convert parent keys to list
      rcpath_list = []
      payload_list = []
      ##prepare rcpath
      rcpath = "firewall:dhcppools"
      rcpath_list.append(rcpath)
      mapping_dict = kwargs.get('mapping_dict')

      #validating inputs and get payload object
      dhcp_pool_object_list = self.validate_inputs_form_payload(mapping_dict)

      for dhcp_pool_object in dhcp_pool_object_list:
        #fetch payload
        dhcp_pool_payload = dhcp_pool_object.getxml(filter=True)
        util.log_debug('dhcp_pool_payload %s'%dhcp_pool_payload)
        payload_list.append(dhcp_pool_payload)

      return rcpath_list, payload_list

    def create(self, sdata, dev, mapping_dict, addref=True, autocommit=True):
      dev = get_valid_devices(dev)
      if len(dev) == 0:
        return
      #convert parent keys to list
      ##prepare rcpath
      rcpath = "firewall:dhcppools"
      self.create_(sdata, dev, mapping_dict=mapping_dict, addref=addref, autocommit=autocommit, rcpath=rcpath)

    def create_(self, sdata, dev, **kwargs):
      mapping_dict = kwargs.get('mapping_dict')

      #validating inputs and get payload object
      dhcp_pool_object_list = self.validate_inputs_form_payload(mapping_dict)

      for dhcp_pool_object in dhcp_pool_object_list:
        #fetch payload
        dhcp_pool_payload = dhcp_pool_object.getxml(filter=True)

        util.log_debug('dhcp_pool_payload %s'%dhcp_pool_payload)

        #call the base abstract class for createData
        super(self.__class__, self).create_(sdata, dev, rcpath=kwargs.get('rcpath'), payload=dhcp_pool_payload, key_hints=self.key_hints, addref=kwargs.get('addref', True), autocommit=kwargs.get('autocommit', True), mapping_dict=mapping_dict)

    def update(self, sdata, dev, mapping_dict, addref=False, autocommit=True):
      dev = get_valid_devices(dev)
      if len(dev) == 0:
        return

      ##prepare rcpath
      rcpath = "firewall:dhcppools"
      self.update_(sdata, dev, mapping_dict=mapping_dict, addref=addref, autocommit=autocommit, rcpath=rcpath)

    def update_(self, sdata, dev, **kwargs):
      mapping_dict = kwargs.get('mapping_dict')

      #validating inputs and get payload object
      dhcp_pool_object_list = self.validate_inputs_form_payload(mapping_dict, update=True)

      #convert keys to list
      name = mapping_dict.get('name')
      if not isinstance(name, list):
        name = [name]

      #prepare rcpath
      rcpath = kwargs.get('rcpath')
      rcpath_list = []
      for name_iterator in name:
        rcpath_tmp =  rcpath+"/dhcp-pool=%s"%(util.make_interfacename(name_iterator))
        rcpath_list.append(rcpath_tmp)
      for rc_counter, dhcp_pool_object in enumerate(dhcp_pool_object_list):
        #fetch payload
        dhcp_pool_payload = dhcp_pool_object.getxml(filter=True)

        util.log_debug('update dhcp_pool_payload %s'%dhcp_pool_payload)

        rcpath = rcpath_list[rc_counter]
        #call the base abstract class for createData
        super(self.__class__, self).update_(sdata, dev, rcpath=rcpath, payload=dhcp_pool_payload, key_hints=self.key_hints, addref=kwargs.get('addref', False), autocommit=kwargs.get('autocommit', True), mapping_dict=mapping_dict)

    def delete(self, sdata, dev, mapping_dict, fail_silently=False, remove_reference=False):
      dev = get_valid_devices(dev)
      if len(dev) == 0:
        return

      ##prepare rcpath
      rcpath = "firewall:dhcppools"
      self.delete_(sdata, dev, mapping_dict=mapping_dict, fail_silently=fail_silently, remove_reference=remove_reference, rcpath=rcpath)

    def delete_(self, sdata, dev, **kwargs):
      mapping_dict = kwargs.get('mapping_dict')

      #validating inputs
      if util.isEmpty(mapping_dict.get('name')):
        raise Exception("'name' cannot be empty")

      #convert keys to list
      name = mapping_dict.get('name')
      if not isinstance(name, list):
        name = [name]

      #prepare rcpath
      rcpath = kwargs.get('rcpath')
      rcpath_list = []
      for name_iterator in name:
        rcpath_tmp =  rcpath+"/dhcp-pool=%s"%(util.make_interfacename(name_iterator))
        rcpath_list.append(rcpath_tmp)
      payload = ''

      for rcpath in rcpath_list:
        #call the base abstract class for deleteData
        super(self.__class__, self).delete_(sdata, dev, rcpath=rcpath, payload=payload, fail_silently=kwargs.get('fail_silently', False), remove_reference=kwargs.get('remove_reference', False))

    def validate_inputs_form_payload(self, mapping_dict, update=False):
      #validating inputs
      if util.isEmpty(mapping_dict.get('name')):
        raise Exception("'name' cannot be empty")
      if util.isEmpty(mapping_dict.get('dhcp_pool_value')):
        raise Exception("'dhcp_pool_value' cannot be empty")
      if util.isEmpty(mapping_dict.get('interface_name')):
        raise Exception("'interface_name' cannot be empty")
      if util.isEmpty(mapping_dict.get('lease_expiry_time')):
        raise Exception("'lease_expiry_time' cannot be empty")
      if util.isEmpty(mapping_dict.get('gateway')):
        raise Exception("'gateway' cannot be empty")

      #convert keys to list
      name = mapping_dict.get('name')
      if not isinstance(name, list):
        name = [name]

      #prepare payload
      dhcp_pool_object_list = []
      for name_iterator in name:
        from servicemodel.controller.devices.device import dhcppools
        dhcp_pool_object = dhcppools.dhcp_pool.dhcp_pool()
        dhcp_pool_object.name = name_iterator
        try:
          if (update == False) or (update == True and str(mapping_dict.get('dhcp_pool_value', None)) != ''):
            dhcp_pool_object.dhcp_pool_value = mapping_dict.get('dhcp_pool_value', None)
          else:
            dhcp_pool_object.dhcp_pool_value._empty_tag = True
        except TypeError:
          pass
        try:
          if (update == False) or (update == True and str(mapping_dict.get('interface_name', None)) != ''):
            dhcp_pool_object.interface_name = mapping_dict.get('interface_name', None)
          else:
            dhcp_pool_object.interface_name._empty_tag = True
        except TypeError:
          pass
        try:
          if (update == False) or (update == True and str(mapping_dict.get('lease_expiry_time', None)) != ''):
            dhcp_pool_object.lease_expiry_time = mapping_dict.get('lease_expiry_time', None)
          else:
            dhcp_pool_object.lease_expiry_time._empty_tag = True
        except TypeError:
          pass
        try:
          if (update == False) or (update == True and str(mapping_dict.get('gateway', None)) != ''):
            dhcp_pool_object.gateway = mapping_dict.get('gateway', None)
          else:
            dhcp_pool_object.gateway._empty_tag = True
        except TypeError:
          pass
        try:
          if (update == False) or (update == True and str(mapping_dict.get('primary_dns', None)) != ''):
            dhcp_pool_object.primary_dns = mapping_dict.get('primary_dns', None)
          else:
            dhcp_pool_object.primary_dns._empty_tag = True
        except TypeError:
          pass
        try:
          if (update == False) or (update == True and str(mapping_dict.get('secondary_dns', None)) != ''):
            dhcp_pool_object.secondary_dns = mapping_dict.get('secondary_dns', None)
          else:
            dhcp_pool_object.secondary_dns._empty_tag = True
        except TypeError:
          pass
        try:
          if (update == False) or (update == True and str(mapping_dict.get('primary_ntp', None)) != ''):
            dhcp_pool_object.primary_ntp = mapping_dict.get('primary_ntp', None)
          else:
            dhcp_pool_object.primary_ntp._empty_tag = True
        except TypeError:
          pass
        try:
          if (update == False) or (update == True and str(mapping_dict.get('secondary_ntp', None)) != ''):
            dhcp_pool_object.secondary_ntp = mapping_dict.get('secondary_ntp', None)
          else:
            dhcp_pool_object.secondary_ntp._empty_tag = True
        except TypeError:
          pass
        try:
          if (update == False) or (update == True and str(mapping_dict.get('probe_ip', None)) != ''):
            dhcp_pool_object.probe_ip = mapping_dict.get('probe_ip', None)
          else:
            dhcp_pool_object.probe_ip._empty_tag = True
        except TypeError:
          pass
        dhcp_pool_object_list.append(dhcp_pool_object)

      return dhcp_pool_object_list

