#
# This computer program is the confidential information and proprietary trade
# secret of Anuta Networks, Inc. Possessions and use of this program must
# conform strictly to the license agreement between the user and
# Anuta Networks, Inc., and receipt or possession does not convey any rights
# to divulge, reproduce, or allow others to use this program without specific
# written authorization of Anuta Networks, Inc.
#
# Copyright (c) 2015-2016 Anuta Networks, Inc. All Rights Reserved.
#

#
#DO NOT EDIT THIS FILE ITS AUTOGENERATED ONE
#

from servicemodel import util
from servicemodel import yang
from servicemodel import devicemgr
from servicemodel.abstract_dev_mgr import AbstractDeviceMgr

def get_valid_devices(devs):
  vdevs = []
  if isinstance(devs, list):
    for dev in devs:
      drivername = dev.device.get_field_value('driver_name')
      if util.isEmpty(drivername):
        vdevs.append(dev)
  else:
    drivername = devs.device.get_field_value('driver_name')
    if util.isEmpty(drivername):
      vdevs.append(devs)

  return vdevs

class policers(object):
  #XPATH devices/device/policers/policer
  class policer(AbstractDeviceMgr):
    key_hints = [[]]
    def getRcpathPayload(self, sdata, dev, mapping_dict):
      dev = get_valid_devices(dev)
      if len(dev) == 0:
        return
      #convert parent keys to list
      rcpath_list = []
      payload_list = []
      ##prepare rcpath
      rcpath = "firewall:policers"
      rcpath_list.append(rcpath)
      mapping_dict = kwargs.get('mapping_dict')

      #validating inputs and get payload object
      policer_object_list = self.validate_inputs_form_payload(mapping_dict)

      for policer_object in policer_object_list:
        #fetch payload
        policer_payload = policer_object.getxml(filter=True)
        util.log_debug('policer_payload %s'%policer_payload)
        payload_list.append(policer_payload)

      return rcpath_list, payload_list

    def create(self, sdata, dev, mapping_dict, addref=True, autocommit=True):
      dev = get_valid_devices(dev)
      if len(dev) == 0:
        return
      #convert parent keys to list
      ##prepare rcpath
      rcpath = "firewall:policers"
      self.create_(sdata, dev, mapping_dict=mapping_dict, addref=addref, autocommit=autocommit, rcpath=rcpath)

    def create_(self, sdata, dev, **kwargs):
      mapping_dict = kwargs.get('mapping_dict')

      #validating inputs and get payload object
      policer_object_list = self.validate_inputs_form_payload(mapping_dict)

      for policer_object in policer_object_list:
        #fetch payload
        policer_payload = policer_object.getxml(filter=True)

        util.log_debug('policer_payload %s'%policer_payload)

        #call the base abstract class for createData
        super(self.__class__, self).create_(sdata, dev, rcpath=kwargs.get('rcpath'), payload=policer_payload, key_hints=self.key_hints, addref=kwargs.get('addref', True), autocommit=kwargs.get('autocommit', True), mapping_dict=mapping_dict)

    def update(self, sdata, dev, mapping_dict, addref=False, autocommit=True):
      dev = get_valid_devices(dev)
      if len(dev) == 0:
        return

      ##prepare rcpath
      rcpath = "firewall:policers"
      self.update_(sdata, dev, mapping_dict=mapping_dict, addref=addref, autocommit=autocommit, rcpath=rcpath)

    def update_(self, sdata, dev, **kwargs):
      mapping_dict = kwargs.get('mapping_dict')

      #validating inputs and get payload object
      policer_object_list = self.validate_inputs_form_payload(mapping_dict, update=True)

      #convert keys to list
      policer_name = mapping_dict.get('policer_name')
      if not isinstance(policer_name, list):
        policer_name = [policer_name]

      #prepare rcpath
      rcpath = kwargs.get('rcpath')
      rcpath_list = []
      for policer_name_iterator in policer_name:
        rcpath_tmp =  rcpath+"/policer=%s"%(util.make_interfacename(policer_name_iterator))
        rcpath_list.append(rcpath_tmp)
      for rc_counter, policer_object in enumerate(policer_object_list):
        #fetch payload
        policer_payload = policer_object.getxml(filter=True)

        util.log_debug('update policer_payload %s'%policer_payload)

        rcpath = rcpath_list[rc_counter]
        #call the base abstract class for createData
        super(self.__class__, self).update_(sdata, dev, rcpath=rcpath, payload=policer_payload, key_hints=self.key_hints, addref=kwargs.get('addref', False), autocommit=kwargs.get('autocommit', True), mapping_dict=mapping_dict)

    def delete(self, sdata, dev, mapping_dict, fail_silently=False, remove_reference=False):
      dev = get_valid_devices(dev)
      if len(dev) == 0:
        return

      ##prepare rcpath
      rcpath = "firewall:policers"
      self.delete_(sdata, dev, mapping_dict=mapping_dict, fail_silently=fail_silently, remove_reference=remove_reference, rcpath=rcpath)

    def delete_(self, sdata, dev, **kwargs):
      mapping_dict = kwargs.get('mapping_dict')

      #validating inputs
      if util.isEmpty(mapping_dict.get('policer_name')):
        raise Exception("'policer_name' cannot be empty")

      #convert keys to list
      policer_name = mapping_dict.get('policer_name')
      if not isinstance(policer_name, list):
        policer_name = [policer_name]

      #prepare rcpath
      rcpath = kwargs.get('rcpath')
      rcpath_list = []
      for policer_name_iterator in policer_name:
        rcpath_tmp =  rcpath+"/policer=%s"%(util.make_interfacename(policer_name_iterator))
        rcpath_list.append(rcpath_tmp)
      payload = ''

      for rcpath in rcpath_list:
        #call the base abstract class for deleteData
        super(self.__class__, self).delete_(sdata, dev, rcpath=rcpath, payload=payload, fail_silently=kwargs.get('fail_silently', False), remove_reference=kwargs.get('remove_reference', False))

    def validate_inputs_form_payload(self, mapping_dict, update=False):
      #validating inputs
      if util.isEmpty(mapping_dict.get('policer_name')):
        raise Exception("'policer_name' cannot be empty")

      #convert keys to list
      policer_name = mapping_dict.get('policer_name')
      if not isinstance(policer_name, list):
        policer_name = [policer_name]

      #prepare payload
      policer_object_list = []
      for policer_name_iterator in policer_name:
        from servicemodel.controller.devices.device import policers
        policer_object = policers.policer.policer()
        policer_object.policer_name = policer_name_iterator
        policer_object_list.append(policer_object)

      return policer_object_list

    #XPATH devices/device/policers/policer/if-exceeding
    class if_exceeding(AbstractDeviceMgr):
      key_hints = [['policer_name']]
      def getRcpathPayload(self, sdata, dev, policer_policer_name, mapping_dict):
        dev = get_valid_devices(dev)
        if len(dev) == 0:
          return
        #Input Key Validations
        self.validate_parent_keys( policer_policer_name)

        #convert parent keys to list
        rcpath_list = []
        payload_list = []
        if not isinstance(policer_policer_name, list):
          policer_policer_name_list = [policer_policer_name]
        else:
          policer_policer_name_list = policer_policer_name

        for policer_policer_name in policer_policer_name_list:
          ##prepare rcpath
          rcpath = "firewall:policers/policer=%s"%(util.make_interfacename(policer_policer_name))
          rcpath_list.append(rcpath)
        mapping_dict = kwargs.get('mapping_dict')

        #validating inputs and get payload object
        if_exceeding_object_list = self.validate_inputs_form_payload(mapping_dict)

        for if_exceeding_object in if_exceeding_object_list:
          #fetch payload
          if_exceeding_payload = if_exceeding_object.getxml(filter=True)
          util.log_debug('if_exceeding_payload %s'%if_exceeding_payload)
          payload_list.append(if_exceeding_payload)

        return rcpath_list, payload_list

      def create(self, sdata, dev, policer_policer_name, mapping_dict, addref=True, autocommit=True):
        dev = get_valid_devices(dev)
        if len(dev) == 0:
          return
        #Input Key Validations
        self.validate_parent_keys( policer_policer_name)

        #convert parent keys to list
        if not isinstance(policer_policer_name, list):
          policer_policer_name_list = [policer_policer_name]
        else:
          policer_policer_name_list = policer_policer_name

        for policer_policer_name in policer_policer_name_list:
          ##prepare rcpath
          rcpath = "firewall:policers/policer=%s"%(util.make_interfacename(policer_policer_name))
          self.create_(sdata, dev, mapping_dict=mapping_dict, addref=addref, autocommit=autocommit, rcpath=rcpath)

      def create_(self, sdata, dev, **kwargs):
        mapping_dict = kwargs.get('mapping_dict')

        #validating inputs and get payload object
        if_exceeding_object_list = self.validate_inputs_form_payload(mapping_dict)

        for if_exceeding_object in if_exceeding_object_list:
          #fetch payload
          if_exceeding_payload = if_exceeding_object.getxml(filter=True)

          util.log_debug('if_exceeding_payload %s'%if_exceeding_payload)

          #call the base abstract class for createData
          super(self.__class__, self).create_(sdata, dev, rcpath=kwargs.get('rcpath'), payload=if_exceeding_payload, key_hints=self.key_hints, addref=kwargs.get('addref', True), autocommit=kwargs.get('autocommit', True), mapping_dict=mapping_dict)

      def update(self, sdata, dev, policer_policer_name, mapping_dict, addref=True, autocommit=True):
        dev = get_valid_devices(dev)
        if len(dev) == 0:
          return
        #Input Key Validations
        self.validate_parent_keys( policer_policer_name)


        ##prepare rcpath
        rcpath = "firewall:policers/policer=%s"%(util.make_interfacename(policer_policer_name))
        self.update_(sdata, dev, mapping_dict=mapping_dict, addref=addref, autocommit=autocommit, rcpath=rcpath)

      def update_(self, sdata, dev, **kwargs):
        mapping_dict = kwargs.get('mapping_dict')

        #validating inputs and get payload object
        if_exceeding_object_list = self.validate_inputs_form_payload(mapping_dict, update=True)

        #convert keys to list

        #prepare rcpath
        rcpath = kwargs.get('rcpath')
        rcpath_list = []
        rcpath_tmp =  rcpath+"/if-exceeding"
        rcpath_list.append(rcpath_tmp)
        for rc_counter, if_exceeding_object in enumerate(if_exceeding_object_list):
          #fetch payload
          if_exceeding_payload = if_exceeding_object.getxml(filter=True)

          util.log_debug('update if_exceeding_payload %s'%if_exceeding_payload)

          rcpath = rcpath_list[rc_counter]
          #call the base abstract class for createData
          super(self.__class__, self).update_(sdata, dev, rcpath=rcpath, payload=if_exceeding_payload, key_hints=self.key_hints, addref=kwargs.get('addref', False), autocommit=kwargs.get('autocommit', True), mapping_dict=mapping_dict)

      def delete(self, sdata, dev, policer_policer_name, mapping_dict, fail_silently=False, remove_reference=False):
        dev = get_valid_devices(dev)
        if len(dev) == 0:
          return
        #Input Key Validations
        self.validate_parent_keys( policer_policer_name)


        if not isinstance(policer_policer_name, list):
          policer_policer_name_list = [policer_policer_name]
        else:
          policer_policer_name_list = policer_policer_name

        for policer_policer_name in policer_policer_name_list:
          ##prepare rcpath
          rcpath = "firewall:policers/policer=%s"%(util.make_interfacename(policer_policer_name))
          self.delete_(sdata, dev, mapping_dict=mapping_dict, fail_silently=fail_silently, remove_reference=remove_reference, rcpath=rcpath)

      def delete_(self, sdata, dev, **kwargs):
        mapping_dict = kwargs.get('mapping_dict')

        #validating inputs

        #convert keys to list

        #prepare rcpath
        rcpath = kwargs.get('rcpath')
        rcpath_list = []
        rcpath_tmp =  rcpath+"/if-exceeding"
        rcpath_list.append(rcpath_tmp)
        payload = ''

        for rcpath in rcpath_list:
          #call the base abstract class for deleteData
          super(self.__class__, self).delete_(sdata, dev, rcpath=rcpath, payload=payload, fail_silently=kwargs.get('fail_silently', False), remove_reference=kwargs.get('remove_reference', False))

      def validate_parent_keys(self, policer_policer_name):
        #Parent Key Validations
        if util.isEmpty(policer_policer_name):
          raise Exception("'policer_policer_name' cannot be empty")

      def validate_inputs_form_payload(self, mapping_dict, update=False):
        #validating inputs

        #convert keys to list

        #prepare payload
        if_exceeding_object_list = []
        from servicemodel.controller.devices.device.policers import policer
        if_exceeding_object = policer.if_exceeding.if_exceeding()
        try:
          if (update == False) or (update == True and str(mapping_dict.get('band_width_limit', None)) != ''):
            if_exceeding_object.band_width_limit = mapping_dict.get('band_width_limit', None)
          else:
            if_exceeding_object.band_width_limit._empty_tag = True
        except TypeError:
          pass
        try:
          if (update == False) or (update == True and str(mapping_dict.get('burst_size_limit', None)) != ''):
            if_exceeding_object.burst_size_limit = mapping_dict.get('burst_size_limit', None)
          else:
            if_exceeding_object.burst_size_limit._empty_tag = True
        except TypeError:
          pass
        try:
          if (update == False) or (update == True and str(mapping_dict.get('bandwidth_percent', None)) != ''):
            if_exceeding_object.bandwidth_percent = mapping_dict.get('bandwidth_percent', None)
          else:
            if_exceeding_object.bandwidth_percent._empty_tag = True
        except TypeError:
          pass
        if_exceeding_object_list.append(if_exceeding_object)

        return if_exceeding_object_list

    #XPATH devices/device/policers/policer/then
    class then(AbstractDeviceMgr):
      key_hints = [['policer_name']]
      def getRcpathPayload(self, sdata, dev, policer_policer_name, mapping_dict):
        dev = get_valid_devices(dev)
        if len(dev) == 0:
          return
        #Input Key Validations
        self.validate_parent_keys( policer_policer_name)

        #convert parent keys to list
        rcpath_list = []
        payload_list = []
        if not isinstance(policer_policer_name, list):
          policer_policer_name_list = [policer_policer_name]
        else:
          policer_policer_name_list = policer_policer_name

        for policer_policer_name in policer_policer_name_list:
          ##prepare rcpath
          rcpath = "firewall:policers/policer=%s"%(util.make_interfacename(policer_policer_name))
          rcpath_list.append(rcpath)
        mapping_dict = kwargs.get('mapping_dict')

        #validating inputs and get payload object
        then_object_list = self.validate_inputs_form_payload(mapping_dict)

        for then_object in then_object_list:
          #fetch payload
          then_payload = then_object.getxml(filter=True)
          util.log_debug('then_payload %s'%then_payload)
          payload_list.append(then_payload)

        return rcpath_list, payload_list

      def create(self, sdata, dev, policer_policer_name, mapping_dict, addref=True, autocommit=True):
        dev = get_valid_devices(dev)
        if len(dev) == 0:
          return
        #Input Key Validations
        self.validate_parent_keys( policer_policer_name)

        #convert parent keys to list
        if not isinstance(policer_policer_name, list):
          policer_policer_name_list = [policer_policer_name]
        else:
          policer_policer_name_list = policer_policer_name

        for policer_policer_name in policer_policer_name_list:
          ##prepare rcpath
          rcpath = "firewall:policers/policer=%s"%(util.make_interfacename(policer_policer_name))
          self.create_(sdata, dev, mapping_dict=mapping_dict, addref=addref, autocommit=autocommit, rcpath=rcpath)

      def create_(self, sdata, dev, **kwargs):
        mapping_dict = kwargs.get('mapping_dict')

        #validating inputs and get payload object
        then_object_list = self.validate_inputs_form_payload(mapping_dict)

        for then_object in then_object_list:
          #fetch payload
          then_payload = then_object.getxml(filter=True)

          util.log_debug('then_payload %s'%then_payload)

          #call the base abstract class for createData
          super(self.__class__, self).create_(sdata, dev, rcpath=kwargs.get('rcpath'), payload=then_payload, key_hints=self.key_hints, addref=kwargs.get('addref', True), autocommit=kwargs.get('autocommit', True), mapping_dict=mapping_dict)

      def update(self, sdata, dev, policer_policer_name, mapping_dict, addref=True, autocommit=True):
        dev = get_valid_devices(dev)
        if len(dev) == 0:
          return
        #Input Key Validations
        self.validate_parent_keys( policer_policer_name)


        ##prepare rcpath
        rcpath = "firewall:policers/policer=%s"%(util.make_interfacename(policer_policer_name))
        self.update_(sdata, dev, mapping_dict=mapping_dict, addref=addref, autocommit=autocommit, rcpath=rcpath)

      def update_(self, sdata, dev, **kwargs):
        mapping_dict = kwargs.get('mapping_dict')

        #validating inputs and get payload object
        then_object_list = self.validate_inputs_form_payload(mapping_dict, update=True)

        #convert keys to list

        #prepare rcpath
        rcpath = kwargs.get('rcpath')
        rcpath_list = []
        rcpath_tmp =  rcpath+"/then"
        rcpath_list.append(rcpath_tmp)
        for rc_counter, then_object in enumerate(then_object_list):
          #fetch payload
          then_payload = then_object.getxml(filter=True)

          util.log_debug('update then_payload %s'%then_payload)

          rcpath = rcpath_list[rc_counter]
          #call the base abstract class for createData
          super(self.__class__, self).update_(sdata, dev, rcpath=rcpath, payload=then_payload, key_hints=self.key_hints, addref=kwargs.get('addref', False), autocommit=kwargs.get('autocommit', True), mapping_dict=mapping_dict)

      def delete(self, sdata, dev, policer_policer_name, mapping_dict, fail_silently=False, remove_reference=False):
        dev = get_valid_devices(dev)
        if len(dev) == 0:
          return
        #Input Key Validations
        self.validate_parent_keys( policer_policer_name)


        if not isinstance(policer_policer_name, list):
          policer_policer_name_list = [policer_policer_name]
        else:
          policer_policer_name_list = policer_policer_name

        for policer_policer_name in policer_policer_name_list:
          ##prepare rcpath
          rcpath = "firewall:policers/policer=%s"%(util.make_interfacename(policer_policer_name))
          self.delete_(sdata, dev, mapping_dict=mapping_dict, fail_silently=fail_silently, remove_reference=remove_reference, rcpath=rcpath)

      def delete_(self, sdata, dev, **kwargs):
        mapping_dict = kwargs.get('mapping_dict')

        #validating inputs

        #convert keys to list

        #prepare rcpath
        rcpath = kwargs.get('rcpath')
        rcpath_list = []
        rcpath_tmp =  rcpath+"/then"
        rcpath_list.append(rcpath_tmp)
        payload = ''

        for rcpath in rcpath_list:
          #call the base abstract class for deleteData
          super(self.__class__, self).delete_(sdata, dev, rcpath=rcpath, payload=payload, fail_silently=kwargs.get('fail_silently', False), remove_reference=kwargs.get('remove_reference', False))

      def validate_parent_keys(self, policer_policer_name):
        #Parent Key Validations
        if util.isEmpty(policer_policer_name):
          raise Exception("'policer_policer_name' cannot be empty")

      def validate_inputs_form_payload(self, mapping_dict, update=False):
        #validating inputs

        #convert keys to list

        #prepare payload
        then_object_list = []
        from servicemodel.controller.devices.device.policers import policer
        then_object = policer.then.then()
        try:
          if (update == False) or (update == True and str(mapping_dict.get('discard', None)) != ''):
            then_object.discard = mapping_dict.get('discard', None)
          else:
            then_object.discard._empty_tag = True
        except TypeError:
          pass
        then_object_list.append(then_object)

        return then_object_list

