#
# This computer program is the confidential information and proprietary trade
# secret of Anuta Networks, Inc. Possessions and use of this program must
# conform strictly to the license agreement between the user and
# Anuta Networks, Inc., and receipt or possession does not convey any rights
# to divulge, reproduce, or allow others to use this program without specific
# written authorization of Anuta Networks, Inc.
#
# Copyright (c) 2015-2016 Anuta Networks, Inc. All Rights Reserved.
#

#
#DO NOT EDIT THIS FILE ITS AUTOGENERATED ONE
#

from servicemodel import util
from servicemodel import yang
from servicemodel import devicemgr
from servicemodel.abstract_dev_mgr import AbstractDeviceMgr

def get_valid_devices(devs):
  vdevs = []
  if isinstance(devs, list):
    for dev in devs:
      drivername = dev.device.get_field_value('driver_name')
      if util.isEmpty(drivername):
        vdevs.append(dev)
  else:
    drivername = devs.device.get_field_value('driver_name')
    if util.isEmpty(drivername):
      vdevs.append(devs)

  return vdevs

class pfr_policy_maps(object):
  #XPATH devices/device/pfr-policy-maps/pfr-policy-map
  class pfr_policy_map(AbstractDeviceMgr):
    key_hints = [[]]
    def getRcpathPayload(self, sdata, dev, mapping_dict):
      dev = get_valid_devices(dev)
      if len(dev) == 0:
        return
      #convert parent keys to list
      rcpath_list = []
      payload_list = []
      ##prepare rcpath
      rcpath = "qos:pfr-policy-maps"
      rcpath_list.append(rcpath)
      mapping_dict = kwargs.get('mapping_dict')

      #validating inputs and get payload object
      pfr_policy_map_object_list = self.validate_inputs_form_payload(mapping_dict)

      for pfr_policy_map_object in pfr_policy_map_object_list:
        #fetch payload
        pfr_policy_map_payload = pfr_policy_map_object.getxml(filter=True)
        util.log_debug('pfr_policy_map_payload %s'%pfr_policy_map_payload)
        payload_list.append(pfr_policy_map_payload)

      return rcpath_list, payload_list

    def create(self, sdata, dev, mapping_dict, addref=True, autocommit=True):
      dev = get_valid_devices(dev)
      if len(dev) == 0:
        return
      #convert parent keys to list
      ##prepare rcpath
      rcpath = "qos:pfr-policy-maps"
      self.create_(sdata, dev, mapping_dict=mapping_dict, addref=addref, autocommit=autocommit, rcpath=rcpath)

    def create_(self, sdata, dev, **kwargs):
      mapping_dict = kwargs.get('mapping_dict')

      #validating inputs and get payload object
      pfr_policy_map_object_list = self.validate_inputs_form_payload(mapping_dict)

      for pfr_policy_map_object in pfr_policy_map_object_list:
        #fetch payload
        pfr_policy_map_payload = pfr_policy_map_object.getxml(filter=True)

        util.log_debug('pfr_policy_map_payload %s'%pfr_policy_map_payload)

        #call the base abstract class for createData
        super(self.__class__, self).create_(sdata, dev, rcpath=kwargs.get('rcpath'), payload=pfr_policy_map_payload, key_hints=self.key_hints, addref=kwargs.get('addref', True), autocommit=kwargs.get('autocommit', True), mapping_dict=mapping_dict)

    def update(self, sdata, dev, mapping_dict, addref=False, autocommit=True):
      dev = get_valid_devices(dev)
      if len(dev) == 0:
        return

      ##prepare rcpath
      rcpath = "qos:pfr-policy-maps"
      self.update_(sdata, dev, mapping_dict=mapping_dict, addref=addref, autocommit=autocommit, rcpath=rcpath)

    def update_(self, sdata, dev, **kwargs):
      mapping_dict = kwargs.get('mapping_dict')

      #validating inputs and get payload object
      pfr_policy_map_object_list = self.validate_inputs_form_payload(mapping_dict, update=True)

      #convert keys to list
      name = mapping_dict.get('name')
      if not isinstance(name, list):
        name = [name]

      #prepare rcpath
      rcpath = kwargs.get('rcpath')
      rcpath_list = []
      for name_iterator in name:
        rcpath_tmp =  rcpath+"/pfr-policy-map=%s"%(util.make_interfacename(name_iterator))
        rcpath_list.append(rcpath_tmp)
      for rc_counter, pfr_policy_map_object in enumerate(pfr_policy_map_object_list):
        #fetch payload
        pfr_policy_map_payload = pfr_policy_map_object.getxml(filter=True)

        util.log_debug('update pfr_policy_map_payload %s'%pfr_policy_map_payload)

        rcpath = rcpath_list[rc_counter]
        #call the base abstract class for createData
        super(self.__class__, self).update_(sdata, dev, rcpath=rcpath, payload=pfr_policy_map_payload, key_hints=self.key_hints, addref=kwargs.get('addref', False), autocommit=kwargs.get('autocommit', True), mapping_dict=mapping_dict)

    def delete(self, sdata, dev, mapping_dict, fail_silently=False, remove_reference=False):
      dev = get_valid_devices(dev)
      if len(dev) == 0:
        return

      ##prepare rcpath
      rcpath = "qos:pfr-policy-maps"
      self.delete_(sdata, dev, mapping_dict=mapping_dict, fail_silently=fail_silently, remove_reference=remove_reference, rcpath=rcpath)

    def delete_(self, sdata, dev, **kwargs):
      mapping_dict = kwargs.get('mapping_dict')

      #validating inputs
      if util.isEmpty(mapping_dict.get('name')):
        raise Exception("'name' cannot be empty")

      #convert keys to list
      name = mapping_dict.get('name')
      if not isinstance(name, list):
        name = [name]

      #prepare rcpath
      rcpath = kwargs.get('rcpath')
      rcpath_list = []
      for name_iterator in name:
        rcpath_tmp =  rcpath+"/pfr-policy-map=%s"%(util.make_interfacename(name_iterator))
        rcpath_list.append(rcpath_tmp)
      payload = ''

      for rcpath in rcpath_list:
        #call the base abstract class for deleteData
        super(self.__class__, self).delete_(sdata, dev, rcpath=rcpath, payload=payload, fail_silently=kwargs.get('fail_silently', False), remove_reference=kwargs.get('remove_reference', False))

    def validate_inputs_form_payload(self, mapping_dict, update=False):
      #validating inputs
      if util.isEmpty(mapping_dict.get('name')):
        raise Exception("'name' cannot be empty")
      if util.isEmpty(mapping_dict.get('domain_name')):
        raise Exception("'domain_name' cannot be empty")
      if util.isEmpty(mapping_dict.get('vrf_name')):
        raise Exception("'vrf_name' cannot be empty")

      #convert keys to list
      name = mapping_dict.get('name')
      if not isinstance(name, list):
        name = [name]

      #prepare payload
      pfr_policy_map_object_list = []
      for name_iterator in name:
        from servicemodel.controller.devices.device import pfr_policy_maps
        pfr_policy_map_object = pfr_policy_maps.pfr_policy_map.pfr_policy_map()
        pfr_policy_map_object.name = name_iterator
        try:
          if (update == False) or (update == True and str(mapping_dict.get('domain_name', None)) != ''):
            pfr_policy_map_object.domain_name = mapping_dict.get('domain_name', None)
          else:
            pfr_policy_map_object.domain_name._empty_tag = True
        except TypeError:
          pass
        try:
          if (update == False) or (update == True and str(mapping_dict.get('vrf_name', None)) != ''):
            pfr_policy_map_object.vrf_name = mapping_dict.get('vrf_name', None)
          else:
            pfr_policy_map_object.vrf_name._empty_tag = True
        except TypeError:
          pass
        pfr_policy_map_object_list.append(pfr_policy_map_object)

      return pfr_policy_map_object_list

    #XPATH devices/device/pfr-policy-maps/pfr-policy-map/pfr-class-entry
    class pfr_class_entry(AbstractDeviceMgr):
      key_hints = [['name']]
      def getRcpathPayload(self, sdata, dev, pfr_policy_map_name, mapping_dict):
        dev = get_valid_devices(dev)
        if len(dev) == 0:
          return
        #Input Key Validations
        self.validate_parent_keys( pfr_policy_map_name)

        #convert parent keys to list
        rcpath_list = []
        payload_list = []
        if not isinstance(pfr_policy_map_name, list):
          pfr_policy_map_name_list = [pfr_policy_map_name]
        else:
          pfr_policy_map_name_list = pfr_policy_map_name

        for pfr_policy_map_name in pfr_policy_map_name_list:
          ##prepare rcpath
          rcpath = "qos:pfr-policy-maps/pfr-policy-map=%s"%(util.make_interfacename(pfr_policy_map_name))
          rcpath_list.append(rcpath)
        mapping_dict = kwargs.get('mapping_dict')

        #validating inputs and get payload object
        pfr_class_entry_object_list = self.validate_inputs_form_payload(mapping_dict)

        for pfr_class_entry_object in pfr_class_entry_object_list:
          #fetch payload
          pfr_class_entry_payload = pfr_class_entry_object.getxml(filter=True)
          util.log_debug('pfr_class_entry_payload %s'%pfr_class_entry_payload)
          payload_list.append(pfr_class_entry_payload)

        return rcpath_list, payload_list

      def create(self, sdata, dev, pfr_policy_map_name, mapping_dict, addref=True, autocommit=True):
        dev = get_valid_devices(dev)
        if len(dev) == 0:
          return
        #Input Key Validations
        self.validate_parent_keys( pfr_policy_map_name)

        #convert parent keys to list
        if not isinstance(pfr_policy_map_name, list):
          pfr_policy_map_name_list = [pfr_policy_map_name]
        else:
          pfr_policy_map_name_list = pfr_policy_map_name

        for pfr_policy_map_name in pfr_policy_map_name_list:
          ##prepare rcpath
          rcpath = "qos:pfr-policy-maps/pfr-policy-map=%s"%(util.make_interfacename(pfr_policy_map_name))
          self.create_(sdata, dev, mapping_dict=mapping_dict, addref=addref, autocommit=autocommit, rcpath=rcpath)

      def create_(self, sdata, dev, **kwargs):
        mapping_dict = kwargs.get('mapping_dict')

        #validating inputs and get payload object
        pfr_class_entry_object_list = self.validate_inputs_form_payload(mapping_dict)

        for pfr_class_entry_object in pfr_class_entry_object_list:
          #fetch payload
          pfr_class_entry_payload = pfr_class_entry_object.getxml(filter=True)

          util.log_debug('pfr_class_entry_payload %s'%pfr_class_entry_payload)

          #call the base abstract class for createData
          super(self.__class__, self).create_(sdata, dev, rcpath=kwargs.get('rcpath'), payload=pfr_class_entry_payload, key_hints=self.key_hints, addref=kwargs.get('addref', True), autocommit=kwargs.get('autocommit', True), mapping_dict=mapping_dict)

      def update(self, sdata, dev, pfr_policy_map_name, mapping_dict, addref=True, autocommit=True):
        dev = get_valid_devices(dev)
        if len(dev) == 0:
          return
        #Input Key Validations
        self.validate_parent_keys( pfr_policy_map_name)


        ##prepare rcpath
        rcpath = "qos:pfr-policy-maps/pfr-policy-map=%s"%(util.make_interfacename(pfr_policy_map_name))
        self.update_(sdata, dev, mapping_dict=mapping_dict, addref=addref, autocommit=autocommit, rcpath=rcpath)

      def update_(self, sdata, dev, **kwargs):
        mapping_dict = kwargs.get('mapping_dict')

        #validating inputs and get payload object
        pfr_class_entry_object_list = self.validate_inputs_form_payload(mapping_dict, update=True)

        #convert keys to list
        class_name = mapping_dict.get('class_name')
        if not isinstance(class_name, list):
          class_name = [class_name]

        #prepare rcpath
        rcpath = kwargs.get('rcpath')
        rcpath_list = []
        for class_name_iterator in class_name:
          rcpath_tmp =  rcpath+"/pfr-class-entry=%s"%(util.make_interfacename(class_name_iterator))
          rcpath_list.append(rcpath_tmp)
        for rc_counter, pfr_class_entry_object in enumerate(pfr_class_entry_object_list):
          #fetch payload
          pfr_class_entry_payload = pfr_class_entry_object.getxml(filter=True)

          util.log_debug('update pfr_class_entry_payload %s'%pfr_class_entry_payload)

          rcpath = rcpath_list[rc_counter]
          #call the base abstract class for createData
          super(self.__class__, self).update_(sdata, dev, rcpath=rcpath, payload=pfr_class_entry_payload, key_hints=self.key_hints, addref=kwargs.get('addref', False), autocommit=kwargs.get('autocommit', True), mapping_dict=mapping_dict)

      def delete(self, sdata, dev, pfr_policy_map_name, mapping_dict, fail_silently=False, remove_reference=False):
        dev = get_valid_devices(dev)
        if len(dev) == 0:
          return
        #Input Key Validations
        self.validate_parent_keys( pfr_policy_map_name)


        if not isinstance(pfr_policy_map_name, list):
          pfr_policy_map_name_list = [pfr_policy_map_name]
        else:
          pfr_policy_map_name_list = pfr_policy_map_name

        for pfr_policy_map_name in pfr_policy_map_name_list:
          ##prepare rcpath
          rcpath = "qos:pfr-policy-maps/pfr-policy-map=%s"%(util.make_interfacename(pfr_policy_map_name))
          self.delete_(sdata, dev, mapping_dict=mapping_dict, fail_silently=fail_silently, remove_reference=remove_reference, rcpath=rcpath)

      def delete_(self, sdata, dev, **kwargs):
        mapping_dict = kwargs.get('mapping_dict')

        #validating inputs
        if util.isEmpty(mapping_dict.get('class_name')):
          raise Exception("'class_name' cannot be empty")

        #convert keys to list
        class_name = mapping_dict.get('class_name')
        if not isinstance(class_name, list):
          class_name = [class_name]

        #prepare rcpath
        rcpath = kwargs.get('rcpath')
        rcpath_list = []
        for class_name_iterator in class_name:
          rcpath_tmp =  rcpath+"/pfr-class-entry=%s"%(util.make_interfacename(class_name_iterator))
          rcpath_list.append(rcpath_tmp)
        payload = ''

        for rcpath in rcpath_list:
          #call the base abstract class for deleteData
          super(self.__class__, self).delete_(sdata, dev, rcpath=rcpath, payload=payload, fail_silently=kwargs.get('fail_silently', False), remove_reference=kwargs.get('remove_reference', False))

      def validate_parent_keys(self, pfr_policy_map_name):
        #Parent Key Validations
        if util.isEmpty(pfr_policy_map_name):
          raise Exception("'pfr_policy_map_name' cannot be empty")

      def validate_inputs_form_payload(self, mapping_dict, update=False):
        #validating inputs
        if util.isEmpty(mapping_dict.get('class_name')):
          raise Exception("'class_name' cannot be empty")
        if util.isEmpty(mapping_dict.get('sequence_number')):
          raise Exception("'sequence_number' cannot be empty")
        if util.isEmpty(mapping_dict.get('primary_path_preference')):
          raise Exception("'primary_path_preference' cannot be empty")

        #convert keys to list
        class_name = mapping_dict.get('class_name')
        if not isinstance(class_name, list):
          class_name = [class_name]

        #prepare payload
        pfr_class_entry_object_list = []
        for class_name_iterator in class_name:
          from servicemodel.controller.devices.device.pfr_policy_maps import pfr_policy_map
          pfr_class_entry_object = pfr_policy_map.pfr_class_entry.pfr_class_entry()
          pfr_class_entry_object.class_name = class_name_iterator
          try:
            if (update == False) or (update == True and str(mapping_dict.get('sequence_number', None)) != ''):
              pfr_class_entry_object.sequence_number = mapping_dict.get('sequence_number', None)
            else:
              pfr_class_entry_object.sequence_number._empty_tag = True
          except TypeError:
            pass
          try:
            if (update == False) or (update == True and str(mapping_dict.get('primary_path_preference', None)) != ''):
              pfr_class_entry_object.primary_path_preference = mapping_dict.get('primary_path_preference', None)
            else:
              pfr_class_entry_object.primary_path_preference._empty_tag = True
          except TypeError:
            pass
          try:
            if (update == False) or (update == True and str(mapping_dict.get('secondary_path_preference', None)) != ''):
              pfr_class_entry_object.secondary_path_preference = mapping_dict.get('secondary_path_preference', None)
            else:
              pfr_class_entry_object.secondary_path_preference._empty_tag = True
          except TypeError:
            pass
          try:
            if (update == False) or (update == True and str(mapping_dict.get('next_fallback', None)) != ''):
              pfr_class_entry_object.next_fallback = mapping_dict.get('next_fallback', None)
            else:
              pfr_class_entry_object.next_fallback._empty_tag = True
          except TypeError:
            pass
          pfr_class_entry_object_list.append(pfr_class_entry_object)

        return pfr_class_entry_object_list

