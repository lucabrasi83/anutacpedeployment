#
# This computer program is the confidential information and proprietary trade
# secret of Anuta Networks, Inc. Possessions and use of this program must
# conform strictly to the license agreement between the user and
# Anuta Networks, Inc., and receipt or possession does not convey any rights
# to divulge, reproduce, or allow others to use this program without specific
# written authorization of Anuta Networks, Inc.
#
# Copyright (c) 2015-2016 Anuta Networks, Inc. All Rights Reserved.
#

#
#DO NOT EDIT THIS FILE ITS AUTOGENERATED ONE
#

from servicemodel import util
from servicemodel import yang
from servicemodel import devicemgr
from servicemodel.abstract_dev_mgr import AbstractDeviceMgr

def get_valid_devices(devs):
  vdevs = []
  if isinstance(devs, list):
    for dev in devs:
      drivername = dev.device.get_field_value('driver_name')
      if util.isEmpty(drivername):
        vdevs.append(dev)
  else:
    drivername = devs.device.get_field_value('driver_name')
    if util.isEmpty(drivername):
      vdevs.append(devs)

  return vdevs

class real_servers(object):
  #XPATH devices/device/real-servers/real-server
  class real_server(AbstractDeviceMgr):
    key_hints = [[]]
    def getRcpathPayload(self, sdata, dev, mapping_dict):
      dev = get_valid_devices(dev)
      if len(dev) == 0:
        return
      #convert parent keys to list
      rcpath_list = []
      payload_list = []
      ##prepare rcpath
      rcpath = "loadbalancer:real-servers"
      rcpath_list.append(rcpath)
      mapping_dict = kwargs.get('mapping_dict')

      #validating inputs and get payload object
      real_server_object_list = self.validate_inputs_form_payload(mapping_dict)

      for real_server_object in real_server_object_list:
        #fetch payload
        real_server_payload = real_server_object.getxml(filter=True)
        util.log_debug('real_server_payload %s'%real_server_payload)
        payload_list.append(real_server_payload)

      return rcpath_list, payload_list

    def create(self, sdata, dev, mapping_dict, addref=True, autocommit=True):
      dev = get_valid_devices(dev)
      if len(dev) == 0:
        return
      #convert parent keys to list
      ##prepare rcpath
      rcpath = "loadbalancer:real-servers"
      self.create_(sdata, dev, mapping_dict=mapping_dict, addref=addref, autocommit=autocommit, rcpath=rcpath)

    def create_(self, sdata, dev, **kwargs):
      mapping_dict = kwargs.get('mapping_dict')

      #validating inputs and get payload object
      real_server_object_list = self.validate_inputs_form_payload(mapping_dict)

      for real_server_object in real_server_object_list:
        #fetch payload
        real_server_payload = real_server_object.getxml(filter=True)

        util.log_debug('real_server_payload %s'%real_server_payload)

        #call the base abstract class for createData
        super(self.__class__, self).create_(sdata, dev, rcpath=kwargs.get('rcpath'), payload=real_server_payload, key_hints=self.key_hints, addref=kwargs.get('addref', True), autocommit=kwargs.get('autocommit', True), mapping_dict=mapping_dict)

    def update(self, sdata, dev, mapping_dict, addref=False, autocommit=True):
      dev = get_valid_devices(dev)
      if len(dev) == 0:
        return

      ##prepare rcpath
      rcpath = "loadbalancer:real-servers"
      self.update_(sdata, dev, mapping_dict=mapping_dict, addref=addref, autocommit=autocommit, rcpath=rcpath)

    def update_(self, sdata, dev, **kwargs):
      mapping_dict = kwargs.get('mapping_dict')

      #validating inputs and get payload object
      real_server_object_list = self.validate_inputs_form_payload(mapping_dict, update=True)

      #convert keys to list
      ip_address = mapping_dict.get('ip_address')
      if not isinstance(ip_address, list):
        ip_address = [ip_address]
      domain_number = mapping_dict.get('domain_number')
      if not isinstance(domain_number, list):
        domain_number = [domain_number]
      partition = mapping_dict.get('partition')
      if not isinstance(partition, list):
        partition = [partition]

      #prepare rcpath
      rcpath = kwargs.get('rcpath')
      rcpath_list = []
      for ip_address_iterator in ip_address:
        for domain_number_iterator in domain_number:
          for partition_iterator in partition:
            rcpath_tmp =  rcpath+"/real-server=%s,%s,%s"%(util.make_interfacename(ip_address_iterator),util.make_interfacename(domain_number_iterator),util.make_interfacename(partition_iterator))
            rcpath_list.append(rcpath_tmp)
      for rc_counter, real_server_object in enumerate(real_server_object_list):
        #fetch payload
        real_server_payload = real_server_object.getxml(filter=True)

        util.log_debug('update real_server_payload %s'%real_server_payload)

        rcpath = rcpath_list[rc_counter]
        #call the base abstract class for createData
        super(self.__class__, self).update_(sdata, dev, rcpath=rcpath, payload=real_server_payload, key_hints=self.key_hints, addref=kwargs.get('addref', False), autocommit=kwargs.get('autocommit', True), mapping_dict=mapping_dict)

    def delete(self, sdata, dev, mapping_dict, fail_silently=False, remove_reference=False):
      dev = get_valid_devices(dev)
      if len(dev) == 0:
        return

      ##prepare rcpath
      rcpath = "loadbalancer:real-servers"
      self.delete_(sdata, dev, mapping_dict=mapping_dict, fail_silently=fail_silently, remove_reference=remove_reference, rcpath=rcpath)

    def delete_(self, sdata, dev, **kwargs):
      mapping_dict = kwargs.get('mapping_dict')

      #validating inputs
      if util.isEmpty(mapping_dict.get('ip_address')):
        raise Exception("'ip_address' cannot be empty")
      if util.isEmpty(mapping_dict.get('domain_number')):
        raise Exception("'domain_number' cannot be empty")
      if util.isEmpty(mapping_dict.get('partition')):
        raise Exception("'partition' cannot be empty")

      #convert keys to list
      ip_address = mapping_dict.get('ip_address')
      if not isinstance(ip_address, list):
        ip_address = [ip_address]
      domain_number = mapping_dict.get('domain_number')
      if not isinstance(domain_number, list):
        domain_number = [domain_number]
      partition = mapping_dict.get('partition')
      if not isinstance(partition, list):
        partition = [partition]

      #prepare rcpath
      rcpath = kwargs.get('rcpath')
      rcpath_list = []
      for ip_address_iterator in ip_address:
        for domain_number_iterator in domain_number:
          for partition_iterator in partition:
            rcpath_tmp =  rcpath+"/real-server=%s,%s,%s"%(util.make_interfacename(ip_address_iterator),util.make_interfacename(domain_number_iterator),util.make_interfacename(partition_iterator))
            rcpath_list.append(rcpath_tmp)
      payload = ''

      for rcpath in rcpath_list:
        #call the base abstract class for deleteData
        super(self.__class__, self).delete_(sdata, dev, rcpath=rcpath, payload=payload, fail_silently=kwargs.get('fail_silently', False), remove_reference=kwargs.get('remove_reference', False))

    def validate_inputs_form_payload(self, mapping_dict, update=False):
      #validating inputs
      if util.isEmpty(mapping_dict.get('ip_address')):
        raise Exception("'ip_address' cannot be empty")
      if util.isEmpty(mapping_dict.get('domain_number')):
        raise Exception("'domain_number' cannot be empty")
      if util.isEmpty(mapping_dict.get('partition')):
        raise Exception("'partition' cannot be empty")

      #convert keys to list
      ip_address = mapping_dict.get('ip_address')
      if not isinstance(ip_address, list):
        ip_address = [ip_address]
      domain_number = mapping_dict.get('domain_number')
      if not isinstance(domain_number, list):
        domain_number = [domain_number]
      partition = mapping_dict.get('partition')
      if not isinstance(partition, list):
        partition = [partition]

      #prepare payload
      real_server_object_list = []
      for ip_address_iterator in ip_address:
        for domain_number_iterator in domain_number:
          for partition_iterator in partition:
            from servicemodel.controller.devices.device import real_servers
            real_server_object = real_servers.real_server.real_server()
            try:
              if (update == False) or (update == True and str(mapping_dict.get('name', None)) != ''):
                real_server_object.name = mapping_dict.get('name', None)
              else:
                real_server_object.name._empty_tag = True
            except TypeError:
              pass
            real_server_object.ip_address = ip_address_iterator
            try:
              if (update == False) or (update == True and str(mapping_dict.get('connection_limit', None)) != ''):
                real_server_object.connection_limit = mapping_dict.get('connection_limit', None)
              else:
                real_server_object.connection_limit._empty_tag = True
            except TypeError:
              pass
            try:
              if (update == False) or (update == True and str(mapping_dict.get('state', None)) != ''):
                real_server_object.state = mapping_dict.get('state', None)
              else:
                real_server_object.state._empty_tag = True
            except TypeError:
              pass
            real_server_object.domain_number = domain_number_iterator
            real_server_object.partition = partition_iterator
            real_server_object_list.append(real_server_object)

      return real_server_object_list

