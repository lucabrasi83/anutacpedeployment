#
# This computer program is the confidential information and proprietary trade
# secret of Anuta Networks, Inc. Possessions and use of this program must
# conform strictly to the license agreement between the user and
# Anuta Networks, Inc., and receipt or possession does not convey any rights
# to divulge, reproduce, or allow others to use this program without specific
# written authorization of Anuta Networks, Inc.
#
# Copyright (c) 2015-2016 Anuta Networks, Inc. All Rights Reserved.
#

#
#DO NOT EDIT THIS FILE ITS AUTOGENERATED ONE
#

from servicemodel import util
from servicemodel import yang
from servicemodel import devicemgr
from servicemodel.abstract_dev_mgr import AbstractDeviceMgr

def get_valid_devices(devs):
  vdevs = []
  if isinstance(devs, list):
    for dev in devs:
      drivername = dev.device.get_field_value('driver_name')
      if util.isEmpty(drivername):
        vdevs.append(dev)
  else:
    drivername = devs.device.get_field_value('driver_name')
    if util.isEmpty(drivername):
      vdevs.append(devs)

  return vdevs

class key_chain(object):
  #XPATH devices/device/key-chain/router-key-chain
  class router_key_chain(AbstractDeviceMgr):
    key_hints = [[]]
    def getRcpathPayload(self, sdata, dev, mapping_dict):
      dev = get_valid_devices(dev)
      if len(dev) == 0:
        return
      #convert parent keys to list
      rcpath_list = []
      payload_list = []
      ##prepare rcpath
      rcpath = "l3features:key-chain"
      rcpath_list.append(rcpath)
      mapping_dict = kwargs.get('mapping_dict')

      #validating inputs and get payload object
      router_key_chain_object_list = self.validate_inputs_form_payload(mapping_dict)

      for router_key_chain_object in router_key_chain_object_list:
        #fetch payload
        router_key_chain_payload = router_key_chain_object.getxml(filter=True)
        util.log_debug('router_key_chain_payload %s'%router_key_chain_payload)
        payload_list.append(router_key_chain_payload)

      return rcpath_list, payload_list

    def create(self, sdata, dev, mapping_dict, addref=True, autocommit=True):
      dev = get_valid_devices(dev)
      if len(dev) == 0:
        return
      #convert parent keys to list
      ##prepare rcpath
      rcpath = "l3features:key-chain"
      self.create_(sdata, dev, mapping_dict=mapping_dict, addref=addref, autocommit=autocommit, rcpath=rcpath)

    def create_(self, sdata, dev, **kwargs):
      mapping_dict = kwargs.get('mapping_dict')

      #validating inputs and get payload object
      router_key_chain_object_list = self.validate_inputs_form_payload(mapping_dict)

      for router_key_chain_object in router_key_chain_object_list:
        #fetch payload
        router_key_chain_payload = router_key_chain_object.getxml(filter=True)

        util.log_debug('router_key_chain_payload %s'%router_key_chain_payload)

        #call the base abstract class for createData
        super(self.__class__, self).create_(sdata, dev, rcpath=kwargs.get('rcpath'), payload=router_key_chain_payload, key_hints=self.key_hints, addref=kwargs.get('addref', True), autocommit=kwargs.get('autocommit', True), mapping_dict=mapping_dict)

    def update(self, sdata, dev, mapping_dict, addref=False, autocommit=True):
      dev = get_valid_devices(dev)
      if len(dev) == 0:
        return

      ##prepare rcpath
      rcpath = "l3features:key-chain"
      self.update_(sdata, dev, mapping_dict=mapping_dict, addref=addref, autocommit=autocommit, rcpath=rcpath)

    def update_(self, sdata, dev, **kwargs):
      mapping_dict = kwargs.get('mapping_dict')

      #validating inputs and get payload object
      router_key_chain_object_list = self.validate_inputs_form_payload(mapping_dict, update=True)

      #convert keys to list
      key_chain_name = mapping_dict.get('key_chain_name')
      if not isinstance(key_chain_name, list):
        key_chain_name = [key_chain_name]

      #prepare rcpath
      rcpath = kwargs.get('rcpath')
      rcpath_list = []
      for key_chain_name_iterator in key_chain_name:
        rcpath_tmp =  rcpath+"/router-key-chain=%s"%(util.make_interfacename(key_chain_name_iterator))
        rcpath_list.append(rcpath_tmp)
      for rc_counter, router_key_chain_object in enumerate(router_key_chain_object_list):
        #fetch payload
        router_key_chain_payload = router_key_chain_object.getxml(filter=True)

        util.log_debug('update router_key_chain_payload %s'%router_key_chain_payload)

        rcpath = rcpath_list[rc_counter]
        #call the base abstract class for createData
        super(self.__class__, self).update_(sdata, dev, rcpath=rcpath, payload=router_key_chain_payload, key_hints=self.key_hints, addref=kwargs.get('addref', False), autocommit=kwargs.get('autocommit', True), mapping_dict=mapping_dict)

    def delete(self, sdata, dev, mapping_dict, fail_silently=False, remove_reference=False):
      dev = get_valid_devices(dev)
      if len(dev) == 0:
        return

      ##prepare rcpath
      rcpath = "l3features:key-chain"
      self.delete_(sdata, dev, mapping_dict=mapping_dict, fail_silently=fail_silently, remove_reference=remove_reference, rcpath=rcpath)

    def delete_(self, sdata, dev, **kwargs):
      mapping_dict = kwargs.get('mapping_dict')

      #validating inputs
      if util.isEmpty(mapping_dict.get('key_chain_name')):
        raise Exception("'key_chain_name' cannot be empty")

      #convert keys to list
      key_chain_name = mapping_dict.get('key_chain_name')
      if not isinstance(key_chain_name, list):
        key_chain_name = [key_chain_name]

      #prepare rcpath
      rcpath = kwargs.get('rcpath')
      rcpath_list = []
      for key_chain_name_iterator in key_chain_name:
        rcpath_tmp =  rcpath+"/router-key-chain=%s"%(util.make_interfacename(key_chain_name_iterator))
        rcpath_list.append(rcpath_tmp)
      payload = ''

      for rcpath in rcpath_list:
        #call the base abstract class for deleteData
        super(self.__class__, self).delete_(sdata, dev, rcpath=rcpath, payload=payload, fail_silently=kwargs.get('fail_silently', False), remove_reference=kwargs.get('remove_reference', False))

    def validate_inputs_form_payload(self, mapping_dict, update=False):
      #validating inputs
      if util.isEmpty(mapping_dict.get('key_chain_name')):
        raise Exception("'key_chain_name' cannot be empty")

      #convert keys to list
      key_chain_name = mapping_dict.get('key_chain_name')
      if not isinstance(key_chain_name, list):
        key_chain_name = [key_chain_name]

      #prepare payload
      router_key_chain_object_list = []
      for key_chain_name_iterator in key_chain_name:
        from servicemodel.controller.devices.device import key_chain
        router_key_chain_object = key_chain.router_key_chain.router_key_chain()
        router_key_chain_object.key_chain_name = key_chain_name_iterator
        router_key_chain_object_list.append(router_key_chain_object)

      return router_key_chain_object_list

    #XPATH devices/device/key-chain/router-key-chain/keys
    class keys(AbstractDeviceMgr):
      key_hints = [['key_chain_name']]
      def getRcpathPayload(self, sdata, dev, router_key_chain_key_chain_name, mapping_dict):
        dev = get_valid_devices(dev)
        if len(dev) == 0:
          return
        #Input Key Validations
        self.validate_parent_keys( router_key_chain_key_chain_name)

        #convert parent keys to list
        rcpath_list = []
        payload_list = []
        if not isinstance(router_key_chain_key_chain_name, list):
          router_key_chain_key_chain_name_list = [router_key_chain_key_chain_name]
        else:
          router_key_chain_key_chain_name_list = router_key_chain_key_chain_name

        for router_key_chain_key_chain_name in router_key_chain_key_chain_name_list:
          ##prepare rcpath
          rcpath = "l3features:key-chain/router-key-chain=%s"%(util.make_interfacename(router_key_chain_key_chain_name))
          rcpath_list.append(rcpath)
        mapping_dict = kwargs.get('mapping_dict')

        #validating inputs and get payload object
        keys_object_list = self.validate_inputs_form_payload(mapping_dict)

        for keys_object in keys_object_list:
          #fetch payload
          keys_payload = keys_object.getxml(filter=True)
          util.log_debug('keys_payload %s'%keys_payload)
          payload_list.append(keys_payload)

        return rcpath_list, payload_list

      def create(self, sdata, dev, router_key_chain_key_chain_name, mapping_dict, addref=True, autocommit=True):
        dev = get_valid_devices(dev)
        if len(dev) == 0:
          return
        #Input Key Validations
        self.validate_parent_keys( router_key_chain_key_chain_name)

        #convert parent keys to list
        if not isinstance(router_key_chain_key_chain_name, list):
          router_key_chain_key_chain_name_list = [router_key_chain_key_chain_name]
        else:
          router_key_chain_key_chain_name_list = router_key_chain_key_chain_name

        for router_key_chain_key_chain_name in router_key_chain_key_chain_name_list:
          ##prepare rcpath
          rcpath = "l3features:key-chain/router-key-chain=%s"%(util.make_interfacename(router_key_chain_key_chain_name))
          self.create_(sdata, dev, mapping_dict=mapping_dict, addref=addref, autocommit=autocommit, rcpath=rcpath)

      def create_(self, sdata, dev, **kwargs):
        mapping_dict = kwargs.get('mapping_dict')

        #validating inputs and get payload object
        keys_object_list = self.validate_inputs_form_payload(mapping_dict)

        for keys_object in keys_object_list:
          #fetch payload
          keys_payload = keys_object.getxml(filter=True)

          util.log_debug('keys_payload %s'%keys_payload)

          #call the base abstract class for createData
          super(self.__class__, self).create_(sdata, dev, rcpath=kwargs.get('rcpath'), payload=keys_payload, key_hints=self.key_hints, addref=kwargs.get('addref', True), autocommit=kwargs.get('autocommit', True), mapping_dict=mapping_dict)

      def update(self, sdata, dev, router_key_chain_key_chain_name, mapping_dict, addref=True, autocommit=True):
        dev = get_valid_devices(dev)
        if len(dev) == 0:
          return
        #Input Key Validations
        self.validate_parent_keys( router_key_chain_key_chain_name)


        ##prepare rcpath
        rcpath = "l3features:key-chain/router-key-chain=%s"%(util.make_interfacename(router_key_chain_key_chain_name))
        self.update_(sdata, dev, mapping_dict=mapping_dict, addref=addref, autocommit=autocommit, rcpath=rcpath)

      def update_(self, sdata, dev, **kwargs):
        mapping_dict = kwargs.get('mapping_dict')

        #validating inputs and get payload object
        keys_object_list = self.validate_inputs_form_payload(mapping_dict, update=True)

        #convert keys to list
        key_identifier = mapping_dict.get('key_identifier')
        if not isinstance(key_identifier, list):
          key_identifier = [key_identifier]

        #prepare rcpath
        rcpath = kwargs.get('rcpath')
        rcpath_list = []
        for key_identifier_iterator in key_identifier:
          rcpath_tmp =  rcpath+"/keys=%s"%(util.make_interfacename(key_identifier_iterator))
          rcpath_list.append(rcpath_tmp)
        for rc_counter, keys_object in enumerate(keys_object_list):
          #fetch payload
          keys_payload = keys_object.getxml(filter=True)

          util.log_debug('update keys_payload %s'%keys_payload)

          rcpath = rcpath_list[rc_counter]
          #call the base abstract class for createData
          super(self.__class__, self).update_(sdata, dev, rcpath=rcpath, payload=keys_payload, key_hints=self.key_hints, addref=kwargs.get('addref', False), autocommit=kwargs.get('autocommit', True), mapping_dict=mapping_dict)

      def delete(self, sdata, dev, router_key_chain_key_chain_name, mapping_dict, fail_silently=False, remove_reference=False):
        dev = get_valid_devices(dev)
        if len(dev) == 0:
          return
        #Input Key Validations
        self.validate_parent_keys( router_key_chain_key_chain_name)


        if not isinstance(router_key_chain_key_chain_name, list):
          router_key_chain_key_chain_name_list = [router_key_chain_key_chain_name]
        else:
          router_key_chain_key_chain_name_list = router_key_chain_key_chain_name

        for router_key_chain_key_chain_name in router_key_chain_key_chain_name_list:
          ##prepare rcpath
          rcpath = "l3features:key-chain/router-key-chain=%s"%(util.make_interfacename(router_key_chain_key_chain_name))
          self.delete_(sdata, dev, mapping_dict=mapping_dict, fail_silently=fail_silently, remove_reference=remove_reference, rcpath=rcpath)

      def delete_(self, sdata, dev, **kwargs):
        mapping_dict = kwargs.get('mapping_dict')

        #validating inputs
        if util.isEmpty(mapping_dict.get('key_identifier')):
          raise Exception("'key_identifier' cannot be empty")

        #convert keys to list
        key_identifier = mapping_dict.get('key_identifier')
        if not isinstance(key_identifier, list):
          key_identifier = [key_identifier]

        #prepare rcpath
        rcpath = kwargs.get('rcpath')
        rcpath_list = []
        for key_identifier_iterator in key_identifier:
          rcpath_tmp =  rcpath+"/keys=%s"%(util.make_interfacename(key_identifier_iterator))
          rcpath_list.append(rcpath_tmp)
        payload = ''

        for rcpath in rcpath_list:
          #call the base abstract class for deleteData
          super(self.__class__, self).delete_(sdata, dev, rcpath=rcpath, payload=payload, fail_silently=kwargs.get('fail_silently', False), remove_reference=kwargs.get('remove_reference', False))

      def validate_parent_keys(self, router_key_chain_key_chain_name):
        #Parent Key Validations
        if util.isEmpty(router_key_chain_key_chain_name):
          raise Exception("'router_key_chain_key_chain_name' cannot be empty")

      def validate_inputs_form_payload(self, mapping_dict, update=False):
        #validating inputs
        if util.isEmpty(mapping_dict.get('key_identifier')):
          raise Exception("'key_identifier' cannot be empty")

        #convert keys to list
        key_identifier = mapping_dict.get('key_identifier')
        if not isinstance(key_identifier, list):
          key_identifier = [key_identifier]

        #prepare payload
        keys_object_list = []
        for key_identifier_iterator in key_identifier:
          from servicemodel.controller.devices.device.key_chain import router_key_chain
          keys_object = router_key_chain.keys.keys()
          keys_object.key_identifier = key_identifier_iterator
          try:
            if (update == False) or (update == True and str(mapping_dict.get('key_string_password', None)) != ''):
              keys_object.key_string_password = mapping_dict.get('key_string_password', None)
            else:
              keys_object.key_string_password._empty_tag = True
          except TypeError:
            pass
          keys_object_list.append(keys_object)

        return keys_object_list

