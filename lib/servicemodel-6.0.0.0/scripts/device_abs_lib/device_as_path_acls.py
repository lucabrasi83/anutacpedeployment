#
# This computer program is the confidential information and proprietary trade
# secret of Anuta Networks, Inc. Possessions and use of this program must
# conform strictly to the license agreement between the user and
# Anuta Networks, Inc., and receipt or possession does not convey any rights
# to divulge, reproduce, or allow others to use this program without specific
# written authorization of Anuta Networks, Inc.
#
# Copyright (c) 2015-2016 Anuta Networks, Inc. All Rights Reserved.
#

#
#DO NOT EDIT THIS FILE ITS AUTOGENERATED ONE
#

from servicemodel import util
from servicemodel import yang
from servicemodel import devicemgr
from servicemodel.abstract_dev_mgr import AbstractDeviceMgr

def get_valid_devices(devs):
  vdevs = []
  if isinstance(devs, list):
    for dev in devs:
      drivername = dev.device.get_field_value('driver_name')
      if util.isEmpty(drivername):
        vdevs.append(dev)
  else:
    drivername = devs.device.get_field_value('driver_name')
    if util.isEmpty(drivername):
      vdevs.append(devs)

  return vdevs

class as_path_acls(object):
  #XPATH devices/device/as-path-acls/as-path-acl
  class as_path_acl(AbstractDeviceMgr):
    key_hints = [[]]
    def getRcpathPayload(self, sdata, dev, mapping_dict):
      dev = get_valid_devices(dev)
      if len(dev) == 0:
        return
      #convert parent keys to list
      rcpath_list = []
      payload_list = []
      ##prepare rcpath
      rcpath = "l3features:as-path-acls"
      rcpath_list.append(rcpath)
      mapping_dict = kwargs.get('mapping_dict')

      #validating inputs and get payload object
      as_path_acl_object_list = self.validate_inputs_form_payload(mapping_dict)

      for as_path_acl_object in as_path_acl_object_list:
        #fetch payload
        as_path_acl_payload = as_path_acl_object.getxml(filter=True)
        util.log_debug('as_path_acl_payload %s'%as_path_acl_payload)
        payload_list.append(as_path_acl_payload)

      return rcpath_list, payload_list

    def create(self, sdata, dev, mapping_dict, addref=True, autocommit=True):
      dev = get_valid_devices(dev)
      if len(dev) == 0:
        return
      #convert parent keys to list
      ##prepare rcpath
      rcpath = "l3features:as-path-acls"
      self.create_(sdata, dev, mapping_dict=mapping_dict, addref=addref, autocommit=autocommit, rcpath=rcpath)

    def create_(self, sdata, dev, **kwargs):
      mapping_dict = kwargs.get('mapping_dict')

      #validating inputs and get payload object
      as_path_acl_object_list = self.validate_inputs_form_payload(mapping_dict)

      for as_path_acl_object in as_path_acl_object_list:
        #fetch payload
        as_path_acl_payload = as_path_acl_object.getxml(filter=True)

        util.log_debug('as_path_acl_payload %s'%as_path_acl_payload)

        #call the base abstract class for createData
        super(self.__class__, self).create_(sdata, dev, rcpath=kwargs.get('rcpath'), payload=as_path_acl_payload, key_hints=self.key_hints, addref=kwargs.get('addref', True), autocommit=kwargs.get('autocommit', True), mapping_dict=mapping_dict)

    def update(self, sdata, dev, mapping_dict, addref=False, autocommit=True):
      dev = get_valid_devices(dev)
      if len(dev) == 0:
        return

      ##prepare rcpath
      rcpath = "l3features:as-path-acls"
      self.update_(sdata, dev, mapping_dict=mapping_dict, addref=addref, autocommit=autocommit, rcpath=rcpath)

    def update_(self, sdata, dev, **kwargs):
      mapping_dict = kwargs.get('mapping_dict')

      #validating inputs and get payload object
      as_path_acl_object_list = self.validate_inputs_form_payload(mapping_dict, update=True)

      #convert keys to list
      number = mapping_dict.get('number')
      if not isinstance(number, list):
        number = [number]
      condition = mapping_dict.get('condition')
      if not isinstance(condition, list):
        condition = [condition]
      expression = mapping_dict.get('expression')
      if not isinstance(expression, list):
        expression = [expression]

      #prepare rcpath
      rcpath = kwargs.get('rcpath')
      rcpath_list = []
      for number_iterator in number:
        for condition_iterator in condition:
          for expression_iterator in expression:
            rcpath_tmp =  rcpath+"/as-path-acl=%s,%s,%s"%(util.make_interfacename(number_iterator),util.make_interfacename(condition_iterator),util.make_interfacename(expression_iterator))
            rcpath_list.append(rcpath_tmp)
      for rc_counter, as_path_acl_object in enumerate(as_path_acl_object_list):
        #fetch payload
        as_path_acl_payload = as_path_acl_object.getxml(filter=True)

        util.log_debug('update as_path_acl_payload %s'%as_path_acl_payload)

        rcpath = rcpath_list[rc_counter]
        #call the base abstract class for createData
        super(self.__class__, self).update_(sdata, dev, rcpath=rcpath, payload=as_path_acl_payload, key_hints=self.key_hints, addref=kwargs.get('addref', False), autocommit=kwargs.get('autocommit', True), mapping_dict=mapping_dict)

    def delete(self, sdata, dev, mapping_dict, fail_silently=False, remove_reference=False):
      dev = get_valid_devices(dev)
      if len(dev) == 0:
        return

      ##prepare rcpath
      rcpath = "l3features:as-path-acls"
      self.delete_(sdata, dev, mapping_dict=mapping_dict, fail_silently=fail_silently, remove_reference=remove_reference, rcpath=rcpath)

    def delete_(self, sdata, dev, **kwargs):
      mapping_dict = kwargs.get('mapping_dict')

      #validating inputs
      if util.isEmpty(mapping_dict.get('number')):
        raise Exception("'number' cannot be empty")
      if util.isEmpty(mapping_dict.get('condition')):
        raise Exception("'condition' cannot be empty")
      if util.isEmpty(mapping_dict.get('expression')):
        raise Exception("'expression' cannot be empty")

      #convert keys to list
      number = mapping_dict.get('number')
      if not isinstance(number, list):
        number = [number]
      condition = mapping_dict.get('condition')
      if not isinstance(condition, list):
        condition = [condition]
      expression = mapping_dict.get('expression')
      if not isinstance(expression, list):
        expression = [expression]

      #prepare rcpath
      rcpath = kwargs.get('rcpath')
      rcpath_list = []
      for number_iterator in number:
        for condition_iterator in condition:
          for expression_iterator in expression:
            rcpath_tmp =  rcpath+"/as-path-acl=%s,%s,%s"%(util.make_interfacename(number_iterator),util.make_interfacename(condition_iterator),util.make_interfacename(expression_iterator))
            rcpath_list.append(rcpath_tmp)
      payload = ''

      for rcpath in rcpath_list:
        #call the base abstract class for deleteData
        super(self.__class__, self).delete_(sdata, dev, rcpath=rcpath, payload=payload, fail_silently=kwargs.get('fail_silently', False), remove_reference=kwargs.get('remove_reference', False))

    def validate_inputs_form_payload(self, mapping_dict, update=False):
      #validating inputs
      if util.isEmpty(mapping_dict.get('number')):
        raise Exception("'number' cannot be empty")
      if util.isEmpty(mapping_dict.get('condition')):
        raise Exception("'condition' cannot be empty")
      if util.isEmpty(mapping_dict.get('expression')):
        raise Exception("'expression' cannot be empty")

      #convert keys to list
      number = mapping_dict.get('number')
      if not isinstance(number, list):
        number = [number]
      condition = mapping_dict.get('condition')
      if not isinstance(condition, list):
        condition = [condition]
      expression = mapping_dict.get('expression')
      if not isinstance(expression, list):
        expression = [expression]

      #prepare payload
      as_path_acl_object_list = []
      for number_iterator in number:
        for condition_iterator in condition:
          for expression_iterator in expression:
            from servicemodel.controller.devices.device import as_path_acls
            as_path_acl_object = as_path_acls.as_path_acl.as_path_acl()
            as_path_acl_object.number = number_iterator
            as_path_acl_object.condition = condition_iterator
            as_path_acl_object.expression = expression_iterator
            as_path_acl_object_list.append(as_path_acl_object)

      return as_path_acl_object_list

