#
# This computer program is the confidential information and proprietary trade
# secret of Anuta Networks, Inc. Possessions and use of this program must
# conform strictly to the license agreement between the user and
# Anuta Networks, Inc., and receipt or possession does not convey any rights
# to divulge, reproduce, or allow others to use this program without specific
# written authorization of Anuta Networks, Inc.
#
# Copyright (c) 2015-2016 Anuta Networks, Inc. All Rights Reserved.
#

#
#DO NOT EDIT THIS FILE ITS AUTOGENERATED ONE
#

from servicemodel import util
from servicemodel import yang
from servicemodel import devicemgr
from servicemodel.abstract_dev_mgr import AbstractDeviceMgr

def get_valid_devices(devs):
  vdevs = []
  if isinstance(devs, list):
    for dev in devs:
      drivername = dev.device.get_field_value('driver_name')
      if util.isEmpty(drivername):
        vdevs.append(dev)
  else:
    drivername = devs.device.get_field_value('driver_name')
    if util.isEmpty(drivername):
      vdevs.append(devs)

  return vdevs

class port_channels(object):
  #XPATH devices/device/port-channels/port-channel
  class port_channel(AbstractDeviceMgr):
    key_hints = [[]]
    def getRcpathPayload(self, sdata, dev, mapping_dict):
      dev = get_valid_devices(dev)
      if len(dev) == 0:
        return
      #convert parent keys to list
      rcpath_list = []
      payload_list = []
      ##prepare rcpath
      rcpath = "l2features:port-channels"
      rcpath_list.append(rcpath)
      mapping_dict = kwargs.get('mapping_dict')

      #validating inputs and get payload object
      port_channel_object_list = self.validate_inputs_form_payload(mapping_dict)

      for port_channel_object in port_channel_object_list:
        #fetch payload
        port_channel_payload = port_channel_object.getxml(filter=True)
        util.log_debug('port_channel_payload %s'%port_channel_payload)
        payload_list.append(port_channel_payload)

      return rcpath_list, payload_list

    def create(self, sdata, dev, mapping_dict, addref=True, autocommit=True):
      dev = get_valid_devices(dev)
      if len(dev) == 0:
        return
      #convert parent keys to list
      ##prepare rcpath
      rcpath = "l2features:port-channels"
      self.create_(sdata, dev, mapping_dict=mapping_dict, addref=addref, autocommit=autocommit, rcpath=rcpath)

    def create_(self, sdata, dev, **kwargs):
      mapping_dict = kwargs.get('mapping_dict')

      #validating inputs and get payload object
      port_channel_object_list = self.validate_inputs_form_payload(mapping_dict)

      for port_channel_object in port_channel_object_list:
        #fetch payload
        port_channel_payload = port_channel_object.getxml(filter=True)

        util.log_debug('port_channel_payload %s'%port_channel_payload)

        #call the base abstract class for createData
        super(self.__class__, self).create_(sdata, dev, rcpath=kwargs.get('rcpath'), payload=port_channel_payload, key_hints=self.key_hints, addref=kwargs.get('addref', True), autocommit=kwargs.get('autocommit', True), mapping_dict=mapping_dict)

    def update(self, sdata, dev, mapping_dict, addref=False, autocommit=True):
      dev = get_valid_devices(dev)
      if len(dev) == 0:
        return

      ##prepare rcpath
      rcpath = "l2features:port-channels"
      self.update_(sdata, dev, mapping_dict=mapping_dict, addref=addref, autocommit=autocommit, rcpath=rcpath)

    def update_(self, sdata, dev, **kwargs):
      mapping_dict = kwargs.get('mapping_dict')

      #validating inputs and get payload object
      port_channel_object_list = self.validate_inputs_form_payload(mapping_dict, update=True)

      #convert keys to list
      name = mapping_dict.get('name')
      if not isinstance(name, list):
        name = [name]

      #prepare rcpath
      rcpath = kwargs.get('rcpath')
      rcpath_list = []
      for name_iterator in name:
        rcpath_tmp =  rcpath+"/port-channel=%s"%(util.make_interfacename(name_iterator))
        rcpath_list.append(rcpath_tmp)
      for rc_counter, port_channel_object in enumerate(port_channel_object_list):
        #fetch payload
        port_channel_payload = port_channel_object.getxml(filter=True)

        util.log_debug('update port_channel_payload %s'%port_channel_payload)

        rcpath = rcpath_list[rc_counter]
        #call the base abstract class for createData
        super(self.__class__, self).update_(sdata, dev, rcpath=rcpath, payload=port_channel_payload, key_hints=self.key_hints, addref=kwargs.get('addref', False), autocommit=kwargs.get('autocommit', True), mapping_dict=mapping_dict)

    def delete(self, sdata, dev, mapping_dict, fail_silently=False, remove_reference=False):
      dev = get_valid_devices(dev)
      if len(dev) == 0:
        return

      ##prepare rcpath
      rcpath = "l2features:port-channels"
      self.delete_(sdata, dev, mapping_dict=mapping_dict, fail_silently=fail_silently, remove_reference=remove_reference, rcpath=rcpath)

    def delete_(self, sdata, dev, **kwargs):
      mapping_dict = kwargs.get('mapping_dict')

      #validating inputs
      if util.isEmpty(mapping_dict.get('name')):
        raise Exception("'name' cannot be empty")

      #convert keys to list
      name = mapping_dict.get('name')
      if not isinstance(name, list):
        name = [name]

      #prepare rcpath
      rcpath = kwargs.get('rcpath')
      rcpath_list = []
      for name_iterator in name:
        rcpath_tmp =  rcpath+"/port-channel=%s"%(util.make_interfacename(name_iterator))
        rcpath_list.append(rcpath_tmp)
      payload = ''

      for rcpath in rcpath_list:
        #call the base abstract class for deleteData
        super(self.__class__, self).delete_(sdata, dev, rcpath=rcpath, payload=payload, fail_silently=kwargs.get('fail_silently', False), remove_reference=kwargs.get('remove_reference', False))

    def validate_inputs_form_payload(self, mapping_dict, update=False):
      #validating inputs
      if util.isEmpty(mapping_dict.get('name')):
        raise Exception("'name' cannot be empty")

      #convert keys to list
      name = mapping_dict.get('name')
      if not isinstance(name, list):
        name = [name]

      #prepare payload
      port_channel_object_list = []
      for name_iterator in name:
        from servicemodel.controller.devices.device import port_channels
        port_channel_object = port_channels.port_channel.port_channel()
        try:
          if (update == False) or (update == True and str(mapping_dict.get('po_index', None)) != ''):
            port_channel_object.po_index = mapping_dict.get('po_index', None)
          else:
            port_channel_object.po_index._empty_tag = True
        except TypeError:
          pass
        port_channel_object.name = name_iterator
        try:
          if (update == False) or (update == True and str(mapping_dict.get('description', None)) != ''):
            port_channel_object.description = mapping_dict.get('description', None)
          else:
            port_channel_object.description._empty_tag = True
        except TypeError:
          pass
        try:
          if (update == False) or (update == True and str(mapping_dict.get('member_ports', None)) != ''):
            port_channel_object.member_ports = mapping_dict.get('member_ports', None)
          else:
            port_channel_object.member_ports._empty_tag = True
        except TypeError:
          pass
        try:
          if (update == False) or (update == True and str(mapping_dict.get('clag_agg_ports_list', None)) != ''):
            port_channel_object.clag_agg_ports_list = mapping_dict.get('clag_agg_ports_list', None)
          else:
            port_channel_object.clag_agg_ports_list._empty_tag = True
        except TypeError:
          pass
        port_channel_object_list.append(port_channel_object)

      return port_channel_object_list

    #XPATH devices/device/port-channels/port-channel/device-interface
    class device_interface(AbstractDeviceMgr):
      key_hints = [['name']]
      def getRcpathPayload(self, sdata, dev, port_channel_name, mapping_dict):
        dev = get_valid_devices(dev)
        if len(dev) == 0:
          return
        #Input Key Validations
        self.validate_parent_keys( port_channel_name)

        #convert parent keys to list
        rcpath_list = []
        payload_list = []
        if not isinstance(port_channel_name, list):
          port_channel_name_list = [port_channel_name]
        else:
          port_channel_name_list = port_channel_name

        for port_channel_name in port_channel_name_list:
          ##prepare rcpath
          rcpath = "l2features:port-channels/port-channel=%s"%(util.make_interfacename(port_channel_name))
          rcpath_list.append(rcpath)
        mapping_dict = kwargs.get('mapping_dict')

        #validating inputs and get payload object
        device_interface_object_list = self.validate_inputs_form_payload(mapping_dict)

        for device_interface_object in device_interface_object_list:
          #fetch payload
          device_interface_payload = device_interface_object.getxml(filter=True)
          util.log_debug('device_interface_payload %s'%device_interface_payload)
          payload_list.append(device_interface_payload)

        return rcpath_list, payload_list

      def create(self, sdata, dev, port_channel_name, mapping_dict, addref=True, autocommit=True):
        dev = get_valid_devices(dev)
        if len(dev) == 0:
          return
        #Input Key Validations
        self.validate_parent_keys( port_channel_name)

        #convert parent keys to list
        if not isinstance(port_channel_name, list):
          port_channel_name_list = [port_channel_name]
        else:
          port_channel_name_list = port_channel_name

        for port_channel_name in port_channel_name_list:
          ##prepare rcpath
          rcpath = "l2features:port-channels/port-channel=%s"%(util.make_interfacename(port_channel_name))
          self.create_(sdata, dev, mapping_dict=mapping_dict, addref=addref, autocommit=autocommit, rcpath=rcpath)

      def create_(self, sdata, dev, **kwargs):
        mapping_dict = kwargs.get('mapping_dict')

        #validating inputs and get payload object
        device_interface_object_list = self.validate_inputs_form_payload(mapping_dict)

        for device_interface_object in device_interface_object_list:
          #fetch payload
          device_interface_payload = device_interface_object.getxml(filter=True)

          util.log_debug('device_interface_payload %s'%device_interface_payload)

          #call the base abstract class for createData
          super(self.__class__, self).create_(sdata, dev, rcpath=kwargs.get('rcpath'), payload=device_interface_payload, key_hints=self.key_hints, addref=kwargs.get('addref', True), autocommit=kwargs.get('autocommit', True), mapping_dict=mapping_dict)

      def update(self, sdata, dev, port_channel_name, mapping_dict, addref=True, autocommit=True):
        dev = get_valid_devices(dev)
        if len(dev) == 0:
          return
        #Input Key Validations
        self.validate_parent_keys( port_channel_name)


        ##prepare rcpath
        rcpath = "l2features:port-channels/port-channel=%s"%(util.make_interfacename(port_channel_name))
        self.update_(sdata, dev, mapping_dict=mapping_dict, addref=addref, autocommit=autocommit, rcpath=rcpath)

      def update_(self, sdata, dev, **kwargs):
        mapping_dict = kwargs.get('mapping_dict')

        #validating inputs and get payload object
        device_interface_object_list = self.validate_inputs_form_payload(mapping_dict, update=True)

        #convert keys to list
        interface = mapping_dict.get('interface')
        if not isinstance(interface, list):
          interface = [interface]

        #prepare rcpath
        rcpath = kwargs.get('rcpath')
        rcpath_list = []
        for interface_iterator in interface:
          rcpath_tmp =  rcpath+"/device-interface=%s"%(util.make_interfacename(interface_iterator))
          rcpath_list.append(rcpath_tmp)
        for rc_counter, device_interface_object in enumerate(device_interface_object_list):
          #fetch payload
          device_interface_payload = device_interface_object.getxml(filter=True)

          util.log_debug('update device_interface_payload %s'%device_interface_payload)

          rcpath = rcpath_list[rc_counter]
          #call the base abstract class for createData
          super(self.__class__, self).update_(sdata, dev, rcpath=rcpath, payload=device_interface_payload, key_hints=self.key_hints, addref=kwargs.get('addref', False), autocommit=kwargs.get('autocommit', True), mapping_dict=mapping_dict)

      def delete(self, sdata, dev, port_channel_name, mapping_dict, fail_silently=False, remove_reference=False):
        dev = get_valid_devices(dev)
        if len(dev) == 0:
          return
        #Input Key Validations
        self.validate_parent_keys( port_channel_name)


        if not isinstance(port_channel_name, list):
          port_channel_name_list = [port_channel_name]
        else:
          port_channel_name_list = port_channel_name

        for port_channel_name in port_channel_name_list:
          ##prepare rcpath
          rcpath = "l2features:port-channels/port-channel=%s"%(util.make_interfacename(port_channel_name))
          self.delete_(sdata, dev, mapping_dict=mapping_dict, fail_silently=fail_silently, remove_reference=remove_reference, rcpath=rcpath)

      def delete_(self, sdata, dev, **kwargs):
        mapping_dict = kwargs.get('mapping_dict')

        #validating inputs
        if util.isEmpty(mapping_dict.get('interface')):
          raise Exception("'interface' cannot be empty")

        #convert keys to list
        interface = mapping_dict.get('interface')
        if not isinstance(interface, list):
          interface = [interface]

        #prepare rcpath
        rcpath = kwargs.get('rcpath')
        rcpath_list = []
        for interface_iterator in interface:
          rcpath_tmp =  rcpath+"/device-interface=%s"%(util.make_interfacename(interface_iterator))
          rcpath_list.append(rcpath_tmp)
        payload = ''

        for rcpath in rcpath_list:
          #call the base abstract class for deleteData
          super(self.__class__, self).delete_(sdata, dev, rcpath=rcpath, payload=payload, fail_silently=kwargs.get('fail_silently', False), remove_reference=kwargs.get('remove_reference', False))

      def validate_parent_keys(self, port_channel_name):
        #Parent Key Validations
        if util.isEmpty(port_channel_name):
          raise Exception("'port_channel_name' cannot be empty")

      def validate_inputs_form_payload(self, mapping_dict, update=False):
        #validating inputs
        if util.isEmpty(mapping_dict.get('interface')):
          raise Exception("'interface' cannot be empty")

        #convert keys to list
        interface = mapping_dict.get('interface')
        if not isinstance(interface, list):
          interface = [interface]

        #prepare payload
        device_interface_object_list = []
        for interface_iterator in interface:
          from servicemodel.controller.devices.device.port_channels import port_channel
          device_interface_object = port_channel.device_interface.device_interface()
          device_interface_object.interface = interface_iterator
          device_interface_object_list.append(device_interface_object)

        return device_interface_object_list

