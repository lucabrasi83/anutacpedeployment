#
# This computer program is the confidential information and proprietary trade
# secret of Anuta Networks, Inc. Possessions and use of this program must
# conform strictly to the license agreement between the user and
# Anuta Networks, Inc., and receipt or possession does not convey any rights
# to divulge, reproduce, or allow others to use this program without specific
# written authorization of Anuta Networks, Inc.
#
# Copyright (c) 2015-2016 Anuta Networks, Inc. All Rights Reserved.
#

#
#DO NOT EDIT THIS FILE ITS AUTOGENERATED ONE
#

from servicemodel import util
from servicemodel import yang
from servicemodel import devicemgr
from servicemodel.abstract_dev_mgr import AbstractDeviceMgr

def get_valid_devices(devs):
  vdevs = []
  if isinstance(devs, list):
    for dev in devs:
      drivername = dev.device.get_field_value('driver_name')
      if util.isEmpty(drivername):
        vdevs.append(dev)
  else:
    drivername = devs.device.get_field_value('driver_name')
    if util.isEmpty(drivername):
      vdevs.append(devs)

  return vdevs

class vn_paths(object):
  #XPATH devices/device/vn-paths/vn-path
  class vn_path(AbstractDeviceMgr):
    key_hints = [[]]
    def getRcpathPayload(self, sdata, dev, mapping_dict):
      dev = get_valid_devices(dev)
      if len(dev) == 0:
        return
      #convert parent keys to list
      rcpath_list = []
      payload_list = []
      ##prepare rcpath
      rcpath = "l2features:vn-paths"
      rcpath_list.append(rcpath)
      mapping_dict = kwargs.get('mapping_dict')

      #validating inputs and get payload object
      vn_path_object_list = self.validate_inputs_form_payload(mapping_dict)

      for vn_path_object in vn_path_object_list:
        #fetch payload
        vn_path_payload = vn_path_object.getxml(filter=True)
        util.log_debug('vn_path_payload %s'%vn_path_payload)
        payload_list.append(vn_path_payload)

      return rcpath_list, payload_list

    def create(self, sdata, dev, mapping_dict, addref=True, autocommit=True):
      dev = get_valid_devices(dev)
      if len(dev) == 0:
        return
      #convert parent keys to list
      ##prepare rcpath
      rcpath = "l2features:vn-paths"
      self.create_(sdata, dev, mapping_dict=mapping_dict, addref=addref, autocommit=autocommit, rcpath=rcpath)

    def create_(self, sdata, dev, **kwargs):
      mapping_dict = kwargs.get('mapping_dict')

      #validating inputs and get payload object
      vn_path_object_list = self.validate_inputs_form_payload(mapping_dict)

      for vn_path_object in vn_path_object_list:
        #fetch payload
        vn_path_payload = vn_path_object.getxml(filter=True)

        util.log_debug('vn_path_payload %s'%vn_path_payload)

        #call the base abstract class for createData
        super(self.__class__, self).create_(sdata, dev, rcpath=kwargs.get('rcpath'), payload=vn_path_payload, key_hints=self.key_hints, addref=kwargs.get('addref', True), autocommit=kwargs.get('autocommit', True), mapping_dict=mapping_dict)

    def update(self, sdata, dev, mapping_dict, addref=False, autocommit=True):
      dev = get_valid_devices(dev)
      if len(dev) == 0:
        return

      ##prepare rcpath
      rcpath = "l2features:vn-paths"
      self.update_(sdata, dev, mapping_dict=mapping_dict, addref=addref, autocommit=autocommit, rcpath=rcpath)

    def update_(self, sdata, dev, **kwargs):
      mapping_dict = kwargs.get('mapping_dict')

      #validating inputs and get payload object
      vn_path_object_list = self.validate_inputs_form_payload(mapping_dict, update=True)

      #convert keys to list
      path_name = mapping_dict.get('path_name')
      if not isinstance(path_name, list):
        path_name = [path_name]

      #prepare rcpath
      rcpath = kwargs.get('rcpath')
      rcpath_list = []
      for path_name_iterator in path_name:
        rcpath_tmp =  rcpath+"/vn-path=%s"%(util.make_interfacename(path_name_iterator))
        rcpath_list.append(rcpath_tmp)
      for rc_counter, vn_path_object in enumerate(vn_path_object_list):
        #fetch payload
        vn_path_payload = vn_path_object.getxml(filter=True)

        util.log_debug('update vn_path_payload %s'%vn_path_payload)

        rcpath = rcpath_list[rc_counter]
        #call the base abstract class for createData
        super(self.__class__, self).update_(sdata, dev, rcpath=rcpath, payload=vn_path_payload, key_hints=self.key_hints, addref=kwargs.get('addref', False), autocommit=kwargs.get('autocommit', True), mapping_dict=mapping_dict)

    def delete(self, sdata, dev, mapping_dict, fail_silently=False, remove_reference=False):
      dev = get_valid_devices(dev)
      if len(dev) == 0:
        return

      ##prepare rcpath
      rcpath = "l2features:vn-paths"
      self.delete_(sdata, dev, mapping_dict=mapping_dict, fail_silently=fail_silently, remove_reference=remove_reference, rcpath=rcpath)

    def delete_(self, sdata, dev, **kwargs):
      mapping_dict = kwargs.get('mapping_dict')

      #validating inputs
      if util.isEmpty(mapping_dict.get('path_name')):
        raise Exception("'path_name' cannot be empty")

      #convert keys to list
      path_name = mapping_dict.get('path_name')
      if not isinstance(path_name, list):
        path_name = [path_name]

      #prepare rcpath
      rcpath = kwargs.get('rcpath')
      rcpath_list = []
      for path_name_iterator in path_name:
        rcpath_tmp =  rcpath+"/vn-path=%s"%(util.make_interfacename(path_name_iterator))
        rcpath_list.append(rcpath_tmp)
      payload = ''

      for rcpath in rcpath_list:
        #call the base abstract class for deleteData
        super(self.__class__, self).delete_(sdata, dev, rcpath=rcpath, payload=payload, fail_silently=kwargs.get('fail_silently', False), remove_reference=kwargs.get('remove_reference', False))

    def validate_inputs_form_payload(self, mapping_dict, update=False):
      #validating inputs
      if util.isEmpty(mapping_dict.get('path_name')):
        raise Exception("'path_name' cannot be empty")

      #convert keys to list
      path_name = mapping_dict.get('path_name')
      if not isinstance(path_name, list):
        path_name = [path_name]

      #prepare payload
      vn_path_object_list = []
      for path_name_iterator in path_name:
        from servicemodel.controller.devices.device import vn_paths
        vn_path_object = vn_paths.vn_path.vn_path()
        vn_path_object.path_name = path_name_iterator
        try:
          if (update == False) or (update == True and str(mapping_dict.get('node_name', None)) != ''):
            vn_path_object.node_name = mapping_dict.get('node_name', None)
          else:
            vn_path_object.node_name._empty_tag = True
        except TypeError:
          pass
        try:
          if (update == False) or (update == True and str(mapping_dict.get('security_profile_name', None)) != ''):
            vn_path_object.security_profile_name = mapping_dict.get('security_profile_name', None)
          else:
            vn_path_object.security_profile_name._empty_tag = True
        except TypeError:
          pass
        try:
          if (update == False) or (update == True and str(mapping_dict.get('order', None)) != ''):
            vn_path_object.order = mapping_dict.get('order', None)
          else:
            vn_path_object.order._empty_tag = True
        except TypeError:
          pass
        try:
          if (update == False) or (update == True and str(mapping_dict.get('port_profile_name', None)) != ''):
            vn_path_object.port_profile_name = mapping_dict.get('port_profile_name', None)
          else:
            vn_path_object.port_profile_name._empty_tag = True
        except TypeError:
          pass
        vn_path_object_list.append(vn_path_object)

      return vn_path_object_list

