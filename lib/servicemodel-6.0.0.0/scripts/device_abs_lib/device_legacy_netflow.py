#
# This computer program is the confidential information and proprietary trade
# secret of Anuta Networks, Inc. Possessions and use of this program must
# conform strictly to the license agreement between the user and
# Anuta Networks, Inc., and receipt or possession does not convey any rights
# to divulge, reproduce, or allow others to use this program without specific
# written authorization of Anuta Networks, Inc.
#
# Copyright (c) 2015-2016 Anuta Networks, Inc. All Rights Reserved.
#

#
#DO NOT EDIT THIS FILE ITS AUTOGENERATED ONE
#

from servicemodel import util
from servicemodel import yang
from servicemodel import devicemgr
from servicemodel.abstract_dev_mgr import AbstractDeviceMgr

def get_valid_devices(devs):
  vdevs = []
  if isinstance(devs, list):
    for dev in devs:
      drivername = dev.device.get_field_value('driver_name')
      if util.isEmpty(drivername):
        vdevs.append(dev)
  else:
    drivername = devs.device.get_field_value('driver_name')
    if util.isEmpty(drivername):
      vdevs.append(devs)

  return vdevs

#XPATH devices/device/legacy-netflow
class legacy_netflow(AbstractDeviceMgr):
  key_hints = [[]]
  def getRcpathPayload(self, sdata, dev, mapping_dict):
    dev = get_valid_devices(dev)
    if len(dev) == 0:
      return
    #convert parent keys to list
    rcpath_list = []
    payload_list = []
    ##prepare rcpath
    rcpath = ""
    rcpath_list.append(rcpath)
    mapping_dict = kwargs.get('mapping_dict')

    #validating inputs and get payload object
    legacy_netflow_object_list = self.validate_inputs_form_payload(mapping_dict)

    for legacy_netflow_object in legacy_netflow_object_list:
      #fetch payload
      legacy_netflow_payload = legacy_netflow_object.getxml(filter=True)
      util.log_debug('legacy_netflow_payload %s'%legacy_netflow_payload)
      payload_list.append(legacy_netflow_payload)

    return rcpath_list, payload_list

  def create(self, sdata, dev, mapping_dict, addref=True, autocommit=True):
    dev = get_valid_devices(dev)
    if len(dev) == 0:
      return
    #convert parent keys to list
    ##prepare rcpath
    rcpath = ""
    self.create_(sdata, dev, mapping_dict=mapping_dict, addref=addref, autocommit=autocommit, rcpath=rcpath)

  def create_(self, sdata, dev, **kwargs):
    mapping_dict = kwargs.get('mapping_dict')

    #validating inputs and get payload object
    legacy_netflow_object_list = self.validate_inputs_form_payload(mapping_dict)

    for legacy_netflow_object in legacy_netflow_object_list:
      #fetch payload
      legacy_netflow_payload = legacy_netflow_object.getxml(filter=True)

      util.log_debug('legacy_netflow_payload %s'%legacy_netflow_payload)

      #call the base abstract class for createData
      super(self.__class__, self).create_(sdata, dev, rcpath=kwargs.get('rcpath'), payload=legacy_netflow_payload, key_hints=self.key_hints, addref=kwargs.get('addref', True), autocommit=kwargs.get('autocommit', True), mapping_dict=mapping_dict)

  def update(self, sdata, dev, mapping_dict, addref=False, autocommit=True):
    dev = get_valid_devices(dev)
    if len(dev) == 0:
      return

    ##prepare rcpath
    rcpath = ""
    self.update_(sdata, dev, mapping_dict=mapping_dict, addref=addref, autocommit=autocommit, rcpath=rcpath)

  def update_(self, sdata, dev, **kwargs):
    mapping_dict = kwargs.get('mapping_dict')

    #validating inputs and get payload object
    legacy_netflow_object_list = self.validate_inputs_form_payload(mapping_dict, update=True)

    #convert keys to list

    #prepare rcpath
    rcpath = kwargs.get('rcpath')
    rcpath_list = []
    rcpath_tmp =  rcpath+"/legacy-netflow"
    rcpath_list.append(rcpath_tmp)
    for rc_counter, legacy_netflow_object in enumerate(legacy_netflow_object_list):
      #fetch payload
      legacy_netflow_payload = legacy_netflow_object.getxml(filter=True)

      util.log_debug('update legacy_netflow_payload %s'%legacy_netflow_payload)

      rcpath = rcpath_list[rc_counter]
      #call the base abstract class for createData
      super(self.__class__, self).update_(sdata, dev, rcpath=rcpath, payload=legacy_netflow_payload, key_hints=self.key_hints, addref=kwargs.get('addref', False), autocommit=kwargs.get('autocommit', True), mapping_dict=mapping_dict)

  def delete(self, sdata, dev, mapping_dict, fail_silently=False, remove_reference=False):
    dev = get_valid_devices(dev)
    if len(dev) == 0:
      return

    ##prepare rcpath
    rcpath = ""
    self.delete_(sdata, dev, mapping_dict=mapping_dict, fail_silently=fail_silently, remove_reference=remove_reference, rcpath=rcpath)

  def delete_(self, sdata, dev, **kwargs):
    mapping_dict = kwargs.get('mapping_dict')

    #validating inputs

    #convert keys to list

    #prepare rcpath
    rcpath = kwargs.get('rcpath')
    rcpath_list = []
    rcpath_tmp =  rcpath+"/legacy-netflow"
    rcpath_list.append(rcpath_tmp)
    payload = ''

    for rcpath in rcpath_list:
      #call the base abstract class for deleteData
      super(self.__class__, self).delete_(sdata, dev, rcpath=rcpath, payload=payload, fail_silently=kwargs.get('fail_silently', False), remove_reference=kwargs.get('remove_reference', False))

  def validate_inputs_form_payload(self, mapping_dict, update=False):
    #validating inputs

    #convert keys to list

    #prepare payload
    legacy_netflow_object_list = []
    from servicemodel.controller.devices import device
    legacy_netflow_object = device.legacy_netflow.legacy_netflow()
    try:
      if (update == False) or (update == True and str(mapping_dict.get('source', None)) != ''):
        legacy_netflow_object.source = mapping_dict.get('source', None)
      else:
        legacy_netflow_object.source._empty_tag = True
    except TypeError:
      pass
    try:
      if (update == False) or (update == True and str(mapping_dict.get('version', None)) != ''):
        legacy_netflow_object.version = mapping_dict.get('version', None)
      else:
        legacy_netflow_object.version._empty_tag = True
    except TypeError:
      pass
    try:
      if (update == False) or (update == True and str(mapping_dict.get('interface_names', None)) != ''):
        legacy_netflow_object.interface_names = mapping_dict.get('interface_names', None)
      else:
        legacy_netflow_object.interface_names._empty_tag = True
    except TypeError:
      pass
    legacy_netflow_object_list.append(legacy_netflow_object)

    return legacy_netflow_object_list

  #XPATH devices/device/legacy-netflow/destinations
  class destinations(AbstractDeviceMgr):
    key_hints = [[]]
    def getRcpathPayload(self, sdata, dev, mapping_dict):
      dev = get_valid_devices(dev)
      if len(dev) == 0:
        return
      #convert parent keys to list
      rcpath_list = []
      payload_list = []
      ##prepare rcpath
      rcpath = "l3features:legacy-netflow"
      rcpath_list.append(rcpath)
      mapping_dict = kwargs.get('mapping_dict')

      #validating inputs and get payload object
      destinations_object_list = self.validate_inputs_form_payload(mapping_dict)

      for destinations_object in destinations_object_list:
        #fetch payload
        destinations_payload = destinations_object.getxml(filter=True)
        util.log_debug('destinations_payload %s'%destinations_payload)
        payload_list.append(destinations_payload)

      return rcpath_list, payload_list

    def create(self, sdata, dev, mapping_dict, addref=True, autocommit=True):
      dev = get_valid_devices(dev)
      if len(dev) == 0:
        return
      #convert parent keys to list
      ##prepare rcpath
      rcpath = "l3features:legacy-netflow"
      self.create_(sdata, dev, mapping_dict=mapping_dict, addref=addref, autocommit=autocommit, rcpath=rcpath)

    def create_(self, sdata, dev, **kwargs):
      mapping_dict = kwargs.get('mapping_dict')

      #validating inputs and get payload object
      destinations_object_list = self.validate_inputs_form_payload(mapping_dict)

      for destinations_object in destinations_object_list:
        #fetch payload
        destinations_payload = destinations_object.getxml(filter=True)

        util.log_debug('destinations_payload %s'%destinations_payload)

        #call the base abstract class for createData
        super(self.__class__, self).create_(sdata, dev, rcpath=kwargs.get('rcpath'), payload=destinations_payload, key_hints=self.key_hints, addref=kwargs.get('addref', True), autocommit=kwargs.get('autocommit', True), mapping_dict=mapping_dict)

    def update(self, sdata, dev, mapping_dict, addref=False, autocommit=True):
      dev = get_valid_devices(dev)
      if len(dev) == 0:
        return

      ##prepare rcpath
      rcpath = "l3features:legacy-netflow"
      self.update_(sdata, dev, mapping_dict=mapping_dict, addref=addref, autocommit=autocommit, rcpath=rcpath)

    def update_(self, sdata, dev, **kwargs):
      mapping_dict = kwargs.get('mapping_dict')

      #validating inputs and get payload object
      destinations_object_list = self.validate_inputs_form_payload(mapping_dict, update=True)

      #convert keys to list
      destination_ip_address = mapping_dict.get('destination_ip_address')
      if not isinstance(destination_ip_address, list):
        destination_ip_address = [destination_ip_address]

      #prepare rcpath
      rcpath = kwargs.get('rcpath')
      rcpath_list = []
      for destination_ip_address_iterator in destination_ip_address:
        rcpath_tmp =  rcpath+"/destinations=%s"%(util.make_interfacename(destination_ip_address_iterator))
        rcpath_list.append(rcpath_tmp)
      for rc_counter, destinations_object in enumerate(destinations_object_list):
        #fetch payload
        destinations_payload = destinations_object.getxml(filter=True)

        util.log_debug('update destinations_payload %s'%destinations_payload)

        rcpath = rcpath_list[rc_counter]
        #call the base abstract class for createData
        super(self.__class__, self).update_(sdata, dev, rcpath=rcpath, payload=destinations_payload, key_hints=self.key_hints, addref=kwargs.get('addref', False), autocommit=kwargs.get('autocommit', True), mapping_dict=mapping_dict)

    def delete(self, sdata, dev, mapping_dict, fail_silently=False, remove_reference=False):
      dev = get_valid_devices(dev)
      if len(dev) == 0:
        return

      ##prepare rcpath
      rcpath = "l3features:legacy-netflow"
      self.delete_(sdata, dev, mapping_dict=mapping_dict, fail_silently=fail_silently, remove_reference=remove_reference, rcpath=rcpath)

    def delete_(self, sdata, dev, **kwargs):
      mapping_dict = kwargs.get('mapping_dict')

      #validating inputs
      if util.isEmpty(mapping_dict.get('destination_ip_address')):
        raise Exception("'destination_ip_address' cannot be empty")

      #convert keys to list
      destination_ip_address = mapping_dict.get('destination_ip_address')
      if not isinstance(destination_ip_address, list):
        destination_ip_address = [destination_ip_address]

      #prepare rcpath
      rcpath = kwargs.get('rcpath')
      rcpath_list = []
      for destination_ip_address_iterator in destination_ip_address:
        rcpath_tmp =  rcpath+"/destinations=%s"%(util.make_interfacename(destination_ip_address_iterator))
        rcpath_list.append(rcpath_tmp)
      payload = ''

      for rcpath in rcpath_list:
        #call the base abstract class for deleteData
        super(self.__class__, self).delete_(sdata, dev, rcpath=rcpath, payload=payload, fail_silently=kwargs.get('fail_silently', False), remove_reference=kwargs.get('remove_reference', False))

    def validate_inputs_form_payload(self, mapping_dict, update=False):
      #validating inputs
      if util.isEmpty(mapping_dict.get('destination_ip_address')):
        raise Exception("'destination_ip_address' cannot be empty")

      #convert keys to list
      destination_ip_address = mapping_dict.get('destination_ip_address')
      if not isinstance(destination_ip_address, list):
        destination_ip_address = [destination_ip_address]

      #prepare payload
      destinations_object_list = []
      for destination_ip_address_iterator in destination_ip_address:
        from servicemodel.controller.devices.device import legacy_netflow
        destinations_object = legacy_netflow.destinations.destinations()
        destinations_object.destination_ip_address = destination_ip_address_iterator
        try:
          if (update == False) or (update == True and str(mapping_dict.get('port', None)) != ''):
            destinations_object.port = mapping_dict.get('port', None)
          else:
            destinations_object.port._empty_tag = True
        except TypeError:
          pass
        destinations_object_list.append(destinations_object)

      return destinations_object_list

