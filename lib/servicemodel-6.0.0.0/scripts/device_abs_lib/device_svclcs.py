#
# This computer program is the confidential information and proprietary trade
# secret of Anuta Networks, Inc. Possessions and use of this program must
# conform strictly to the license agreement between the user and
# Anuta Networks, Inc., and receipt or possession does not convey any rights
# to divulge, reproduce, or allow others to use this program without specific
# written authorization of Anuta Networks, Inc.
#
# Copyright (c) 2015-2016 Anuta Networks, Inc. All Rights Reserved.
#

#
#DO NOT EDIT THIS FILE ITS AUTOGENERATED ONE
#

from servicemodel import util
from servicemodel import yang
from servicemodel import devicemgr
from servicemodel.abstract_dev_mgr import AbstractDeviceMgr

def get_valid_devices(devs):
  vdevs = []
  if isinstance(devs, list):
    for dev in devs:
      drivername = dev.device.get_field_value('driver_name')
      if util.isEmpty(drivername):
        vdevs.append(dev)
  else:
    drivername = devs.device.get_field_value('driver_name')
    if util.isEmpty(drivername):
      vdevs.append(devs)

  return vdevs

class svclcs(object):
  #XPATH devices/device/svclcs/svclc
  class svclc(AbstractDeviceMgr):
    key_hints = [[]]
    def getRcpathPayload(self, sdata, dev, mapping_dict):
      dev = get_valid_devices(dev)
      if len(dev) == 0:
        return
      #convert parent keys to list
      rcpath_list = []
      payload_list = []
      ##prepare rcpath
      rcpath = "firewall:svclcs"
      rcpath_list.append(rcpath)
      mapping_dict = kwargs.get('mapping_dict')

      #validating inputs and get payload object
      svclc_object_list = self.validate_inputs_form_payload(mapping_dict)

      for svclc_object in svclc_object_list:
        #fetch payload
        svclc_payload = svclc_object.getxml(filter=True)
        util.log_debug('svclc_payload %s'%svclc_payload)
        payload_list.append(svclc_payload)

      return rcpath_list, payload_list

    def create(self, sdata, dev, mapping_dict, addref=True, autocommit=True):
      dev = get_valid_devices(dev)
      if len(dev) == 0:
        return
      #convert parent keys to list
      ##prepare rcpath
      rcpath = "firewall:svclcs"
      self.create_(sdata, dev, mapping_dict=mapping_dict, addref=addref, autocommit=autocommit, rcpath=rcpath)

    def create_(self, sdata, dev, **kwargs):
      mapping_dict = kwargs.get('mapping_dict')

      #validating inputs and get payload object
      svclc_object_list = self.validate_inputs_form_payload(mapping_dict)

      for svclc_object in svclc_object_list:
        #fetch payload
        svclc_payload = svclc_object.getxml(filter=True)

        util.log_debug('svclc_payload %s'%svclc_payload)

        #call the base abstract class for createData
        super(self.__class__, self).create_(sdata, dev, rcpath=kwargs.get('rcpath'), payload=svclc_payload, key_hints=self.key_hints, addref=kwargs.get('addref', True), autocommit=kwargs.get('autocommit', True), mapping_dict=mapping_dict)

    def update(self, sdata, dev, mapping_dict, addref=False, autocommit=True):
      dev = get_valid_devices(dev)
      if len(dev) == 0:
        return

      ##prepare rcpath
      rcpath = "firewall:svclcs"
      self.update_(sdata, dev, mapping_dict=mapping_dict, addref=addref, autocommit=autocommit, rcpath=rcpath)

    def update_(self, sdata, dev, **kwargs):
      mapping_dict = kwargs.get('mapping_dict')

      #validating inputs and get payload object
      svclc_object_list = self.validate_inputs_form_payload(mapping_dict, update=True)

      #convert keys to list
      group_number = mapping_dict.get('group_number')
      if not isinstance(group_number, list):
        group_number = [group_number]
      module_number = mapping_dict.get('module_number')
      if not isinstance(module_number, list):
        module_number = [module_number]

      #prepare rcpath
      rcpath = kwargs.get('rcpath')
      rcpath_list = []
      for group_number_iterator in group_number:
        for module_number_iterator in module_number:
          rcpath_tmp =  rcpath+"/svclc=%s,%s"%(util.make_interfacename(group_number_iterator),util.make_interfacename(module_number_iterator))
          rcpath_list.append(rcpath_tmp)
      for rc_counter, svclc_object in enumerate(svclc_object_list):
        #fetch payload
        svclc_payload = svclc_object.getxml(filter=True)

        util.log_debug('update svclc_payload %s'%svclc_payload)

        rcpath = rcpath_list[rc_counter]
        #call the base abstract class for createData
        super(self.__class__, self).update_(sdata, dev, rcpath=rcpath, payload=svclc_payload, key_hints=self.key_hints, addref=kwargs.get('addref', False), autocommit=kwargs.get('autocommit', True), mapping_dict=mapping_dict)

    def delete(self, sdata, dev, mapping_dict, fail_silently=False, remove_reference=False):
      dev = get_valid_devices(dev)
      if len(dev) == 0:
        return

      ##prepare rcpath
      rcpath = "firewall:svclcs"
      self.delete_(sdata, dev, mapping_dict=mapping_dict, fail_silently=fail_silently, remove_reference=remove_reference, rcpath=rcpath)

    def delete_(self, sdata, dev, **kwargs):
      mapping_dict = kwargs.get('mapping_dict')

      #validating inputs
      if util.isEmpty(mapping_dict.get('group_number')):
        raise Exception("'group_number' cannot be empty")
      if util.isEmpty(mapping_dict.get('module_number')):
        raise Exception("'module_number' cannot be empty")

      #convert keys to list
      group_number = mapping_dict.get('group_number')
      if not isinstance(group_number, list):
        group_number = [group_number]
      module_number = mapping_dict.get('module_number')
      if not isinstance(module_number, list):
        module_number = [module_number]

      #prepare rcpath
      rcpath = kwargs.get('rcpath')
      rcpath_list = []
      for group_number_iterator in group_number:
        for module_number_iterator in module_number:
          rcpath_tmp =  rcpath+"/svclc=%s,%s"%(util.make_interfacename(group_number_iterator),util.make_interfacename(module_number_iterator))
          rcpath_list.append(rcpath_tmp)
      payload = ''

      for rcpath in rcpath_list:
        #call the base abstract class for deleteData
        super(self.__class__, self).delete_(sdata, dev, rcpath=rcpath, payload=payload, fail_silently=kwargs.get('fail_silently', False), remove_reference=kwargs.get('remove_reference', False))

    def validate_inputs_form_payload(self, mapping_dict, update=False):
      #validating inputs
      if util.isEmpty(mapping_dict.get('group_number')):
        raise Exception("'group_number' cannot be empty")
      if util.isEmpty(mapping_dict.get('module_number')):
        raise Exception("'module_number' cannot be empty")

      #convert keys to list
      group_number = mapping_dict.get('group_number')
      if not isinstance(group_number, list):
        group_number = [group_number]
      module_number = mapping_dict.get('module_number')
      if not isinstance(module_number, list):
        module_number = [module_number]

      #prepare payload
      svclc_object_list = []
      for group_number_iterator in group_number:
        for module_number_iterator in module_number:
          from servicemodel.controller.devices.device import svclcs
          svclc_object = svclcs.svclc.svclc()
          svclc_object.group_number = group_number_iterator
          try:
            if (update == False) or (update == True and str(mapping_dict.get('switch_number', None)) != ''):
              svclc_object.switch_number = mapping_dict.get('switch_number', None)
            else:
              svclc_object.switch_number._empty_tag = True
          except TypeError:
            pass
          svclc_object.module_number = module_number_iterator
          svclc_object_list.append(svclc_object)

      return svclc_object_list

