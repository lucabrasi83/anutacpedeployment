#
# This computer program is the confidential information and proprietary trade
# secret of Anuta Networks, Inc. Possessions and use of this program must
# conform strictly to the license agreement between the user and
# Anuta Networks, Inc., and receipt or possession does not convey any rights
# to divulge, reproduce, or allow others to use this program without specific
# written authorization of Anuta Networks, Inc.
#
# Copyright (c) 2015-2016 Anuta Networks, Inc. All Rights Reserved.
#

#
#DO NOT EDIT THIS FILE ITS AUTOGENERATED ONE
#

from servicemodel import util
from servicemodel import yang
from servicemodel import devicemgr
from servicemodel.abstract_dev_mgr import AbstractDeviceMgr

def get_valid_devices(devs):
  vdevs = []
  if isinstance(devs, list):
    for dev in devs:
      drivername = dev.device.get_field_value('driver_name')
      if util.isEmpty(drivername):
        vdevs.append(dev)
  else:
    drivername = devs.device.get_field_value('driver_name')
    if util.isEmpty(drivername):
      vdevs.append(devs)

  return vdevs

class vdom_links(object):
  #XPATH devices/device/vdom-links/vdom-link
  class vdom_link(AbstractDeviceMgr):
    key_hints = [[]]
    def getRcpathPayload(self, sdata, dev, mapping_dict):
      dev = get_valid_devices(dev)
      if len(dev) == 0:
        return
      #convert parent keys to list
      rcpath_list = []
      payload_list = []
      ##prepare rcpath
      rcpath = "firewall:vdom-links"
      rcpath_list.append(rcpath)
      mapping_dict = kwargs.get('mapping_dict')

      #validating inputs and get payload object
      vdom_link_object_list = self.validate_inputs_form_payload(mapping_dict)

      for vdom_link_object in vdom_link_object_list:
        #fetch payload
        vdom_link_payload = vdom_link_object.getxml(filter=True)
        util.log_debug('vdom_link_payload %s'%vdom_link_payload)
        payload_list.append(vdom_link_payload)

      return rcpath_list, payload_list

    def create(self, sdata, dev, mapping_dict, addref=True, autocommit=True):
      dev = get_valid_devices(dev)
      if len(dev) == 0:
        return
      #convert parent keys to list
      ##prepare rcpath
      rcpath = "firewall:vdom-links"
      self.create_(sdata, dev, mapping_dict=mapping_dict, addref=addref, autocommit=autocommit, rcpath=rcpath)

    def create_(self, sdata, dev, **kwargs):
      mapping_dict = kwargs.get('mapping_dict')

      #validating inputs and get payload object
      vdom_link_object_list = self.validate_inputs_form_payload(mapping_dict)

      for vdom_link_object in vdom_link_object_list:
        #fetch payload
        vdom_link_payload = vdom_link_object.getxml(filter=True)

        util.log_debug('vdom_link_payload %s'%vdom_link_payload)

        #call the base abstract class for createData
        super(self.__class__, self).create_(sdata, dev, rcpath=kwargs.get('rcpath'), payload=vdom_link_payload, key_hints=self.key_hints, addref=kwargs.get('addref', True), autocommit=kwargs.get('autocommit', True), mapping_dict=mapping_dict)

    def update(self, sdata, dev, mapping_dict, addref=False, autocommit=True):
      dev = get_valid_devices(dev)
      if len(dev) == 0:
        return

      ##prepare rcpath
      rcpath = "firewall:vdom-links"
      self.update_(sdata, dev, mapping_dict=mapping_dict, addref=addref, autocommit=autocommit, rcpath=rcpath)

    def update_(self, sdata, dev, **kwargs):
      mapping_dict = kwargs.get('mapping_dict')

      #validating inputs and get payload object
      vdom_link_object_list = self.validate_inputs_form_payload(mapping_dict, update=True)

      #convert keys to list
      name = mapping_dict.get('name')
      if not isinstance(name, list):
        name = [name]

      #prepare rcpath
      rcpath = kwargs.get('rcpath')
      rcpath_list = []
      for name_iterator in name:
        rcpath_tmp =  rcpath+"/vdom-link=%s"%(util.make_interfacename(name_iterator))
        rcpath_list.append(rcpath_tmp)
      for rc_counter, vdom_link_object in enumerate(vdom_link_object_list):
        #fetch payload
        vdom_link_payload = vdom_link_object.getxml(filter=True)

        util.log_debug('update vdom_link_payload %s'%vdom_link_payload)

        rcpath = rcpath_list[rc_counter]
        #call the base abstract class for createData
        super(self.__class__, self).update_(sdata, dev, rcpath=rcpath, payload=vdom_link_payload, key_hints=self.key_hints, addref=kwargs.get('addref', False), autocommit=kwargs.get('autocommit', True), mapping_dict=mapping_dict)

    def delete(self, sdata, dev, mapping_dict, fail_silently=False, remove_reference=False):
      dev = get_valid_devices(dev)
      if len(dev) == 0:
        return

      ##prepare rcpath
      rcpath = "firewall:vdom-links"
      self.delete_(sdata, dev, mapping_dict=mapping_dict, fail_silently=fail_silently, remove_reference=remove_reference, rcpath=rcpath)

    def delete_(self, sdata, dev, **kwargs):
      mapping_dict = kwargs.get('mapping_dict')

      #validating inputs
      if util.isEmpty(mapping_dict.get('name')):
        raise Exception("'name' cannot be empty")

      #convert keys to list
      name = mapping_dict.get('name')
      if not isinstance(name, list):
        name = [name]

      #prepare rcpath
      rcpath = kwargs.get('rcpath')
      rcpath_list = []
      for name_iterator in name:
        rcpath_tmp =  rcpath+"/vdom-link=%s"%(util.make_interfacename(name_iterator))
        rcpath_list.append(rcpath_tmp)
      payload = ''

      for rcpath in rcpath_list:
        #call the base abstract class for deleteData
        super(self.__class__, self).delete_(sdata, dev, rcpath=rcpath, payload=payload, fail_silently=kwargs.get('fail_silently', False), remove_reference=kwargs.get('remove_reference', False))

    def validate_inputs_form_payload(self, mapping_dict, update=False):
      #validating inputs
      if util.isEmpty(mapping_dict.get('name')):
        raise Exception("'name' cannot be empty")
      if util.isEmpty(mapping_dict.get('vdom_link_interface0')):
        raise Exception("'vdom_link_interface0' cannot be empty")
      if util.isEmpty(mapping_dict.get('interface0_vdom_name')):
        raise Exception("'interface0_vdom_name' cannot be empty")
      if util.isEmpty(mapping_dict.get('interface0_ip_address')):
        raise Exception("'interface0_ip_address' cannot be empty")
      if util.isEmpty(mapping_dict.get('interface0_netmask')):
        raise Exception("'interface0_netmask' cannot be empty")
      if util.isEmpty(mapping_dict.get('vdom_link_interface1')):
        raise Exception("'vdom_link_interface1' cannot be empty")
      if util.isEmpty(mapping_dict.get('interface1_vdom_name')):
        raise Exception("'interface1_vdom_name' cannot be empty")
      if util.isEmpty(mapping_dict.get('interface1_ip_address')):
        raise Exception("'interface1_ip_address' cannot be empty")
      if util.isEmpty(mapping_dict.get('interface1_netmask')):
        raise Exception("'interface1_netmask' cannot be empty")

      #convert keys to list
      name = mapping_dict.get('name')
      if not isinstance(name, list):
        name = [name]

      #prepare payload
      vdom_link_object_list = []
      for name_iterator in name:
        from servicemodel.controller.devices.device import vdom_links
        vdom_link_object = vdom_links.vdom_link.vdom_link()
        vdom_link_object.name = name_iterator
        try:
          if (update == False) or (update == True and str(mapping_dict.get('vdom_link_interface0', None)) != ''):
            vdom_link_object.vdom_link_interface0 = mapping_dict.get('vdom_link_interface0', None)
          else:
            vdom_link_object.vdom_link_interface0._empty_tag = True
        except TypeError:
          pass
        try:
          if (update == False) or (update == True and str(mapping_dict.get('interface0_vdom_name', None)) != ''):
            vdom_link_object.interface0_vdom_name = mapping_dict.get('interface0_vdom_name', None)
          else:
            vdom_link_object.interface0_vdom_name._empty_tag = True
        except TypeError:
          pass
        try:
          if (update == False) or (update == True and str(mapping_dict.get('interface0_ip_address', None)) != ''):
            vdom_link_object.interface0_ip_address = mapping_dict.get('interface0_ip_address', None)
          else:
            vdom_link_object.interface0_ip_address._empty_tag = True
        except TypeError:
          pass
        try:
          if (update == False) or (update == True and str(mapping_dict.get('interface0_netmask', None)) != ''):
            vdom_link_object.interface0_netmask = mapping_dict.get('interface0_netmask', None)
          else:
            vdom_link_object.interface0_netmask._empty_tag = True
        except TypeError:
          pass
        try:
          if (update == False) or (update == True and str(mapping_dict.get('interface0_access_types', None)) != ''):
            vdom_link_object.interface0_access_types = mapping_dict.get('interface0_access_types', None)
          else:
            vdom_link_object.interface0_access_types._empty_tag = True
        except TypeError:
          pass
        try:
          if (update == False) or (update == True and str(mapping_dict.get('vdom_link_interface1', None)) != ''):
            vdom_link_object.vdom_link_interface1 = mapping_dict.get('vdom_link_interface1', None)
          else:
            vdom_link_object.vdom_link_interface1._empty_tag = True
        except TypeError:
          pass
        try:
          if (update == False) or (update == True and str(mapping_dict.get('interface1_vdom_name', None)) != ''):
            vdom_link_object.interface1_vdom_name = mapping_dict.get('interface1_vdom_name', None)
          else:
            vdom_link_object.interface1_vdom_name._empty_tag = True
        except TypeError:
          pass
        try:
          if (update == False) or (update == True and str(mapping_dict.get('interface1_ip_address', None)) != ''):
            vdom_link_object.interface1_ip_address = mapping_dict.get('interface1_ip_address', None)
          else:
            vdom_link_object.interface1_ip_address._empty_tag = True
        except TypeError:
          pass
        try:
          if (update == False) or (update == True and str(mapping_dict.get('interface1_netmask', None)) != ''):
            vdom_link_object.interface1_netmask = mapping_dict.get('interface1_netmask', None)
          else:
            vdom_link_object.interface1_netmask._empty_tag = True
        except TypeError:
          pass
        try:
          if (update == False) or (update == True and str(mapping_dict.get('interface1_access_types', None)) != ''):
            vdom_link_object.interface1_access_types = mapping_dict.get('interface1_access_types', None)
          else:
            vdom_link_object.interface1_access_types._empty_tag = True
        except TypeError:
          pass
        vdom_link_object_list.append(vdom_link_object)

      return vdom_link_object_list

