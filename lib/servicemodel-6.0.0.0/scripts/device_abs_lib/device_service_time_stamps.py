#
# This computer program is the confidential information and proprietary trade
# secret of Anuta Networks, Inc. Possessions and use of this program must
# conform strictly to the license agreement between the user and
# Anuta Networks, Inc., and receipt or possession does not convey any rights
# to divulge, reproduce, or allow others to use this program without specific
# written authorization of Anuta Networks, Inc.
#
# Copyright (c) 2015-2016 Anuta Networks, Inc. All Rights Reserved.
#

#
#DO NOT EDIT THIS FILE ITS AUTOGENERATED ONE
#

from servicemodel import util
from servicemodel import yang
from servicemodel import devicemgr
from servicemodel.abstract_dev_mgr import AbstractDeviceMgr

def get_valid_devices(devs):
  vdevs = []
  if isinstance(devs, list):
    for dev in devs:
      drivername = dev.device.get_field_value('driver_name')
      if util.isEmpty(drivername):
        vdevs.append(dev)
  else:
    drivername = devs.device.get_field_value('driver_name')
    if util.isEmpty(drivername):
      vdevs.append(devs)

  return vdevs

#XPATH devices/device/service-time-stamps
class service_time_stamps(AbstractDeviceMgr):
  key_hints = [[]]
  def getRcpathPayload(self, sdata, dev, mapping_dict):
    dev = get_valid_devices(dev)
    if len(dev) == 0:
      return
    #convert parent keys to list
    rcpath_list = []
    payload_list = []
    ##prepare rcpath
    rcpath = ""
    rcpath_list.append(rcpath)
    mapping_dict = kwargs.get('mapping_dict')

    #validating inputs and get payload object
    service_time_stamps_object_list = self.validate_inputs_form_payload(mapping_dict)

    for service_time_stamps_object in service_time_stamps_object_list:
      #fetch payload
      service_time_stamps_payload = service_time_stamps_object.getxml(filter=True)
      util.log_debug('service_time_stamps_payload %s'%service_time_stamps_payload)
      payload_list.append(service_time_stamps_payload)

    return rcpath_list, payload_list

  def create(self, sdata, dev, mapping_dict, addref=True, autocommit=True):
    dev = get_valid_devices(dev)
    if len(dev) == 0:
      return
    #convert parent keys to list
    ##prepare rcpath
    rcpath = ""
    self.create_(sdata, dev, mapping_dict=mapping_dict, addref=addref, autocommit=autocommit, rcpath=rcpath)

  def create_(self, sdata, dev, **kwargs):
    mapping_dict = kwargs.get('mapping_dict')

    #validating inputs and get payload object
    service_time_stamps_object_list = self.validate_inputs_form_payload(mapping_dict)

    for service_time_stamps_object in service_time_stamps_object_list:
      #fetch payload
      service_time_stamps_payload = service_time_stamps_object.getxml(filter=True)

      util.log_debug('service_time_stamps_payload %s'%service_time_stamps_payload)

      #call the base abstract class for createData
      super(self.__class__, self).create_(sdata, dev, rcpath=kwargs.get('rcpath'), payload=service_time_stamps_payload, key_hints=self.key_hints, addref=kwargs.get('addref', True), autocommit=kwargs.get('autocommit', True), mapping_dict=mapping_dict)

  def update(self, sdata, dev, mapping_dict, addref=False, autocommit=True):
    dev = get_valid_devices(dev)
    if len(dev) == 0:
      return

    ##prepare rcpath
    rcpath = ""
    self.update_(sdata, dev, mapping_dict=mapping_dict, addref=addref, autocommit=autocommit, rcpath=rcpath)

  def update_(self, sdata, dev, **kwargs):
    mapping_dict = kwargs.get('mapping_dict')

    #validating inputs and get payload object
    service_time_stamps_object_list = self.validate_inputs_form_payload(mapping_dict, update=True)

    #convert keys to list

    #prepare rcpath
    rcpath = kwargs.get('rcpath')
    rcpath_list = []
    rcpath_tmp =  rcpath+"/service-time-stamps"
    rcpath_list.append(rcpath_tmp)
    for rc_counter, service_time_stamps_object in enumerate(service_time_stamps_object_list):
      #fetch payload
      service_time_stamps_payload = service_time_stamps_object.getxml(filter=True)

      util.log_debug('update service_time_stamps_payload %s'%service_time_stamps_payload)

      rcpath = rcpath_list[rc_counter]
      #call the base abstract class for createData
      super(self.__class__, self).update_(sdata, dev, rcpath=rcpath, payload=service_time_stamps_payload, key_hints=self.key_hints, addref=kwargs.get('addref', False), autocommit=kwargs.get('autocommit', True), mapping_dict=mapping_dict)

  def delete(self, sdata, dev, mapping_dict, fail_silently=False, remove_reference=False):
    dev = get_valid_devices(dev)
    if len(dev) == 0:
      return

    ##prepare rcpath
    rcpath = ""
    self.delete_(sdata, dev, mapping_dict=mapping_dict, fail_silently=fail_silently, remove_reference=remove_reference, rcpath=rcpath)

  def delete_(self, sdata, dev, **kwargs):
    mapping_dict = kwargs.get('mapping_dict')

    #validating inputs

    #convert keys to list

    #prepare rcpath
    rcpath = kwargs.get('rcpath')
    rcpath_list = []
    rcpath_tmp =  rcpath+"/service-time-stamps"
    rcpath_list.append(rcpath_tmp)
    payload = ''

    for rcpath in rcpath_list:
      #call the base abstract class for deleteData
      super(self.__class__, self).delete_(sdata, dev, rcpath=rcpath, payload=payload, fail_silently=kwargs.get('fail_silently', False), remove_reference=kwargs.get('remove_reference', False))

  def validate_inputs_form_payload(self, mapping_dict, update=False):
    #validating inputs

    #convert keys to list

    #prepare payload
    service_time_stamps_object_list = []
    from servicemodel.controller.devices import device
    service_time_stamps_object = device.service_time_stamps.service_time_stamps()
    try:
      if (update == False) or (update == True and str(mapping_dict.get('service_timestamps_debug', None)) != ''):
        service_time_stamps_object.service_timestamps_debug = mapping_dict.get('service_timestamps_debug', None)
      else:
        service_time_stamps_object.service_timestamps_debug._empty_tag = True
    except TypeError:
      pass
    try:
      if (update == False) or (update == True and str(mapping_dict.get('service_timestamps_log', None)) != ''):
        service_time_stamps_object.service_timestamps_log = mapping_dict.get('service_timestamps_log', None)
      else:
        service_time_stamps_object.service_timestamps_log._empty_tag = True
    except TypeError:
      pass
    try:
      if (update == False) or (update == True and str(mapping_dict.get('service_password_encryption', None)) != ''):
        service_time_stamps_object.service_password_encryption = mapping_dict.get('service_password_encryption', None)
      else:
        service_time_stamps_object.service_password_encryption._empty_tag = True
    except TypeError:
      pass
    try:
      if (update == False) or (update == True and str(mapping_dict.get('enable_secret', None)) != ''):
        service_time_stamps_object.enable_secret = mapping_dict.get('enable_secret', None)
      else:
        service_time_stamps_object.enable_secret._empty_tag = True
    except TypeError:
      pass
    try:
      if (update == False) or (update == True and str(mapping_dict.get('enable_secret_password', None)) != ''):
        service_time_stamps_object.enable_secret_password = mapping_dict.get('enable_secret_password', None)
      else:
        service_time_stamps_object.enable_secret_password._empty_tag = True
    except TypeError:
      pass
    try:
      if (update == False) or (update == True and str(mapping_dict.get('tcp_keepalives_in', None)) != ''):
        service_time_stamps_object.tcp_keepalives_in = mapping_dict.get('tcp_keepalives_in', None)
      else:
        service_time_stamps_object.tcp_keepalives_in._empty_tag = True
    except TypeError:
      pass
    service_time_stamps_object_list.append(service_time_stamps_object)

    return service_time_stamps_object_list

  #XPATH devices/device/service-time-stamps/service-tstamps
  class service_tstamps(AbstractDeviceMgr):
    key_hints = [[]]
    def getRcpathPayload(self, sdata, dev, mapping_dict):
      dev = get_valid_devices(dev)
      if len(dev) == 0:
        return
      #convert parent keys to list
      rcpath_list = []
      payload_list = []
      ##prepare rcpath
      rcpath = "basicDeviceConfigs:service-time-stamps"
      rcpath_list.append(rcpath)
      mapping_dict = kwargs.get('mapping_dict')

      #validating inputs and get payload object
      service_tstamps_object_list = self.validate_inputs_form_payload(mapping_dict)

      for service_tstamps_object in service_tstamps_object_list:
        #fetch payload
        service_tstamps_payload = service_tstamps_object.getxml(filter=True)
        util.log_debug('service_tstamps_payload %s'%service_tstamps_payload)
        payload_list.append(service_tstamps_payload)

      return rcpath_list, payload_list

    def create(self, sdata, dev, mapping_dict, addref=True, autocommit=True):
      dev = get_valid_devices(dev)
      if len(dev) == 0:
        return
      #convert parent keys to list
      ##prepare rcpath
      rcpath = "basicDeviceConfigs:service-time-stamps"
      self.create_(sdata, dev, mapping_dict=mapping_dict, addref=addref, autocommit=autocommit, rcpath=rcpath)

    def create_(self, sdata, dev, **kwargs):
      mapping_dict = kwargs.get('mapping_dict')

      #validating inputs and get payload object
      service_tstamps_object_list = self.validate_inputs_form_payload(mapping_dict)

      for service_tstamps_object in service_tstamps_object_list:
        #fetch payload
        service_tstamps_payload = service_tstamps_object.getxml(filter=True)

        util.log_debug('service_tstamps_payload %s'%service_tstamps_payload)

        #call the base abstract class for createData
        super(self.__class__, self).create_(sdata, dev, rcpath=kwargs.get('rcpath'), payload=service_tstamps_payload, key_hints=self.key_hints, addref=kwargs.get('addref', True), autocommit=kwargs.get('autocommit', True), mapping_dict=mapping_dict)

    def update(self, sdata, dev, mapping_dict, addref=False, autocommit=True):
      dev = get_valid_devices(dev)
      if len(dev) == 0:
        return

      ##prepare rcpath
      rcpath = "basicDeviceConfigs:service-time-stamps"
      self.update_(sdata, dev, mapping_dict=mapping_dict, addref=addref, autocommit=autocommit, rcpath=rcpath)

    def update_(self, sdata, dev, **kwargs):
      mapping_dict = kwargs.get('mapping_dict')

      #validating inputs and get payload object
      service_tstamps_object_list = self.validate_inputs_form_payload(mapping_dict, update=True)

      #convert keys to list
      service_time_stamp_level = mapping_dict.get('service_time_stamp_level')
      if not isinstance(service_time_stamp_level, list):
        service_time_stamp_level = [service_time_stamp_level]

      #prepare rcpath
      rcpath = kwargs.get('rcpath')
      rcpath_list = []
      for service_time_stamp_level_iterator in service_time_stamp_level:
        rcpath_tmp =  rcpath+"/service-tstamps=%s"%(util.make_interfacename(service_time_stamp_level_iterator))
        rcpath_list.append(rcpath_tmp)
      for rc_counter, service_tstamps_object in enumerate(service_tstamps_object_list):
        #fetch payload
        service_tstamps_payload = service_tstamps_object.getxml(filter=True)

        util.log_debug('update service_tstamps_payload %s'%service_tstamps_payload)

        rcpath = rcpath_list[rc_counter]
        #call the base abstract class for createData
        super(self.__class__, self).update_(sdata, dev, rcpath=rcpath, payload=service_tstamps_payload, key_hints=self.key_hints, addref=kwargs.get('addref', False), autocommit=kwargs.get('autocommit', True), mapping_dict=mapping_dict)

    def delete(self, sdata, dev, mapping_dict, fail_silently=False, remove_reference=False):
      dev = get_valid_devices(dev)
      if len(dev) == 0:
        return

      ##prepare rcpath
      rcpath = "basicDeviceConfigs:service-time-stamps"
      self.delete_(sdata, dev, mapping_dict=mapping_dict, fail_silently=fail_silently, remove_reference=remove_reference, rcpath=rcpath)

    def delete_(self, sdata, dev, **kwargs):
      mapping_dict = kwargs.get('mapping_dict')

      #validating inputs
      if util.isEmpty(mapping_dict.get('service_time_stamp_level')):
        raise Exception("'service_time_stamp_level' cannot be empty")

      #convert keys to list
      service_time_stamp_level = mapping_dict.get('service_time_stamp_level')
      if not isinstance(service_time_stamp_level, list):
        service_time_stamp_level = [service_time_stamp_level]

      #prepare rcpath
      rcpath = kwargs.get('rcpath')
      rcpath_list = []
      for service_time_stamp_level_iterator in service_time_stamp_level:
        rcpath_tmp =  rcpath+"/service-tstamps=%s"%(util.make_interfacename(service_time_stamp_level_iterator))
        rcpath_list.append(rcpath_tmp)
      payload = ''

      for rcpath in rcpath_list:
        #call the base abstract class for deleteData
        super(self.__class__, self).delete_(sdata, dev, rcpath=rcpath, payload=payload, fail_silently=kwargs.get('fail_silently', False), remove_reference=kwargs.get('remove_reference', False))

    def validate_inputs_form_payload(self, mapping_dict, update=False):
      #validating inputs
      if util.isEmpty(mapping_dict.get('service_time_stamp_level')):
        raise Exception("'service_time_stamp_level' cannot be empty")

      #convert keys to list
      service_time_stamp_level = mapping_dict.get('service_time_stamp_level')
      if not isinstance(service_time_stamp_level, list):
        service_time_stamp_level = [service_time_stamp_level]

      #prepare payload
      service_tstamps_object_list = []
      for service_time_stamp_level_iterator in service_time_stamp_level:
        from servicemodel.controller.devices.device import service_time_stamps
        service_tstamps_object = service_time_stamps.service_tstamps.service_tstamps()
        service_tstamps_object.service_time_stamp_level = service_time_stamp_level_iterator
        try:
          if (update == False) or (update == True and str(mapping_dict.get('service_time_stamp_with', None)) != ''):
            service_tstamps_object.service_time_stamp_with = mapping_dict.get('service_time_stamp_with', None)
          else:
            service_tstamps_object.service_time_stamp_with._empty_tag = True
        except TypeError:
          pass
        try:
          if (update == False) or (update == True and str(mapping_dict.get('service_tstamp_datetime_option_1', None)) != ''):
            service_tstamps_object.service_tstamp_datetime_option_1 = mapping_dict.get('service_tstamp_datetime_option_1', None)
          else:
            service_tstamps_object.service_tstamp_datetime_option_1._empty_tag = True
        except TypeError:
          pass
        try:
          if (update == False) or (update == True and str(mapping_dict.get('service_tstamp_datetime_option_2', None)) != ''):
            service_tstamps_object.service_tstamp_datetime_option_2 = mapping_dict.get('service_tstamp_datetime_option_2', None)
          else:
            service_tstamps_object.service_tstamp_datetime_option_2._empty_tag = True
        except TypeError:
          pass
        try:
          if (update == False) or (update == True and str(mapping_dict.get('service_tstamp_datetime_option_3', None)) != ''):
            service_tstamps_object.service_tstamp_datetime_option_3 = mapping_dict.get('service_tstamp_datetime_option_3', None)
          else:
            service_tstamps_object.service_tstamp_datetime_option_3._empty_tag = True
        except TypeError:
          pass
        service_tstamps_object_list.append(service_tstamps_object)

      return service_tstamps_object_list

